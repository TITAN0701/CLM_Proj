# 整合測試情境：RXCLM 跨模組整合驗證

> 系統：Ruenxin CLM
> 版本：2026-06-25
> 範圍：跨模組端對端整合情境（與 scenarios.md 互補，scenarios.md 驗單頁行為，此檔驗跨模組串聯）
> 命名規則：IT-{分類}-{序號}-{子情境序號}

---

## 憑證生命週期

### IT-LC-NEG-01-01 申請送出後 CA 非同步失敗 → Order 頁顯示 FAILED 狀態
- **Given** 已登入 admin，Account 設定為會觸發 CA 回傳失敗的環境
- **When** 填寫憑證申請表單並送出
- **Then** Order 詳細頁狀態應顯示 FAILED，不是申請頁彈窗

### IT-LC-NEG-01-02 Order FAILED 後詳細頁顯示失敗原因
- **Given** Order 狀態為 FAILED
- **When** 進入該 Order 詳細頁
- **Then** 應顯示 CA 回傳的具體失敗原因訊息

### IT-LC-NEG-01-03 Order FAILED 後憑證列表無新增該憑證
- **Given** Order 狀態為 FAILED
- **When** 進入憑證列表
- **Then** 不應出現該申請對應的 ISSUED 憑證

---

### IT-LC-NEG-02-01 匯入完全空白 PEM → 前端拒絕
- **Given** 已登入 admin，進入匯入既有憑證頁
- **When** PEM 欄位留空直接送出
- **Then** 應顯示「格式錯誤」或「必填」驗證訊息，不送出至後端

### IT-LC-NEG-02-02 匯入空字串 PEM → 後端拒絕並回傳錯誤
- **Given** 已登入 admin，進入匯入既有憑證頁
- **When** 貼入空字串並送出
- **Then** 後端應回傳格式錯誤，頁面顯示錯誤訊息

### IT-LC-NEG-02-03 空白 PEM 與損壞 PEM 的錯誤訊息不同
- **Given** 已登入 admin
- **When** 分別貼入空白與損壞 PEM 各送出一次
- **Then** 兩次的錯誤訊息應不同（空白 vs 格式無效），各自有明確說明

---

### IT-LC-STATUS-01-01 PENDING_DNS 狀態 Order 顯示 DNS 挑戰資訊
- **Given** 有一筆 Order 處於 PENDING_DNS 狀態
- **When** 進入該 Order 詳細頁
- **Then** 應顯示 DNS 挑戰類型、Record Name、Record Value

### IT-LC-STATUS-01-02 DNS 驗證完成後 Order 狀態更新為 ISSUED
- **Given** Order 處於 PENDING_DNS，DNS 挑戰記錄已正確設定
- **When** 等待 DNS 驗證完成（系統輪詢）
- **Then** Order 狀態應自動更新為 ISSUED，不需手動重整

### IT-LC-STATUS-01-03 Order ISSUED 後憑證列表同步出現該憑證
- **Given** Order 狀態剛從 PENDING_DNS 轉為 ISSUED
- **When** 進入憑證列表
- **Then** 應出現對應 CN 的 ISSUED 憑證，到期日正確

---

### IT-LC-01-01 申請新憑證 → Order issued → 憑證列表顯示 ISSUED
- **Given** 已設定可用 ACME Account + DNS Provider
- **When** 填寫申請表單送出，等待 CA 處理完成
- **Then** Order 狀態為 ISSUED，憑證列表出現對應 CN 的憑證

### IT-LC-01-02 ISSUED 憑證出現在 30 天到期快篩（模擬近到期）
- **Given** 憑證列表有 ISSUED 憑證
- **When** 套用「30 天內到期」快篩
- **Then** 近到期的憑證應出現在列表中

### IT-LC-01-03 觸發 Renew → 新 Order 產生並進入流程
- **Given** 有 ISSUED 憑證且到期日在 30 天內
- **When** 在憑證列表或網域詳細頁手動觸發 Renew
- **Then** 產生新的 Order，狀態為 PENDING 或 PROCESSING

---

## 部署流程

### IT-DP-NEG-01-01 Endpoint 設定不可達主機 → 部署任務觸發後標為 Error
- **Given** 已登入 admin，Endpoint 主機設定為不可達 IP，有 Pending 部署任務
- **When** 觸發對該 Endpoint 的部署
- **Then** 部署任務狀態應標為 Error，不顯示 Done

### IT-DP-NEG-01-02 部署 Error 後待審清單仍保留該任務
- **Given** 部署任務狀態為 Error
- **When** 進入部署核准頁
- **Then** 該任務仍顯示在列表中，狀態標為 Error（非消失）

### IT-DP-NEG-01-03 部署 Error 後可重新觸發部署
- **Given** 部署任務狀態為 Error
- **When** 修正 Endpoint 連線設定後重新觸發
- **Then** 部署應重新執行，狀態更新

---

### IT-DP-NEG-02-01 新增 webhook Endpoint 填入非 URL 格式 → 前端驗證拒絕
- **Given** 已登入 admin，進入新增 Endpoint 表單，選擇 webhook 類型
- **When** URL 欄位填入 `not-a-url` 並送出
- **Then** 前端應顯示 URL 格式驗證錯誤，不送出至後端

### IT-DP-NEG-02-02 新增 webhook Endpoint 填入無 schema 的 URL → 前端拒絕
- **Given** 已登入 admin，進入新增 Endpoint 表單，選擇 webhook 類型
- **When** URL 欄位填入 `192.168.0.1`（無 http://）並送出
- **Then** 應顯示 URL 格式錯誤提示

### IT-DP-NEG-02-03 新增 webhook Endpoint 填入有效 URL → 可成功儲存
- **Given** 已登入 admin，進入新增 Endpoint 表單，選擇 webhook 類型
- **When** URL 欄位填入合法 URL（如 `http://192.168.0.1:8080/hook`）並送出
- **Then** Endpoint 儲存成功，出現在清單中，操作完成後自行刪除

---

### IT-DP-ROLE-01-01 user 角色嘗試進入 /admin/approvals → 被導向
- **Given** 已以 user 角色登入
- **When** 直接瀏覽 /admin/approvals
- **Then** 應被導向無權限頁或跳回首頁，不顯示部署核准內容

### IT-DP-ROLE-01-02 supervisor 角色可正常進入 /admin/approvals
- **Given** 已以 supervisor 角色登入
- **When** 進入 /admin/approvals
- **Then** 應正常顯示待審清單

### IT-DP-ROLE-01-03 user 角色導航列不顯示「部署核准」入口
- **Given** 已以 user 角色登入
- **When** 查看導航列
- **Then** 不應出現「部署核准」或 /admin/approvals 的連結

---

### IT-DP-01-01 憑證 issued 後部署核准待審清單自動出現
- **Given** 已設定 Endpoint 與合規政策，憑證申請完成並 issued
- **When** 進入部署核准頁
- **Then** 待審清單應出現對應的部署任務

### IT-DP-01-02 supervisor 核准後 Endpoint 接收部署
- **Given** 有待審部署任務
- **When** supervisor 登入並核准該任務
- **Then** 部署狀態更新為 Done，Endpoint 接收到憑證

### IT-DP-01-03 核准後稽核紀錄出現核准操作
- **Given** supervisor 剛完成核准操作
- **When** 進入 /audit
- **Then** 應有一筆核准操作的稽核紀錄，含操作者與時間

---

## CA / DNS / Account 設定

### IT-CA-NEG-01-01 新增 CA 機構表單開啟後點取消 → 清單無新增
- **Given** 已登入 admin，進入 CA 機構管理
- **When** 點擊「新增」→ 填入 CA 資訊 → 點「取消」
- **Then** CA 清單無新增項目，筆數不變

### IT-CA-NEG-01-02 取消新增後再次開啟表單 → 欄位為空
- **Given** 已執行過一次取消新增
- **When** 再次點擊「新增」
- **Then** 表單欄位應為空，不保留上次填入的值

---

### IT-DNS-NEG-01-01 Cloudflare DNS Provider 填入無效 Token → 測試連線失敗
- **Given** 已登入 admin，新增 Cloudflare 類型 DNS Provider
- **When** 填入無效 API Token 並點「測試連線」
- **Then** 顯示連線失敗錯誤訊息

### IT-DNS-NEG-01-02 測試連線失敗後表單仍可修正重送
- **Given** 測試連線顯示失敗
- **When** 修正 Token 後再次點「測試連線」
- **Then** 若 Token 正確則顯示連線成功；錯誤則再次失敗

---

### IT-DNS-FORM-01-01 新增表單選 Manual 類型 → 顯示對應欄位
- **Given** 已登入 admin，進入新增 DNS Provider 表單
- **When** 類型選擇 Manual
- **Then** 顯示 Manual 類型專屬欄位，不顯示 API Token 欄位

### IT-DNS-FORM-01-02 切換類型 Manual → Cloudflare → 欄位動態更換
- **Given** 已選 Manual 類型且欄位已顯示
- **When** 切換類型至 Cloudflare
- **Then** 欄位切換為 Cloudflare 專屬欄位（API Token），Manual 欄位消失

### IT-DNS-FORM-01-03 切換回 Manual 後欄位重置為空
- **Given** 曾切換至 Cloudflare 並填入部分資料
- **When** 切換回 Manual
- **Then** Manual 欄位重新顯示且為空，不帶入 Cloudflare 的殘留值

---

### IT-CA-01-01 新增 CA Provider → 申請憑證表單的 CA 下拉出現新 CA
- **Given** 已登入 admin
- **When** 在 CA 機構管理新增一個 CA Provider，再前往申請新憑證頁
- **Then** CA 下拉選單應出現剛新增的 CA 名稱

### IT-CA-01-02 選用新增的 CA 送出申請 → Order 正常產生
- **Given** 申請表單已選用剛新增的 CA
- **When** 填寫完整表單並送出
- **Then** Order 正常產生，CA 欄位顯示為剛新增的 CA 名稱

### IT-CA-01-03 刪除 CA Provider 後申請表單下拉不再出現該 CA
- **Given** 完成驗證後刪除剛新增的 CA
- **When** 回到申請新憑證頁
- **Then** CA 下拉選單不再出現已刪除的 CA

---

### IT-DNS-01-01 新增 DNS Provider → 申請憑證 DNS 驗證選項出現新 Provider
- **Given** 已登入 admin
- **When** 在 DNS 機構新增一個 Provider，再前往申請新憑證頁
- **Then** DNS 驗證方式下拉應出現剛新增的 Provider 名稱

### IT-DNS-01-02 選用新增的 DNS Provider 送出申請 → Order 使用正確 DNS
- **Given** 申請表單已選用剛新增的 DNS Provider
- **When** 填寫完整表單並送出
- **Then** Order 詳細頁顯示使用的 DNS Provider 為剛新增者

### IT-DNS-01-03 刪除 DNS Provider 後申請表單下拉不再出現該 Provider
- **Given** 完成驗證後刪除剛新增的 Provider
- **When** 回到申請新憑證頁
- **Then** DNS 驗證方式下拉不再出現已刪除的 Provider

---

## 帳號權限

### IT-UA-NEG-01-01 修改密碼填入錯誤舊密碼 → Auth 拒絕
- **Given** 已登入
- **When** 前往我的帳號頁，修改密碼填入錯誤的舊密碼並送出
- **Then** Auth 拒絕，UI 顯示「舊密碼錯誤」或類似錯誤訊息

### IT-UA-NEG-01-02 錯誤舊密碼被拒後密碼未變更 → 原密碼仍可登入
- **Given** 修改密碼因錯誤舊密碼被拒
- **When** 登出後以原密碼重新登入
- **Then** 登入成功，密碼未被修改

### IT-UA-NEG-01-03 輸入正確舊密碼 → 密碼修改成功 → 舊密碼無效
- **Given** 已登入
- **When** 修改密碼填入正確舊密碼與新密碼並送出
- **Then** 修改成功，以舊密碼登入應失敗

---

### IT-UA-NEG-02-01 新增 User 填入非 email 格式 → 前端驗證拒絕
- **Given** 已登入 admin，進入 User 管理新增表單
- **When** email 欄填入 `not-an-email` 並送出
- **Then** 前端顯示 email 格式驗證錯誤，不送出至後端

### IT-UA-NEG-02-02 新增 User 填入缺少 @ 的字串 → 前端驗證拒絕
- **Given** 已登入 admin，進入 User 管理新增表單
- **When** email 欄填入 `userdomain.com` 並送出
- **Then** 前端顯示格式驗證錯誤

### IT-UA-NEG-02-03 填入合法 email 格式 → 可成功新增（完成後自行刪除）
- **Given** 已登入 admin，進入 User 管理新增表單
- **When** 填入合法 email 格式（如 `test-it@rxclm.local`）並完成送出
- **Then** User 成功建立，出現在 User 清單，操作完成後自行刪除

---

### IT-UA-ROLE-01-01 user 角色嘗試進入 /admin/users → 被導向
- **Given** 已以 user 角色登入
- **When** 直接瀏覽 /admin/users
- **Then** 應被導向無權限頁或跳回首頁

### IT-UA-ROLE-01-02 user 角色嘗試進入 /admin/policies → 被導向
- **Given** 已以 user 角色登入
- **When** 直接瀏覽 /admin/policies
- **Then** 應被導向無權限頁或跳回首頁

### IT-UA-ROLE-01-03 admin 角色可正常進入所有 /admin/* 路由
- **Given** 已以 admin 角色登入
- **When** 依序進入 /admin/users、/admin/policies、/admin/approvals
- **Then** 三個頁面均正常顯示內容

---

### IT-GRP-PERM-01-01 建立 Group 並設定可存取指定 Account
- **Given** 已登入 admin
- **When** 建立新 Group，設定可存取特定 ACME Account
- **Then** Group 設定儲存成功，設定頁顯示正確的 Account 存取範圍

### IT-GRP-PERM-01-02 將 User 加入該 Group → User 登入後可見該 Account
- **Given** Group 已設定可存取特定 Account，User 已加入該 Group
- **When** 以該 User 帳號登入
- **Then** 帳號列表應出現被授權的 Account，不在授權範圍的 Account 不顯示

### IT-GRP-PERM-01-03 將 User 移出 Group → User 再次登入後 Account 消失
- **Given** User 在 Group 中可見授權 Account
- **When** admin 將 User 從 Group 移除，User 重新登入
- **Then** 該 Account 不再顯示於 User 的帳號列表

---

### IT-UA-01-01 新增 User 並指派有限 Group → 憑證可見範圍受限
- **Given** 已登入 admin，有一個只能存取部分 Account 的 Group
- **When** 建立新 User 並指派至該 Group，以新 User 登入
- **Then** 憑證列表只顯示 Group 授權範圍內的憑證

### IT-UA-01-02 User 未指派任何 Group → 憑證列表為空
- **Given** 已建立一個未指派 Group 的 User
- **When** 以該 User 登入並進入憑證列表
- **Then** 憑證列表應為空或只顯示系統預設可見範圍

### IT-UA-01-03 User 指派多個 Group → 可見範圍為多個 Group 的聯集
- **Given** User 同時加入 Group-A（Account 1）與 Group-B（Account 2）
- **When** 以該 User 登入並進入帳號列表
- **Then** Account 1 和 Account 2 均應可見

---

### IT-UA-02-01 admin 對線上 User 執行強登出 → 操作成功
- **Given** 已登入 admin，目標 User 有現存 session
- **When** 在 User 管理對該 User 點擊強登出並確認
- **Then** 操作成功，UI 顯示確認訊息

### IT-UA-02-02 被強登出的 User 現有操作被導向登入頁
- **Given** 目標 User 剛被 admin 強登出
- **When** 目標 User 在同一 session 繼續操作（如點擊連結）
- **Then** 應跳轉至 /login，session 已失效

### IT-UA-02-03 被強登出後以原帳密重新登入可成功
- **Given** 目標 User 被強登出
- **When** 以原帳密重新登入
- **Then** 登入成功，session 重新建立

---

## 掃描 / 合規 / 稽核

### IT-AU-NEG-01-01 user 角色存取 /audit → 被導向
- **Given** 已以 user 角色登入
- **When** 直接瀏覽 /audit
- **Then** 應被導向無權限頁或跳回首頁，不顯示稽核紀錄

### IT-AU-NEG-01-02 supervisor 角色存取 /audit → 被導向（稽核為 admin 限定）
- **Given** 已以 supervisor 角色登入
- **When** 直接瀏覽 /audit
- **Then** 應被導向無權限頁，稽核紀錄不對 supervisor 開放

### IT-AU-NEG-01-03 admin 角色可正常查看 /audit
- **Given** 已以 admin 角色登入
- **When** 進入 /audit
- **Then** 正常顯示稽核紀錄列表

---

### IT-POL-NEG-01-01 設定 RSA-2048 強制政策 → 政策清單出現
- **Given** 已登入 admin
- **When** 新增合規政策，設定強制最低 RSA-2048
- **Then** 政策清單出現剛設定的政策，狀態為啟用

### IT-POL-NEG-01-02 申請 RSA-1024 憑證 → 系統阻擋（政策生效）
- **Given** RSA-2048 強制政策已啟用
- **When** 嘗試申請 RSA-1024 憑證
- **Then** 系統應阻擋申請並顯示違規原因（不合規政策）

### IT-POL-NEG-01-03 申請 RSA-2048 憑證 → 不被阻擋可正常送出
- **Given** RSA-2048 強制政策已啟用
- **When** 嘗試申請 RSA-2048 憑證
- **Then** 申請不被阻擋，Order 正常產生

---

### IT-SC-UNMANAGED-01-01 對有 unmanaged 憑證的 Endpoint 執行掃描 → 結果顯示
- **Given** 已登入 admin，Endpoint 主機有系統外憑證
- **When** 觸發憑證掃描
- **Then** 掃描結果頁出現 unmanaged 狀態的憑證項目

### IT-SC-UNMANAGED-01-02 點擊「納管」→ 憑證進入系統憑證列表
- **Given** 掃描結果有 unmanaged 憑證
- **When** 點擊該憑證的「納管」操作
- **Then** 憑證進入系統，於憑證列表以 ISSUED 狀態顯示

### IT-SC-UNMANAGED-01-03 納管後掃描結果不再顯示該憑證為 unmanaged
- **Given** 憑證已完成納管
- **When** 再次觸發掃描
- **Then** 該憑證不再出現在 unmanaged 列表

---

### IT-SC-01-01 設定合規政策 → 觸發掃描 → 不合規憑證被標注
- **Given** 已設定合規政策，Endpoint 有不符政策的憑證
- **When** 觸發 Endpoint 掃描
- **Then** 掃描結果中不合規憑證有違規標注

### IT-SC-01-02 掃描完成後憑證列表顯示不合規標記
- **Given** 掃描已完成且偵測到不合規憑證
- **When** 進入憑證列表
- **Then** 不合規憑證應有政策違規標記或特殊狀態顯示

### IT-SC-01-03 修正不合規憑證後重新掃描 → 標記消除
- **Given** 不合規憑證已被替換為合規版本
- **When** 再次觸發掃描
- **Then** 該憑證不再有違規標記

---

### IT-AU-01-01 執行寫入操作後稽核紀錄即時出現
- **Given** 已登入 admin
- **When** 執行任意寫入操作（如編輯 User 顯示名稱）並進入 /audit
- **Then** 對應操作的稽核紀錄應立即出現，不需手動重整

### IT-AU-01-02 稽核紀錄含 before/after diff 且內容正確
- **Given** 有一筆剛產生的稽核紀錄
- **When** 展開該稽核紀錄的詳細內容
- **Then** 應顯示 before 與 after 的欄位差異，且 after 值與實際操作結果一致

### IT-AU-01-03 多筆連續操作後稽核紀錄序列完整且時序正確
- **Given** 已登入 admin
- **When** 依序執行三筆不同操作（如新增 User → 編輯 Group → 修改設定）
- **Then** 稽核紀錄應有三筆，時序與操作順序一致

---

## 職責分離（SoD）

### IT-SOD-01-01 簽發請求提交者嘗試自我核准 → 系統阻擋
- **Given** 已以 admin 帳號登入，並提交一筆憑證簽發請求（CIR）
- **When** 進入核准頁，嘗試對自己提交的請求點擊「核准」
- **Then** 系統應拒絕並顯示 SoD 阻擋訊息，操作不成功；稽核紀錄記錄此次拒絕

### IT-SOD-01-02 SoD 阻擋訊息應包含拒絕原因說明
- **Given** 已提交 CIR 的 admin 嘗試自我核准
- **When** 系統拒絕後顯示錯誤訊息
- **Then** 錯誤訊息應明確說明「同一使用者不可提交與核准同一請求」，非通用錯誤

### IT-SOD-01-03 SoD 阻擋事件記錄於稽核紀錄
- **Given** admin A 嘗試自我核准並被系統阻擋
- **When** 進入稽核紀錄查詢
- **Then** 應有一筆記錄顯示：操作者=admin A、動作=自我核准嘗試、結果=拒絕

### IT-SOD-02-01 Binding 核准與部署作業核准須由不同帳號執行
- **Given** 帳號 A 提交 binding 請求，帳號 B 核准 binding
- **When** 帳號 B 嘗試同時核准後續部署作業
- **Then** 系統應依 SoD 政策判斷是否阻擋（若阻擋則顯示原因，若允許則記錄例外）

### IT-SOD-02-02 部署核准需由非部署申請者執行（雙閘驗證）
- **Given** 帳號 A 申請部署，帳號 B 為可核准者
- **When** 帳號 B 核准部署
- **Then** 部署作業執行，稽核紀錄記錄：申請者=A、核准者=B

### IT-SOD-02-03 同一帳號無法同時提交 binding 與核准部署
- **Given** 只有單一 admin 帳號
- **When** 帳號嘗試自己完成提交 binding + 核准部署的完整流程
- **Then** 至少一個步驟應被 SoD 規則阻擋

### IT-SOD-03-01 申請者無法核准自己的下載請求
- **Given** 帳號 A 申請憑證下載
- **When** 帳號 A 嘗試自我核准下載請求
- **Then** 系統應拒絕，核准須由其他授權帳號執行

### IT-SOD-03-02 下載連結為一次性且使用後失效
- **Given** 帳號 B 核准帳號 A 的下載請求，系統產生一次性下載連結
- **When** 帳號 A 使用該連結下載後，再次嘗試使用同一連結
- **Then** 第二次請求應回傳 403 或 410，連結不可重複使用

### IT-SOD-03-03 下載連結 15 分鐘 TTL 到期後 URL 無效
- **Given** 已產生一次性下載連結但尚未使用
- **When** 等待 15 分鐘後嘗試存取該連結
- **Then** 系統應回傳 403 或 410，連結已過期

---

## 跨群組隔離（IDOR）

### IT-IDOR-01-01 User 只能看到自己群組內指派給自己的憑證
- **Given** 已以 Group A 的 user 角色登入
- **When** 進入憑證列表
- **Then** 只顯示 assignee==self 且在 Group A 內的憑證，不顯示 Group B 憑證

### IT-IDOR-01-02 User 帶他群組憑證 ID 直接存取 → 404
- **Given** 已以 Group A 的 user 角色登入
- **When** 直接輸入 Group B 憑證的 order-id 嘗試存取
- **Then** 系統回傳 404（不回傳 403，不洩漏資源存在性）

### IT-IDOR-01-03 憑證列表不顯示他群組 EXPIRED 或 REVOKED 憑證
- **Given** Group B 有 EXPIRED 憑證，Group A user 已登入
- **When** 進入憑證列表
- **Then** Group B 的 EXPIRED 憑證完全不顯示（非隱藏，是完全不存在）

### IT-IDOR-02-01 Group A supervisor 的待審清單只顯示 Group A 請求
- **Given** 已以 Group A supervisor 登入
- **When** 進入待審（核准）清單
- **Then** 只顯示 Group A 的待審請求，Group B 的請求完全不出現

### IT-IDOR-02-02 Group A supervisor 帶 Group B 請求 ID 執行核准 → 被拒
- **Given** 已以 Group A supervisor 登入，取得 Group B 的待審請求 ID
- **When** 嘗試對 Group B 請求 ID 執行核准 API 呼叫
- **Then** 系統回傳 403 或 404，核准操作不成功

### IT-IDOR-02-03 Group A supervisor 帶 Group B 請求 ID 執行撤銷核准 → 被拒
- **Given** 已以 Group A supervisor 登入，取得 Group B 的已核准請求 ID
- **When** 嘗試對 Group B 請求 ID 撤銷核准
- **Then** 系統回傳 403 或 404，撤銷操作不成功

### IT-IDOR-03-01 Group A supervisor 無法存取 Group B 的 discovery PEM
- **Given** 已以 Group A supervisor 登入，Group B 有 discovery PEM
- **When** 嘗試存取 Group B 的 discovery PEM 路徑或 API
- **Then** 系統拒絕（403 或 404），PEM 內容無法取得

### IT-IDOR-03-02 憑證 Excel 匯出只含自己群組範圍的憑證
- **Given** 已以 Group A supervisor 登入
- **When** 執行憑證匯出至 Excel
- **Then** 下載的 Excel 只含 Group A 的憑證，不含 Group B 資料

### IT-IDOR-03-03 匯入 Group B 的 discovery PEM → 系統拒絕
- **Given** 已以 Group A supervisor 登入，有 Group B 的 discovery PEM 檔案
- **When** 嘗試匯入 Group B 的 PEM 至 Group A
- **Then** 系統拒絕（範圍不符），匯入失敗

### IT-IDOR-04-01 supervisor 存取稽核 API 只回傳自己群組資料
- **Given** 已以 Group A supervisor 登入
- **When** 呼叫稽核查詢 API
- **Then** 只回傳 Group A 的操作紀錄，不含 Group B 資料

### IT-IDOR-04-02 user 嘗試存取 /admin/settings → 403
- **Given** 已以 user 角色登入
- **When** 嘗試 GET /admin/settings
- **Then** 回傳 403，設定內容不回傳

### IT-IDOR-04-03 user 嘗試 POST /mail/test → 403
- **Given** 已以 user 角色登入
- **When** 嘗試 POST /mail/test
- **Then** 回傳 403，操作不執行

### IT-IDOR-05-01 supervisor 使用者管理只列出自己群組成員
- **Given** 已以 Group A supervisor 登入
- **When** 進入使用者管理頁
- **Then** 只顯示 Group A 的成員，不顯示 Group B 成員

### IT-IDOR-05-02 跨群組 user 看到聯集資料但不超過所屬群組範圍
- **Given** 某 user 同時屬於 Group A 和 Group B
- **When** 進入憑證列表
- **Then** 顯示 A+B 的憑集（屬於 self 的），但不顯示 Group C 或其他群組資料

### IT-IDOR-06-01 Group A user 存取 Group B 部署作業 → 被拒
- **Given** 已以 Group A user 登入，取得 Group B 的部署 jobId
- **When** 嘗試存取 Group B 部署作業（帶 jobId）
- **Then** 系統拒絕（403 或 404）

### IT-IDOR-06-02 user 角色呼叫僅 admin 端點 → 403（垂直權限提升防護）
- **Given** 已以 user 角色登入
- **When** 嘗試呼叫僅限 admin 的 API 端點
- **Then** 回傳 403，操作不執行，稽核紀錄記錄此次嘗試

### IT-IDOR-07-01 Group A admin 憑證列表、user 清單均只含 Group A 資料
- **Given** 已以 Group A admin 登入
- **When** 依序查看憑證列表與使用者列表
- **Then** 兩個清單均只顯示 Group A 資料，Group B 資料完全不出現

### IT-IDOR-07-02 Group A admin 帶 Group B 資源 ID 呼叫 API → 403/404
- **Given** 已以 Group A admin 登入，持有 Group B 的資源 ID
- **When** 帶 Group B 資源 ID 呼叫各類 API（憑證、user、群組）
- **Then** 全部回傳 403 或 404，無一成功

### IT-IDOR-07-03 Group A admin 稽核紀錄只含 Group A 操作
- **Given** 已以 Group A admin 登入
- **When** 查看稽核紀錄
- **Then** 只顯示 Group A 的操作紀錄，Group B 操作完全不出現

---

## MFA 認證

### IT-MFA-01-01 MFA 註冊流程完成且 TOTP 驗證成功
- **Given** 帳號尚未啟用 MFA
- **When** 進入 MFA 設定頁，掃描 QR Code 完成驗證器綁定，登出後重新登入輸入正確 TOTP
- **Then** 登入成功，session 建立，MFA 狀態顯示為已啟用

### IT-MFA-01-02 輸入錯誤 TOTP → 登入被拒
- **Given** 帳號已啟用 MFA，進入 TOTP 輸入步驟
- **When** 輸入錯誤的 TOTP 碼
- **Then** 系統拒絕登入，顯示「驗證碼不正確」，不建立 session

### IT-MFA-01-03 MFA 密鑰持久化且重新登入後仍有效
- **Given** 帳號已完成 MFA 設定，密鑰已儲存
- **When** 24 小時後再次登入輸入當下 TOTP
- **Then** TOTP 仍有效，驗證通過，session 建立

### IT-MFA-02-01 admin 強制 MFA 後非 MFA 帳號登入被阻擋
- **Given** admin 啟用強制 MFA 政策
- **When** 未設定 MFA 的帳號嘗試登入
- **Then** 系統要求先完成 MFA 設定才能繼續，或直接拒絕登入

### IT-MFA-02-02 OIDC SSO 繞過 MFA 行為明確記錄（安全降級風險）
- **Given** 已設定 OIDC SSO，帳號已啟用 MFA
- **When** 透過 OIDC SSO 流程完成登入
- **Then** 確認 MFA 是否被繞過，並在稽核紀錄中明確標記此為 SSO 登入（含 MFA 狀態）

---

## 合規進階

### IT-POL-02-01 WARN 嚴重度政策：違規請求通過但記錄警告
- **Given** 已設定一條 WARN 嚴重度政策
- **When** 提交違反 WARN 政策的憑證申請
- **Then** 申請通過，但合規警告記錄在稽核紀錄中

### IT-POL-02-02 AUDIT_ONLY 嚴重度政策：違規請求通過且稽核
- **Given** 已設定一條 AUDIT_ONLY 嚴重度政策
- **When** 提交違反 AUDIT_ONLY 政策的憑證申請
- **Then** 申請通過，稽核紀錄包含合規稽核標記

### IT-POL-02-03 BLOCK 嚴重度政策：違規請求被拒並顯示原因
- **Given** 已設定一條 BLOCK 嚴重度政策
- **When** 提交違反 BLOCK 政策的憑證申請
- **Then** 申請被拒，顯示具體政策違規原因訊息

### IT-POL-03-01 private-key.export.forbidden=true 時 UI 下載私鑰被阻擋
- **Given** 已設定 private-key.export.forbidden=true，有 ISSUED 憑證
- **When** 嘗試透過 UI 下載私鑰
- **Then** 系統拒絕，下載按鈕不可用或回傳錯誤

### IT-POL-03-02 private-key.export.forbidden=true 時 API 取得私鑰被阻擋
- **Given** 已設定 private-key.export.forbidden=true，有 ISSUED 憑證
- **When** 嘗試透過 API 取得私鑰
- **Then** API 回傳 403，私鑰內容不回傳

### IT-POL-03-03 private-key.export.forbidden=true 時含私鑰 PFX 匯出被阻擋
- **Given** 已設定 private-key.export.forbidden=true，有 ISSUED 憑證
- **When** 嘗試匯出含私鑰的 PFX 格式
- **Then** 系統拒絕 PFX 匯出，提示私鑰匯出已禁用

### IT-POL-04-01 BLOCK 政策在核准時拒絕請求並顯示違規原因給請求者
- **Given** 已設定 BLOCK 政策，提交違反政策的簽發請求
- **When** 核准流程觸發政策檢查
- **Then** 核准被拒，請求者可看到具體違規原因說明

### IT-POL-04-02 RSA-1024 憑證簽發後被標記且自動部署被抑制
- **Given** 提交 RSA-1024 憑證（繞過前端驗證），CA 完成簽發
- **When** 憑證狀態為 ISSUED
- **Then** 合規系統標記此憑證違規，自動部署被抑制；憑證狀態仍顯示 ISSUED

### IT-POL-05-01 匯入不合規 PEM 不觸發合規引擎（完全繞過）
- **Given** 有一張違反合規政策的 PEM 憑證（如 RSA-1024）
- **When** 透過匯入功能上傳此 PEM
- **Then** 匯入成功，不觸發合規政策拒絕；記錄此行為是否標記或完全無警示

---

## 稽核鏈進階

### IT-AU-02-01 撤銷流程產生三筆鏈結稽核紀錄（申請、核准、CA完成）
- **Given** 有 ISSUED 憑證，提交撤銷申請
- **When** 核准通過並 CA 完成撤銷
- **Then** 稽核紀錄含三筆：撤銷申請、核准、CA 撤銷完成，且三筆可互相溯源

### IT-AU-02-02 每筆稽核紀錄含前一步驟的參考 ID
- **Given** 撤銷流程已完成，稽核鏈已建立
- **When** 查看各筆稽核紀錄詳細內容
- **Then** 每筆含前一步驟的參考 ID 或流程串聯識別欄位

### IT-AU-02-03 稽核鏈可從第一筆追溯至最後一筆
- **Given** 完整撤銷流程的稽核鏈已建立
- **When** 從撤銷申請那筆開始，依參考 ID 追溯
- **Then** 可完整追溯至 CA 撤銷完成那筆，鏈不斷裂

### IT-AU-03-01 偵測稽核紀錄被篡改（鏈中途斷裂警示）
- **Given** 系統有完整的稽核鏈，模擬篡改其中一筆紀錄
- **When** 系統執行稽核驗證
- **Then** 系統應偵測到鏈中途斷裂並發出警示，指出異常紀錄位置

### IT-AU-03-02 刪除中間稽核紀錄後鏈斷裂被偵測
- **Given** 系統有連續三筆稽核紀錄（A→B→C）
- **When** 刪除中間紀錄 B
- **Then** 系統應偵測到 A 到 C 之間的斷裂，觸發警示

### IT-AU-04-01 user 角色存取 /audit → 403
- **Given** 已以 user 角色登入
- **When** 嘗試存取 /audit 頁面或 API
- **Then** 回傳 403，稽核內容不顯示

### IT-AU-04-02 supervisor 角色存取 /audit → 403
- **Given** 已以 supervisor 角色登入
- **When** 嘗試存取 /audit 頁面或 API
- **Then** 回傳 403，稽核內容不顯示

### IT-AU-04-03 admin 角色可存取 /audit 且可執行匯出
- **Given** 已以 admin 角色登入
- **When** 存取 /audit 頁面，並執行稽核匯出
- **Then** 頁面正常顯示，匯出檔案成功下載

---

## 批次匯入

### IT-BATCH-01-01 有效列成功匯入，不受其他失敗列影響
- **Given** 上傳含有效列與無效格式列的 Excel
- **When** 批次匯入完成
- **Then** 有效列成功匯入，無效列回傳對應錯誤，兩者互相獨立不影響

### IT-BATCH-01-02 重複 CN 列回傳重複錯誤
- **Given** 上傳含重複 CN 的 Excel（兩列 CN 相同）
- **When** 批次匯入處理重複 CN 列
- **Then** 重複列回傳「已存在」或「重複 CN」錯誤，其餘有效列不受影響

### IT-BATCH-01-03 超出群組範圍的列回傳範圍錯誤
- **Given** 上傳含超出群組授權範圍的憑證列
- **When** 批次匯入處理此列
- **Then** 回傳範圍不符錯誤，該列匯入失敗，批次結果頁顯示各列狀態

### IT-BATCH-02-01 超過列數上限的 Excel 被拒並顯示上限說明
- **Given** 準備超過系統允許列數的 Excel
- **When** 上傳至批次匯入
- **Then** 系統拒絕，顯示列數上限說明，不執行任何匯入

### IT-BATCH-02-02 超過大小限制的工作簿被拒
- **Given** 準備超過檔案大小限制的 Excel
- **When** 上傳至批次匯入
- **Then** 系統拒絕，顯示檔案大小限制說明

---

## 續約排程

### IT-RENEW-01-01 手動觸發續約後新 Order 進入核准佇列
- **Given** 有 ISSUED 憑證，已設定核准流程
- **When** 手動觸發續約
- **Then** 新 Order 進入核准佇列，狀態為待審

### IT-RENEW-01-02 對同一憑證再次觸發續約 → 去重，不產生重複 Order
- **Given** 已對某憑證觸發一次續約（Order 進入佇列）
- **When** 再次觸發同一憑證的續約
- **Then** 系統不產生第二筆 Order（去重機制），或明確顯示「已有進行中的續約」

### IT-RENEW-01-03 續約後受指派人資訊保留至新 Order
- **Given** 憑證有受指派人，觸發續約
- **When** 新 Order 建立
- **Then** 新 Order 的受指派人與原憑證相同

### IT-RENEW-02-01 Cron 後備機制處理無政策的 CN 並記錄
- **Given** 有無政策的 CN 憑證，Cron 觸發
- **When** Cron 處理無政策 CN
- **Then** 後備機制啟動並在日誌或稽核中記錄處理行為

### IT-RENEW-02-02 靜默時段內不產生新續約 Order
- **Given** 已設定靜默時段（如每日 02:00-04:00）
- **When** 在靜默時段內到期或 Cron 觸發
- **Then** 不產生新續約 Order，等待靜默時段結束

### IT-RENEW-03-01 憑證到期後新訂單被拒，既有 ISSUED 憑證狀態不變
- **Given** 憑證已到期
- **When** 嘗試提交新簽發 Order
- **Then** 被拒（到期後不可新訂），既有 ISSUED 憑證狀態仍為 ISSUED

### IT-RENEW-03-02 到期警示每門檻僅發送一次（去重）
- **Given** 憑證接近到期，每日重新掃描
- **When** 連續多天掃描觸發同一門檻（如 30 天警示）
- **Then** 30 天警示只發送一次，不重複發送

---

## 韌性 / 故障轉移

### IT-RES-01-01 CA 回傳 authz INVALID → Order 標為 FAILED
- **Given** CA Provider 回傳 authz INVALID
- **When** Order 處理此回應
- **Then** Order 狀態標為 FAILED，含 CA 回傳的失敗原因

### IT-RES-01-02 FAILED Order 含診斷訊息且 DNS 挑戰記錄已清理
- **Given** Order 已因 authz INVALID 標為 FAILED
- **When** 查看 Order 詳細頁與 DNS 記錄
- **Then** 詳細頁顯示診斷訊息；DNS 挑戰記錄已清理，不殘留

### IT-RES-02-01 CA 撤銷失敗時憑證狀態維持 ISSUED（fail-closed）
- **Given** 有 ISSUED 憑證，核准撤銷後 CA 撤銷 API 失敗
- **When** 系統處理 CA 失敗
- **Then** 憑證狀態維持 ISSUED，不標為假 REVOKED，稽核紀錄記錄失敗原因

### IT-RES-02-02 CA 撤銷成功後憑證狀態標為 REVOKED
- **Given** 有 ISSUED 憑證，核准撤銷後 CA 撤銷 API 成功
- **When** CA 確認撤銷完成
- **Then** 憑證狀態標為 REVOKED，稽核紀錄記錄完成

### IT-RES-03-01 應用程式重啟後 AcmeOrderRecoveryRunner 恢復進行中 Order
- **Given** 有 Order 處於 PROCESSING 狀態時模擬應用程式重啟
- **When** 應用程式重啟完成，AcmeOrderRecoveryRunner 啟動
- **Then** 進行中 Order 被接管並繼續完成，或被標為適當失敗狀態（不卡在 PROCESSING）

---

## 併發 / 競態

### IT-CONC-01-01 並發核准同一 CN 的兩筆 CIR → 只產生一筆 ACME Order
- **Given** 同一 CN 有兩筆待審 CIR，模擬同時送出核准請求
- **When** 兩個核准請求幾乎同時到達
- **Then** 系統只建立一筆 ACME Order，另一筆被去重或標為 DUPLICATE

### IT-CONC-01-02 去重後被拒的核准請求應有明確狀態說明
- **Given** 並發核准中，第二筆被去重
- **When** 查看第二筆請求結果
- **Then** 顯示「已有進行中的同 CN Order」或「去重拒絕」說明

### IT-CONC-02-01 並行核准同一 jobId → 只派發一次
- **Given** 同一 deploy jobId 有兩個並行核准請求
- **When** 兩個核准請求幾乎同時到達
- **Then** 只執行一次派發，第二個核准回傳「已處理」或冪等結果

### IT-CONC-02-02 一次性下載連結在並發下嚴格只允許單次使用
- **Given** 已核准的一次性下載連結，兩個請求幾乎同時使用
- **When** 兩個並發下載請求到達
- **Then** 只有一個成功，另一個回傳 403 或 410

### IT-CONC-03-01 CAS 原子操作防止雙重核准
- **Given** 兩個 session 同時嘗試核准同一筆請求
- **When** 兩個核准請求並發處理
- **Then** 只有一個成功核准，另一個回傳「已被處理」或類似錯誤

### IT-CONC-03-02 CAS 原子操作防止雙重私鑰提取
- **Given** 兩個 session 同時嘗試提取同一憑證的私鑰
- **When** 兩個提取請求並發處理
- **Then** 只有一個成功，另一個回傳錯誤（不可重複提取）

---

## 安全

### IT-SEC-01-01 同 IP 超過速率限制回傳 429 + Retry-After 標頭
- **Given** 從同一 IP 連續送出超過允許次數的登入請求
- **When** 超過速率限制門檻
- **Then** 回傳 HTTP 429，回應含 Retry-After 標頭，說明何時可重試

### IT-SEC-01-02 Retry-After 時間過後可重新嘗試登入
- **Given** 已觸發速率限制，等待 Retry-After 時間
- **When** 時間到後重新嘗試登入（正確帳密）
- **Then** 登入成功，速率限制已重置

### IT-SEC-02-01 OIDC 帶未驗證 email 的 token → 被拒
- **Given** OIDC Provider 發出含未驗證 email 的 token
- **When** 使用此 token 嘗試登入
- **Then** 系統拒絕，顯示 email 未驗證錯誤

### IT-SEC-02-02 已停用的本機帳號嘗試登入 → 被拒
- **Given** 管理員已停用某本機帳號
- **When** 該帳號嘗試使用密碼登入
- **Then** 系統拒絕，顯示帳號已停用訊息

### IT-SEC-03-01 cert-inspect 端點對低權限角色隱藏敏感欄位
- **Given** 已以低權限角色登入
- **When** 呼叫 cert-inspect 端點
- **Then** 回應不含私鑰、CA 密鑰等敏感欄位（欄位不回傳或遮蔽）

### IT-SEC-04-01 竄改 localStorage role 為 admin 後無法存取 admin 功能
- **Given** 以 user 角色登入，開啟 DevTools 修改 localStorage 的 role 欄位為 admin
- **When** 重整頁面後嘗試存取 admin 功能
- **Then** 無法存取 admin 功能，API 呼叫仍以 JWT 中的 user 角色為準

---

## 路由 / UI

### IT-UI-01-01 深層路由 hard-refresh 後頁面正確渲染
- **Given** 已登入
- **When** 直接輸入深層路由（如 /certs/123）並 hard-refresh
- **Then** 頁面正確渲染，不跳轉至 404 或空白

### IT-UI-01-02 不存在路由顯示統一 404 頁面
- **Given** 已登入
- **When** 輸入不存在的路由（如 /nonexistent-path）
- **Then** 顯示統一的 404 錯誤頁面，非空白頁或崩潰

### IT-UI-02-01 切換語系後頁面標題與選單文字更新
- **Given** 已登入，語系為中文
- **When** 切換語系至英文
- **Then** 頁面標題與選單文字更新為英文，不殘留中文

---

## 通知

### IT-NOTIFY-01-01 SMTP 暫時失敗後退避重試，最終 SENT
- **Given** SMTP 伺服器暫時不可達，已觸發通知（如到期警示）
- **When** 系統進入退避重試，SMTP 恢復正常
- **Then** 通知最終標記為 SENT 狀態，記錄每次重試嘗試

### IT-NOTIFY-01-02 SENT 狀態後同一通知不再重複發送
- **Given** 通知已成功發送（狀態 SENT）
- **When** 系統重新掃描或 Cron 再次觸發
- **Then** 不重複發送同一通知，SENT 狀態作為去重依據

---

## ACME 核心 / 憑證生命週期（進階）

### IT-LC-ADV-01 INT-001 端到端生命週期
- **Given** 群組 G 中一個已註冊的 ACME 帳號,以及一個可受控的測試網域 test.example.com
- **When** 申請者 POST /api/v1/letsencrypt/accounts/{acc}/orders {commonName:test.example.com}(預期回傳 202 pendingApproval),發布 DNS TXT,核准者 POST /admin/cert-issuance-requests/{cir}/approve,接著輪詢 GET /orders/{id} 直到 ISSUED,然後 GET /orders/{id}/certificate
- **Then** CIR 由 PENDING→APPROVED 轉換並帶有 createdOrderId;OrderEntity 由 PENDING_DNS→VERIFYING→PROCESSING→ISSUED 轉換;憑證 PEM 可下載並解析為有效的憑證鏈,其末端憑證(leaf)的 SAN==test.example.com;cert_fingerprint_sha256 自動填入;發出等同 ORDER_ISSUED 的稽核紀錄,並寄出 cert.issued 郵件

### IT-LC-ADV-02 INT-002 負向/邊界
- **Given** 一組全新且有效的 TWCA EAB Key ID + HMAC
- **When** POST /api/v1/twca/accounts 帶著該 EAB 在 CA 端成功(帳號已建立),但本地儲存失敗(模擬儲存例外/崩潰);接著以相同的 EAB 重試註冊
- **Then** 重試必須在 CA 端因 EAB-already-used 而失敗,且系統必須呈現一個清楚、可採取行動的錯誤(而非洩漏 exceptionClass 的通用 500);絕不能存在可用於簽發的孤兒半建立帳號;最好有一條文件化的復原路徑。測試斷言目前的行為,以標示出缺少的補償/回滾機制。

### IT-LC-ADV-03 INT-003 併發/競態
- **Given** 兩筆待處理的簽發申請 cir_A 與 cir_B,皆針對同一帳號上的 cn=race.example.com,或兩條執行緒同時核准 cir_A
- **When** 兩個核准在同一毫秒範圍內被觸發(並行 HTTP)
- **Then** 恰好一個核准產生一筆已建立的 ACME 訂單;另一個回傳 DuplicateRequestException(409) 或 IllegalState 'already processed'(CAS markApprovedIfPending 必須序列化);CA 絕不能針對同一 CN 收到兩次 newOrder/execute 呼叫;不得有兩筆 OrderEntity 同時進入 PENDING_DNS/VERIFYING

### IT-LC-ADV-04 INT-004 安全
- **Given** 一筆由群組 A 帳號所擁有、有效且為 ISSUED 的訂單 ord_A
- **When** userB(群組 B 的 supervisor)與 userC(群組 B 的 user)直接替換 accA 與 ord_A 的 id,呼叫 GET /api/v1/{provider}/accounts/{accA}/orders/{ord_A}、/certificate、/parsed、DELETE、/revoke,以及 PATCH /assignee
- **Then** 對於跨群組,每個回應都是 404(不是 403,也不是 200);同群組但非受指派人的 'user' 在訂單層級讀取時也得到 404(受指派人過濾);不洩漏任何憑證 PEM、網域身分或存在性;稽核紀錄記下被拒絕的嘗試

### IT-LC-ADV-05 INT-013 負向/邊界
- **Given** 一張 ISSUED 憑證,以及一名「不是」申請者的 admin
- **When** POST /orders/{id}/revoke(預期回傳 202 PENDING_APPROVAL),核准者核准,但 CA 撤銷呼叫失敗
- **Then** 本地狀態維持 ISSUED(絕不變為 REVOKED),error_message 記錄該 CA 失敗,寫入一筆 audit.failure ORDER_REVOKED 並帶 caRevoked=false 且轉發至 SIEM,且操作者收到一個明確的失敗(而非成功);相對地,一次成功的 CA 撤銷則產生 REVOKED + ORDER_REVOKED 成功

### IT-LC-ADV-06 INT-005 韌性/故障轉移
- **Given** 一筆被觸發驗證的訂單,其發布的 TXT 是錯誤的/缺漏的,使 CA 回傳 Authorization INVALID
- **When** AcmeOrderPoller.doPoll 執行至完成
- **Then** 訂單狀態變為 FAILED,且 error_message 包含 CA 的 challenge 錯誤;markFailed 觸發 dnsAutomation.onOrderTerminated 清理、autoRenew.onOrderTerminated(false),以及一封寄給受指派人的 cert.failed 郵件;憑證未被簽發,也沒有任何佈署工作被排入佇列

### IT-LC-ADV-07 INT-006 災難復原/備份
- **Given** 上述各列在應用程式 context 啟動前即存在(AcmeOrderRecoveryRunner Order=200)
- **When** 應用程式啟動
- **Then** 兩筆 <6h 的進行中訂單被重新提交至 pollOrderAsync;那筆 >6h 的 VERIFYING 訂單被設為 FAILED 並帶 'stale in-flight' 訊息;PENDING_DNS 訂單保持原狀(依設計);復原以已恢復/已過期的計數被記錄

### IT-LC-ADV-08 INT-007 併發/競態
- **Given** 5 筆進行中訂單,以及近乎同時啟動的 2 個節點(滾動重啟)
- **When** 兩個節點都執行 AcmeOrderRecoveryRunner.run(),且兩者都對相同的 orderIds 觸發 pollOrderAsync
- **Then** CA 絕不能針對同一訂單收到重複的 order.execute(),且訂單絕不能因並行 finalize 而被毀損為 INVALID;測試斷言目前缺乏叢集協調的狀況,並量化重複 CSR 的風險(以驅動加入鎖)

### IT-LC-ADV-09 INT-008 韌性/故障轉移
- **Given** 對同一網域反覆核准/續約並超過 CA 速率限制
- **When** createOrder / 輪詢器 / fetchWithRetry 遇到 429(AcmeRateLimitedException)或 duplicate-certificate 限制
- **Then** 系統呈現一個明確、不重試的錯誤(不會猛打 CA),以 FAILED 並帶速率限制原因記錄它,且不會在緊密迴圈中自動重新提交;斷言 fetchWithRetry 固定的 2×2s 重試不會放大這次封鎖

### IT-LC-ADV-10 INT-009 效能/負載
- **Given** 80 筆被觸發至 VERIFYING 的訂單,搭配一個會把每次輪詢都按住整個 maxPollAttempts 窗口的 CA mock
- **When** 全部 80 個 pollOrderAsync 工作都被提交
- **Then** 超過 max+queue(>60)的工作被執行器拒絕;測試斷言被拒絕的訂單是否被默默留在 VERIFYING 而沒有 FAILED/重試(目前行為)——以標示出此缺口——並衡量吞吐量;一個健康的產品必須要嘛以 FAILED 拒絕、要嘛對核准速率施加背壓

### IT-LC-ADV-11 INT-010 授權強制
- **Given** 一筆數天前在授權仍生效時送出的待處理簽發申請
- **When** 授權已到期,接著一名 admin 核准這筆舊的 CIR
- **Then** approve() 必須重新檢查 license.canIssue() 並以授權訊息拒絕(FD-02)且不呼叫 CA;CIR 維持 PENDING/可用;同理,RenewalService.runOnce 與 certs/import 必須在授權到期下退出

### IT-LC-ADV-12 INT-011 授權強制
- **Given** 受管 FQDN 集合已接近上限
- **When** (a) 送出一筆新訂單,其 CN∪SAN 把相異 FQDN 數推過 N;(b) 批次送出數筆申請後再核准它們,使訂單在送出時不存在;(c) 匯入一張超過上限的 PEM
- **Then** (a) 與 (c) 在邊界處以配額訊息被拒絕;(b) approve() 中的第二道閘門(findCnAndSans 包含進行中)阻擋超過上限的核准;續約不計入上限

### IT-LC-ADV-13 INT-012 合規強制
- **Given** 一個生效中的 BLOCK 合規政策與一筆違反它的訂單申請
- **When** (a) 送出一筆違反簽發前規則的訂單;(b) 送出/核准一筆僅違反簽發後規則的訂單(例如該申請仍產生了的 key.size.min)
- **Then** (a) createOrder 拋出 PolicyViolationException → HTTP 400 policy_violation,沒有由 CIR 驅動的 CA 呼叫;(b) 憑證到達 ISSUED 但自動佈署被抑制(blockDeploy),觸發一筆 POST_ISSUANCE_POLICY_VIOLATION 稽核 + cert.failed 通知,且該憑證不會被自動撤銷

### IT-LC-ADV-14 INT-014 功能整合
- **Given** cn=renew.example.com 為 ISSUED、接近到期、受指派人為 userX
- **When** Admin POST /api/v1/certs/renew-now(以及另外排程器執行)
- **Then** 建立一筆 RENEWAL CIR(requestedBy='system' 標記,受指派人保留為 userX,而非 admin),它出現在核准佇列;若該 CN 已存在進行中訂單/PENDING CIR 則略過續約;核准時新訂單簽發並「取代」(SUPERSEDES)舊的;一筆被拒絕的自動續約 CIR 呼叫 AutoRenewScheduler.onRenewalRejected 以升級,使憑證不會默默到期

### IT-LC-ADV-15 INT-015 負向/邊界
- **Given** 橫跨兩個群組、處於不同狀態的訂單
- **When** 對 ISSUED 執行 DELETE /orders/{id}(取消)、對 ISSUED 執行 DELETE /certs/{id}(刪除匯入)、由一名跨群組 supervisor 對 IMPORTED 執行 DELETE /certs/{id}
- **Then** 取消 ISSUED → 409(請改用撤銷);對非 IMPORTED 的刪除匯入 → 409 wrong_status;對另一群組之 IMPORTED 的刪除匯入 → 404(群組守衛,不洩漏存在性);取消 PENDING_DNS → CANCELLED

### IT-LC-ADV-16 INT-016 相容性
- **Given** 同一 LE 帳號上一個先前已驗證過的網域
- **When** 在 CA 授權仍為 VALID/已快取時,為該網域核准一筆新訂單
- **Then** order.getAuthorizations() 回傳 VALID,challenges 清單為空,OrderRecord 的 initialStatus 為 VERIFYING(而非 PENDING_DNS),立即叫用輪詢器,且憑證在沒有任何 DNS TXT 步驟下簽發;TWCA(無此快取)仍要求 DNS challenge

---

## DNS-01 自動化進階

### IT-DNS-ADV-01 INT-017 端到端生命週期
- **Given** 一個 type=cloudflare、managedZones=['foo.com'] 的 DNS 帳號,以及一個 cn=app.foo.com、mode=AUTO_PLUGIN 指向它的綁定;一筆建立於 PENDING_DNS、帶一個 challenge(recordName=_acme-challenge.app.foo.com、value=<43 字元摘要>)的訂單
- **When** onOrderCreated 觸發非同步的 runAutoFlow:它透過 longestManagedZone 解析 zone、呼叫 Cloudflare POST /zones/{id}/dns_records、等待傳播(stub resolver 回傳相符),然後以反射呼叫 acmeOrderService.triggerValidation
- **Then** dnsPublishStatus 由 NONE->PUBLISHING->PUBLISHED 轉換;dnsPublishRecordsJson 包含 {fqdn,recordId,accountId,zoneOverride=foo.com};triggerValidation 被叫用;在 ISSUED 時 onOrderTerminated 執行清理 -> 以儲存的 recordId 呼叫 Cloudflare DELETE;狀態以 CLEANED 結束;稽核帶有 DNS_AUTO_PUBLISHED + DNS_AUTO_CLEANUP

### IT-DNS-ADV-02 INT-018 安全
- **Given** 群組 A 的 admin 建立了一個 DNS 帳號 type=route53、managedZones=['secret-internal.example.tw'];一個屬於群組 B、role=user 的低權限角色帶有有效的 JWT cookie
- **When** role=user 呼叫 GET /api/v1/dns-bindings/auto-detect?fqdn=anything.secret-internal.example.tw
- **Then** 對於可販售的產品的「預期」:對群組外帳號回傳 403/404 或空相符。「實際」(程式碼):該端點沒有 @PreAuthorize 也沒有 canAccessCn,因此它回傳 matched=true 並帶 dnsProviderAccountId、displayName、type 與 zone='secret-internal.example.tw',洩露了另一個租戶的 DNS 拓撲與內部 zone 名稱

### IT-DNS-ADV-03 INT-019 權限/多租戶隔離
- **Given** cn=app.foo.com 已綁定 mode=AUTO_PLUGIN(帳號在群組 A)。群組 A 的 Supervisor S_A、群組 B 的 Supervisor S_B(對此 CN 無訂單)、Admin
- **When** (a) S_A PUT 一個變更到 ACME_DNS -> 建立一筆 PENDING 申請(未套用);(b) S_A POST 核准自己的申請;(c) S_B POST 核准;(d) Admin POST 核准
- **Then** (a) 回應 kind=PENDING_APPROVAL,綁定不變;(b) 409 SoD 'requester cannot approve own request'(非 admin);(c) 404 not found(跨群組,canAccessCn 為 false);(d) 成功,CAS 把 PENDING->APPROVED 恰好翻轉一次,綁定變為 ACME_DNS,寫入稽核 DNS_BINDING_CHANGE_APPROVED

### IT-DNS-ADV-04 INT-020 併發/競態
- **Given** 兩筆訂單(或一筆訂單中的兩個 SAN)解析到同一 route53 帳號上的同一 fqdn _acme-challenge.shared.foo.com,兩者近乎同時發布不同的 TXT 值
- **When** runAutoFlow 處理兩個發布;Route53 發布在 synchronized(rmwLock(account,zone,record)) 下執行 GET-current-values -> append -> UPSERT
- **Then** stub 端的最終 RRset 包含「兩個」TXT 值(沒有遺失更新);兩個 recordId 都被儲存;清理其中一個值會使另一個保持完好(Route53 清理會重新讀取並 UPSERT 剩餘部分)

### IT-DNS-ADV-05 INT-021 併發/競態
- **Given** 兩個 CLM 節點共用一張 MSSQL ShedLock 表;若干訂單在 7 天窗口內被留在 CLEANUP_PENDING
- **When** 兩個節點同時碰上 0 15 * * * * 的 cron
- **Then** 恰好一個節點執行掃描(持有 ShedLock lockAtMostFor=PT15M);另一個略過。另外,記錄/斷言發布路徑的 256 分條鎖僅為每 JVM 範圍,因此來自「兩個」節點對同一 RRset 的並行發布「不會」被序列化(迴歸守衛,以驅動修正或一個分散式鎖)

### IT-DNS-ADV-06 INT-022 韌性/故障轉移
- **Given** 一個綁定 + 訂單,其中被 stub 的 DnsPrechecker 從不回報 allMatch(模擬 NS slave 延遲 / DNSSEC 重簽延遲)
- **When** runAutoFlow 成功發布後,迴圈 30x5s 都未收斂
- **Then** 約 2.5 分鐘後它記錄 'propagation poll did not converge, triggering CA anyway' 並仍呼叫 triggerValidation;斷言訂單「不會」被默默卡住,且後續的 CA 端失敗會把訂單標記為 FAILED 並觸發清理(CLEANUP_PENDING->CLEANED)。驅動一個產品決策:管線是否應改為快速失敗(fail-fast)而非燒掉 CA rate-limit?

### IT-DNS-ADV-07 INT-023 韌性/故障轉移
- **Given** 兩筆處於 CLEANUP_PENDING 的訂單:訂單 A 的 createdAt 為 2 天前、訂單 B 的 createdAt 為 30 天前;provider cleanup stub 對 A 回傳成功、對 B 回傳失敗
- **When** retryStuckCleanups 在 clm.dns.cleanup-retry.max-age-days=7 的設定下執行
- **Then** A 被重新推進至 CLEANED;B 被歸類為 tooOld、不被重試,並在一筆列出其 orderId 的 warn 日誌中發出;斷言 B 的 TXT 仍存在於 stub zone 中(屬於需要手動清理的已記載殘留)

### IT-DNS-ADV-08 INT-024 功能整合
- **Given** 一個 type=acme-dns 且已設定 baseUrl 的 DNS 帳戶;一個新綁定 cn=app.foo.com mode=ACME_DNS
- **When** submit 建立該綁定 -> register stub 回傳 {username,password,subdomain,fulldomain};password 以 AES-GCM 儲存;稍後 runAutoFlow 注入 K_USERNAME/K_PASSWORD/K_SUBDOMAIN 並 POST /update;同時發布一筆刻意做成畸形的 20 字元 TXT
- **Then** 綁定持久化 acmeDnsFulldomain(供客戶 CNAME 顯示)與一個加密後的 password(絕不在 viewMap 中回傳);/update 收到 X-Api-User/X-Api-Key + 43 字元 txt 並成功;20 字元的值回傳 PublishResult.fail('43 char') 且該訂單轉為 PUBLISH_FAILED;cleanup 是經驗證的 no-op

### IT-DNS-ADV-09 INT-025 負向/邊界
- **Given** 一個參照 dnsProviderAccountId=X 的綁定 cn=app.foo.com mode=AUTO_PLUGIN;admin 接著 DELETE 帳戶 X
- **When** 一筆 app.foo.com 的自動續約訂單觸發 onOrderCreated -> runAutoFlow -> accountService.getOrThrow(X)
- **Then** 對一個穩定的產品而言的 EXPECTED:當有綁定參照 X 時,刪除應被阻擋或發出警告。ACTUAL(程式碼):刪除無條件成功;續約拋出 'dns account not found: X',被捕捉為 auto-DNS 初始化失敗,且該訂單被標記為 PUBLISH_FAILED;斷言這會以一個清楚的錯誤呈現,並推動一項參照完整性修正

### IT-DNS-ADV-10 INT-026 授權強制
- **Given** 一張 quotaDnsTypes=3 的 Standard license;Group-A 已有 cloudflare、route53、azure 種類的帳戶(3 種不同種類)
- **When** Group-B 的 admin POST /api/v1/admin/dns-providers,type=gcp(第 4 種不同種類,不同群組)
- **Then** 請求被以 '已達授權 DNS 連接器種類上限 (3 種)' 拒絕,因為計數是針對全域所有帳戶的不同種類;斷言這個確切的第 4 種拒絕,並記載配額並不依 groupId 分割(產品決策:配額是全域還是各租戶?)

### IT-DNS-ADV-11 INT-027 韌性/故障轉移
- **Given** 一個既有的 cloudflare 帳戶,其 credentials_encrypted 是以金鑰 K1 寫入;執行中的執行個體以 cipher 金鑰 K2 啟動/輪替(或密文已損毀)
- **When** runAutoFlow 呼叫 accountService.decryptCreds(acc)
- **Then** decryptCreds 拋出 IllegalStateException('decrypt creds failed');runAutoFlow 的 catch 以該訊息將訂單標記為 PUBLISH_FAILED;斷言日誌/稽核中不出現任何明文機密,且 health-check 呈現相同的失敗,讓維運人員能在簽發中斷前偵測到此設定錯誤

### IT-DNS-ADV-12 INT-028 負向/邊界
- **Given** 一個 managedZones=[](空)的 DNS 帳戶與綁定 cn=api.foo.com.tw mode=AUTO_PLUGIN
- **When** runAutoFlow 計算出 zoneOverride=null(沒有受管 zone),於是 provider 後備到 guessZone,取最後兩個 label -> 'com.tw'
- **Then** Cloudflare findZoneId('com.tw') 回傳 null -> PublishResult.fail('zone not found') -> 訂單 PUBLISH_FAILED;斷言將 managedZones=['foo.com.tw'] 設定後,longestManagedZone 會回傳正確的 zone 且發布成功(對已記載的多層級 TLD bug 的回歸防護)

### IT-DNS-ADV-13 INT-029 端到端生命週期
- **Given** 一個綁定 cn=app.foo.com mode=DNS_PERSIST,persistTxtPublished=false 且使用 plugin 帳戶;第一筆訂單使用 dns-persist-01 挑戰簽發
- **When** 第一筆訂單到達 ISSUED(onOrderTerminated),接著建立一筆續約訂單
- **Then** 在第一次 ISSUED 且 orderUsedPersist=true 時,binding.persistTxtPublished 翻轉為 true 並寫入稽核 DNS_PERSIST_TXT_CONFIRMED;在續約時,onOrderCreated 看到 persistTxtPublished 便直接跳到 PUBLISHED + triggerCaValidation,且不呼叫 provider publish;在 terminate 時將其標記為 CLEANED,但 TXT 刻意不被刪除;斷言續約時從未呼叫 provider publish/cleanup

---

## 憑證部署 / 端點驅動

### IT-DEPLOY-01 INT-030 端到端生命週期
- **Given** 一筆 cn=app.test.local 的訂單透過 AcmeOrderPoller 到達 ISSUED;一個已啟用的 ssh EndpointEntity(已設定 expectedHostKey)、其 installSpec 為 {certPath,keyPath,reloadCmd:'systemctl reload nginx'} 被綁定(auto_deploy=true),且有一台 nginx 主機提供 :443 服務。
- **When** Poller 呼叫 enqueueForOrder(order,'system','auto');一位 supervisor POST /api/v1/admin/deploy-approvals/{jobId}/approve。
- **Then** 作業由 PENDING_APPROVAL -> QUEUED -> RUNNING -> SUCCESS;cert/key 落在目標路徑(key mode 0600),nginx 重載,after_serial 與 cert serial 相符,log_text 中出現 [verify-live] OK,endpoint.lastDeployStatus='success',且 DEPLOY_ENQUEUED/DEPLOY_APPROVED/DEPLOY_SUCCESS 稽核事件被串接。

### IT-DEPLOY-02 INT-031 安全
- **Given** Supervisor sup@corp 觸發一筆手動部署(POST /api/v1/orders/{id}/deploy),建立一個 PENDING_APPROVAL 作業,triggeredBy=sup@corp。
- **When** 同一個 sup@corp 對該 jobId 呼叫 approve。
- **Then** HTTP 409,訊息包含 'SoD';作業維持 PENDING_APPROVAL,沒有 QUEUED 轉移、沒有 driver 執行;接著由另一位核准者(sup2@corp)成功。同時斷言由系統觸發的作業(triggeredBy=system)任何人皆可核准。

### IT-DEPLOY-03 INT-032 併發/競態
- **Given** 一個 PENDING_APPROVAL 作業與兩位符合資格的 admin。
- **When** 兩者同時(平行執行緒)對同一個 jobId POST approve。
- **Then** 恰好一個回傳 200(作業 -> QUEUED,runJobAsync 觸發一次);另一個回傳 409 'job not pending'。只發生一次 DeployJob 執行,且 endpoint 只被安裝一次(一個 DEPLOY_SUCCESS,沒有重複的 F5 物件 / secret patch)。

### IT-DEPLOY-04 INT-033 韌性/故障轉移
- **Given** 節點 A 正在部署中(一個作業處於 RUNNING)且存在一個 QUEUED 作業,在一個共用單一 DB 的 2 節點叢集中。
- **When** 節點 B 啟動(或重啟),執行 DeployJobRecoveryRunner。
- **Then** 對於一個可銷售的 HA 產品而言,節點 B 不可將節點 A 進行中的 RUNNING 作業標記為 FAILED,也不可重新派發節點 A 可能已在執行的 QUEUED 作業。測試擷取目前行為(RUNNING->FAILED 無條件、QUEUED 在每個節點上都被重新派發)以衡量落差並驗證修正(在 worker 上加 ShedLock/owner fencing)。

### IT-DEPLOY-05 INT-034 功能整合
- **Given** 一個 local 或 k8s_secret endpoint 已持有 cert serial S_old 及其 key;一筆 S_new 的新部署被核准。
- **When** 進行安裝以落地一個其安裝後 inventory 指紋與 payload 不符的 cert(例如目標仍回傳 S_old)。
- **Then** 作業 -> ROLLED_BACK;目標使用 before_cert_pem+before_key_pem 還原為 S_old(而非把新 key 配舊 cert);endpoint.lastDeployStatus='rolled_back';發出 DEPLOY_ROLLED_BACK 稽核。另外驗證 fail-closed 分支:當 before_key_pem 為 null 時作業結束為 FAILED,絕不回滾。

### IT-DEPLOY-06 INT-036 安全
- **Given** 一個已設定 hmacSecret 的 webhook endpoint,以及一個會重新計算 hex(HMAC-SHA256(body,secret)) 的接收端。
- **When** (a) 一次部署 POST 到一個有效的 https 接收端;(b) 一位 admin 將 installSpec.url 設為 http://169.254.169.254/latest/meta-data/ 並部署。
- **Then** (a) 接收端針對確切的 JSON body 驗證 X-CLM-Signature 並回傳 2xx -> 作業走 SUCCESS 路徑;(b) SafeOutboundUrl 拋出 -> 作業 FAILED,沒有任何請求送往 metadata IP。同時確認非 http(s) 配置(file://、gopher://)被拒絕。

### IT-DEPLOY-07 INT-039 權限/多租戶隔離
- **Given** User U1(群組 G,訂單 O1 的 assignee)、user U2(同群組 G 但非 O1 的 assignee),以及在群組 H 中兩者皆看不到的 endpoints。
- **When** U2 GET /api/v1/orders/{O1}/deploys 並 POST /api/v1/orders/{O1}/deploy;deploy-approvals 列出時其中有一個群組 H 的作業。
- **Then** U2 收到 404 'order not found'(assignee 過濾連同群組的同儕也阻擋,使其無法讀取 logText/serials);deploy-approvals 過濾掉群組 H 的作業;binding 建立/刪除與 endpoint get 都遵守群組範圍(群組外 -> 404)。

### IT-DEPLOY-08 INT-035 韌性/故障轉移
- **Given** deploy.live-verify.strict=true;一個 ssh endpoint 其 reloadCmd 是 no-op(':'),因此設定已寫入但服務在 :443 上持續提供舊 cert。
- **When** 部署並核准一個新 cert。
- **Then** 控制平面驗證通過,但 [verify-live] 在 :443 上看到舊葉憑證指紋;作業被標記為 FAILED 並附帶 DEPLOY_LIVE_VERIFY 稽核。在 strict=false 下相同設定產生 SUCCESS + 警告(記載預設風險)。當探測主機無法連線時,即時步驟被跳過(不是誤判失敗)。

### IT-DEPLOY-09 INT-037 安全
- **Given** 一位 ADMIN/SUPERVISOR 建立一個 f5 endpoint,connection.host=169.254.169.254(或一個 rebinding 主機),verifyTls=false,沒有 expectedFingerprint。
- **When** 對此 endpoint 執行 test-connect 或一次部署。
- **Then** 展示 F5Driver 並未呼叫 SafeOutboundUrl(不同於 webhook/k8s)且 Mode C 是 trust-all -> 請求抵達 metadata/MITM 目標。測試斷言此落差並驗證修正(對 F5 主機套用 SafeOutboundUrl + 在沒有 pin 的 trust-all 時拒絕/警告)。

### IT-DEPLOY-10 INT-038 負向/邊界
- **Given** 一個沒有 expectedHostKey 且 allowUnknownHostKey=false 的 ssh endpoint;另外,一個其 installSpec.reloadCmd 含有 shell 中介字元 payload 的 endpoint。
- **When** (a) 在沒有 pin 的 host key 下執行部署;(b) 以精心構造的 reloadCmd 執行部署。
- **Then** (a) 連線在任何 scp/exec 之前被拒絕(私鑰絕不外露),作業 FAILED 並附帶 FD-06 訊息;(b) reloadCmd 被原樣傳給 shell(不做 {cn} 替換、不轉義)並執行——確認已記載的維運人員信任邊界,而 certPath/keyPath 的 {cn} 替換則拒絕路徑穿越/中介字元。將 reloadCmd 記載為維運人員可信任的欄位以供強化指引。

### IT-DEPLOY-11 INT-040 授權強制
- **Given** 一個在 Enterprise license(允許)下建立的 f5 endpoint,接著作用中的 license 被降級為排除 f5 的 Standard。
- **When** (a) 嘗試 CREATE 一個新的 f5 endpoint;(b) 核准一筆對既有 f5 endpoint 的部署。
- **Then** (a) 建立被以 license 訊息拒絕;(b) 既有的部署仍然執行,因為 endpointTypeAllowed 只在建立時檢查——記載此強制執行落差並驗證任何加入的執行期閘門。

### IT-DEPLOY-12 INT-041 冪等性
- **Given** 一筆訂單有一個已啟用的 binding 與一個既有的作用中作業(PENDING_APPROVAL)。
- **When** enqueueForOrder 在短時間內被呼叫兩次(自動重播 + 手動),接著在真正並行下執行。
- **Then** 依序執行時每個 (order,endpoint) 只存在一個作用中作業,且第二次發出 DEPLOY_ENQUEUE_SKIPPED 稽核;並行執行則被斷言以揭露 check-then-save 視窗(記載是否需要唯一約束 / 鎖定)。

### IT-DEPLOY-13 INT-042 功能整合
- **Given** 一個帶有 client-ssl profile 的 BIG-IP VE 實驗環境;cn=svc.test.local 已部署一次(物件名稱為 name-<fp8a>)。
- **When** 為同一個 CN 部署並核准第二個不同的 cert(物件 name-<fp8b>);另外還存在一個參照舊物件的第二個 profile。
- **Then** 新 cert/key 以新的 fp8 名稱安裝,profile 在一次 PATCH 中重新指向(沒有 in-use 01070317 錯誤),virtual-server 綁定被附加(而非取代),sys config save 成功(或在 save 失敗時為 SUCCESS-with-warning),且先前的 fp8a 物件只因沒有其他 profile 參照它而被移除(共用的那個被保留)。

---

## 自動續期進階

### IT-RENEW-ADV-01 INT-043 端到端生命週期
- **Given** 一個距到期 20 天的 ISSUED cert,帶有一個作用中的 30 天政策
- **When** AutoRenewScheduler.scan() 執行(直接呼叫或等待一個間隔)
- **Then** 恰好建立一個 CertIssuanceRequestEntity,status=PENDING、request_type=RENEWAL、requested_by_email='system:autorenew'、renewal_of_order_id=舊訂單、common_name=svc.example.com;發出 AUTORENEW_QUEUED 稽核;policy.last_attempt_at 被更新;立即進行第二次 scan() 不會建立任何重複(由 existsByCommonNameAndAccountIdAndStatus(PENDING) 抑制)

### IT-RENEW-ADV-02 INT-044 併發/競態
- **Given** 一個叢集部署,有一個可續約的 CN,且兩個節點被排程在同一瞬間掃描
- **When** 兩個節點對鎖名稱 'autorenew-scan' 並行觸發 scheduledScan()
- **Then** 恰好有一列 shedlock 'autorenew-scan' 被持有;只有一個節點執行 scan();該 CN 只存在一個 PENDING RENEWAL CIR(後續核准時沒有重複的 ACME 訂單);落敗的節點記錄跳過且不建立任何東西

### IT-RENEW-ADV-03 INT-045 權限/多租戶隔離
- **Given** 群組 B 的一位 supervisor,以及一筆隸屬於群組 A 的續約政策
- **When** S_B 呼叫 PUT /api/v1/autorenew/policies/groupA.example.com {paused:true} 與 DELETE /api/v1/autorenew/policies/groupA.example.com
- **Then** 兩者都回傳找不到類型的錯誤(不洩漏其存在);政策資料列維持不變(paused 仍為 false、資料列仍存在);S_B 的 GET 清單「不」包含 groupA.example.com;admin 的 GET 仍可看到它完好無缺

### IT-RENEW-ADV-04 INT-047 功能整合
- **Given** 一筆等待核准且已失敗 3 次的自動續約
- **When** admin 對該 CIR 呼叫 reject(POST issuance reject)並附上原因
- **Then** CIR.status=REJECTED;onRenewalRejected 執行 recordFailure -> consecutive_failures 變為 4 -> escalated=true、next_retry_at=null;cert.failed 通知同時寄送給「兩位」distList 收件者;記錄 AUTORENEW 升級;後續的 AutoRenewScheduler.scan() 會略過此 CN(被 findByEnabledTrueAndPausedFalseAndEscalatedFalse 排除)

### IT-RENEW-ADV-05 INT-049 端到端生命週期
- **Given** 一張同 CN、較舊、已 ISSUED 且有一筆未完成部署作業的憑證,以及一張剛續約的憑證
- **When** O2 達到 ISSUED 且 supersedeOlderFor(O2) 執行
- **Then** O1.status=SUPERSEDED、設定 updated_at;O1 的 PENDING_APPROVAL/QUEUED 部署作業翻轉為 SKIPPED,其 logText 引用 O2;O2 維持 ISSUED;此 CN 僅以 O2 計為現行憑證;不動到其他 CN 的訂單

### IT-RENEW-ADV-06 INT-050 授權強制
- **Given** 符合續約條件的憑證,但產品授權已到期
- **When** 呼叫 POST /api/v1/certs/renew-now(ADMIN),以及另外有一位 admin 嘗試核准一筆先前已排入的 RENEWAL CIR
- **Then** runOnce RenewalReport.skipped 包含 '(all): <license msg>' 且不排入任何項目;approve() 呼叫拋出授權被阻擋(在核准時重新檢查 canIssue),因此不會發生 CA 呼叫;稽核反映此阻擋

### IT-RENEW-ADV-07 INT-053 併發/競態
- **Given** 一筆等待核准的續約與兩位同時核准者
- **When** 兩個 approve 呼叫併發執行
- **Then** markApprovedIfPending CAS 讓恰好一個勝出(claimed=1);另一個得到 'already processed'/DuplicateRequest;此 CN 至多建立「一」筆 OrderEntity(PENDING_DNS);不留下孤立的 APPROVED-without-order(createOrder 失敗時的回滾路徑會還原為 PENDING)

### IT-RENEW-ADV-08 INT-046 安全
- **Given** 一位通過驗證的一般 USER,以及橫跨兩個群組的政策
- **When** U 呼叫 GET /api/v1/autorenew/policies 與 GET /api/v1/autorenew/policies/{cn-in-GB}
- **Then** 清單「僅」回傳 G_A 的 CN;針對 G_B 的單筆查詢回傳找不到類型的回應;未驗證的請求回傳 401(進入點),確認讀取是以資料列過濾而非以角色進行授權把關

### IT-RENEW-ADV-09 INT-048 韌性/故障轉移
- **Given** 一筆已失敗一次且處於 1 小時退避中的政策
- **When** 在 now < next_retry_at 時執行 AutoRenewScheduler.scan(),接著在將時鐘推進超過 next_retry_at 之後再次執行
- **Then** 第一次掃描「略過」此 CN(無新 CIR,第 119 行的把關),且不增加失敗次數;第二次掃描(時鐘已超過 next_retry_at)會排入一筆新的 RENEWAL CIR 並更新 last_attempt_at

### IT-RENEW-ADV-10 INT-051 負向/邊界
- **Given** 一個其續約政策表示「不要自動部署」但其部署 binding 表示要自動部署的 CN
- **When** 續約後的憑證轉換為 ISSUED 且 poller 的部署 hook 執行
- **Then** 「會」排入一筆部署作業(binding 勝出)——證明 policy.autoDeploy 旗標未被遵守;測試驗證目前行為並標示此矛盾,讓產品在 GA 前決定預期語意

### IT-RENEW-ADV-11 INT-052 功能整合
- **Given** 一張即將到期、沒有每個 CN 自動續約政策的憑證
- **When** RenewalService.runOnce() 執行(透過 cron 或 POST /renew-now),以及另外有 AutoRenewScheduler.scan() 執行
- **Then** AutoRenewScheduler 不做任何事(無政策資料列);RenewalService 排入一筆 RENEWAL CIR(requested_by_email='system+auto-renew')與 ORDER_AUTO_RENEW_QUEUED 稽核;RenewalService 會「略過」任何具備啟用中(enabled&&!paused&&!escalated)政策的 CN 以避免重複排入

### IT-RENEW-ADV-12 INT-054 功能整合
- **Given** 在設定的靜默視窗期間有一個符合條件的 CN,以及另外有一個被停用的全域開關
- **When** AutoRenewScheduler.scan() 在靜默時段執行,接著在 autorenew.enabled=false 時執行,再接著在兩者都清除後執行
- **Then** 靜默時段期間不建立 CIR(記錄 'in quiet hours, skip');autorenew.enabled=false 時不建立 CIR(記錄 'disabled, skip');兩者都清除後,scan() 排入該 RENEWAL CIR——證明設定為即時讀取、無需重啟

### IT-RENEW-ADV-13 INT-055 資料遷移
- **Given** 存放已到期短期驗證狀態的資料表,以及一個叢集部署
- **When** cleanupExpired()(clm.cleanup.cron)在兩個節點上觸發
- **Then** 僅一個節點持有 shedlock 'expired-state-cleanup';已到期的資料列被刪除、存活的資料列保留;oidc_state 清理失敗不會阻止 mfa_pending 清理(獨立的 try/catch);重新執行刪除 0 筆

### IT-RENEW-ADV-14 INT-056 韌性/故障轉移
- **Given** 一次在運作中節點上超過 30 分鐘 lockAtMostFor 的續約執行
- **When** node2 的觸發在 node1 仍在執行時、鎖自動到期後觸發
- **Then** 測試證明 node2 可取得 'renewal-scheduler' 並啟動一次並行執行——記載 lockAtMostFor 必須超過最糟情況的掃描+DNS 傳播時間;驗證 CIR 去重(PENDING + 進行中)作為第二道安全網仍能防止重複的 CA 訂單

---

## 簽核工作流進階

### IT-APPR-01 INT-057 併發/競態
- **Given** 存在兩筆對應「相同」common_name+account_id 的 PENDING CertIssuanceRequestEntity 資料列(cir_A、cir_B),兩者皆在任一被核准前建立(送出時去重 existsBy 對兩者皆通過;DB 上無 common_name+account_id+status 的 UNIQUE 限制)。
- **When** 兩個 ADMIN/SUPERVISOR 執行緒同時呼叫 POST /api/v1/admin/cert-issuance-requests/{cir_A}/approve 與 /{cir_B}/approve,各自帶入其每筆資料列的 CAS(markApprovedIfPending 對兩個不同的 requestId 皆成功),接著競相進入 AcmeOrderService.createOrder。
- **Then** 系統「必須」以此 CN 至多一筆 ACME 訂單收尾:approve 內的 hasInflightOrderForCn 把關必須拒絕第二個,「或」必須有 DB UNIQUE/序列化檢查防止它。驗證此 CN 恰好一筆 OrderEntity、敗者回傳 409 duplicate_request,且稽核顯示一筆 CERT_REQUEST_APPROVED。

### IT-APPR-02 INT-058 韌性/故障轉移
- **Given** 一筆 PENDING 簽發請求 cir_X;對 AcmeOrderService.createOrder 進行檢測,使其在 markApprovedIfPending 提交 APPROVED 後立即終止執行緒/JVM(或拋出一個 catch(Exception) 未捕捉的 Error)。
- **When** approve(cir_X) 執行;APPROVED 已提交;程序在 createOrder 完成之前、且在 catch 區塊的回滾執行之前死亡。
- **Then** 重啟後「必須」有一條對帳路徑:要嘛有一個啟動掃掠找出 status=APPROVED 且 created_order_id IS NULL 並重新驅動或還原它,要嘛有一個 admin 可見的「卡在 approved」警示。驗證該憑證未被靜默遺失,且該請求可復原。

### IT-APPR-03 INT-059 權限/多租戶隔離
- **Given** 群組 G1(supervisor sup1)與群組 G2(supervisor sup2)。存在一筆對應其帳號在 G2 之訂單的 PENDING 撤銷請求 rvk_G2。
- **When** sup1 呼叫 GET /api/v1/admin/revoke-requests 與 POST /api/v1/admin/revoke-requests/{rvk_G2}/approve。
- **Then** GET「不可」列出 rvk_G2(sup1 的 canAccessGroup(G2) 為 false);approve 必須回傳 404 not_found(找不到請求),且「不」發生 CA 撤銷。對 download-requests、binding-requests、cert-issuance-requests 重複此項。另驗證 null 群組的請求對兩位 supervisor 皆不可見(fail-closed)且僅對 admin 可見。

### IT-APPR-04 INT-060 安全
- **Given** 使用者 userA(群組 G1)有一筆 APPROVED、尚未消費的下載請求 dlr_A(format=pfx,由 admin 代為請求,TTL 15m)。userB(群組 G2)已通過驗證。
- **When** userB 呼叫 GET /api/v1/download-requests/{dlr_A}/download(猜測的 requestId);另外將 userA 自 G1 移除/帳號移至 G2,然後 userA 重試下載。
- **Then** userB「必須」得到 403 'this download is only for the original requester';群組變更後,userA 的消費「必須」未通過縱深防禦的 canAccessGroup 重新檢查(SecurityException),即使他們是原始請求者。驗證未回傳任何金鑰位元組、consumed_at 未變更、稽核未顯示 SECRET_DOWNLOADED。

### IT-APPR-05 INT-061 韌性/故障轉移
- **Given** 一筆對應 ISSUED 訂單的 PENDING 撤銷 rvk_Y;mock CA 設定為「接受」撤銷呼叫但「不」實際撤銷(模擬那筆已記載的 fail-open 行為),或回傳一個被 acme4j 吞掉的軟性錯誤。
- **When** approve(rvk_Y) 執行:CAS 標記 APPROVED、revokeCertificate 回傳 'success'。
- **Then** 定義並驗證契約:系統「必須」在宣告 REVOKED 之前驗證撤銷後狀態(OCSP/CRL 或 CA 重新查詢),「或」清楚地浮現「已送出撤銷、尚未確認」。測試驗證一個 fail-open 的 CA 不會在沒有警示的情況下靜默地產生 REVOKED-在-DB/有效-在-CA 的分歧。

### IT-APPR-06 INT-062 功能整合
- **Given** admin1 各送出一筆:憑證簽發請求(以 admin1 透過 POST orders)、撤銷、下載、部署 binding——全部 requestedByEmail=admin1。
- **When** admin1 核准其自己的每一筆請求;接著 supervisor(非 admin)嘗試自我核准其自己的撤銷/下載/binding。
- **Then** admin1 的簽發自我核准「必須」被拒絕(409 SoD),且 admin「無」例外。admin1 的撤銷/下載/binding 自我核准「必須」成功,selfApproved=true 且稽核 emergencySelfApprove=true。supervisor 對撤銷/下載/binding 的自我核准「必須」被拒絕(僅允許 ADMIN)。驗證每一筆稽核事件記錄正確的 selfApproved/bypassedSoD 旗標。

### IT-APPR-07 INT-063 安全
- **Given** 一個舊式(非範圍化)的 X-API-Key,其主體解析為 role=admin、isServiceAccount=true、email='apikey:<id>'。一位人類使用者 userC 送出一筆撤銷請求(requestedByEmail=userC)。
- **When** 該舊式 API 金鑰對 userC 的請求呼叫 approve(以及另外,該金鑰送出其「自己的」請求然後核准它)。
- **Then** 定義並測試其預期政策:一個服務金鑰核准一位人類的請求會輕易通過 SoD(email 不同)——驗證產品究竟是否打算允許服務金鑰作為核准者;對於金鑰送出後金鑰核准,因 email 相同,SoD 必須阻擋。驗證服務帳號的 canAccessGroup(null) 繞過是否為預期且有被稽核。此情境迫使一個產品決策並以測試將其鎖定。

### IT-APPR-08 INT-064 併發/競態
- **Given** APPROVED 下載 dlr_Z,expires_at = approve+15m,consumed_at 為 NULL。
- **When** (a) 原始請求者雙擊下載(兩個併發 GET .../download);(b) 在 expires_at+1ms 進行第二次嘗試;(c) 兩個時鐘有偏移的叢集節點各自評估到期。
- **Then** (a) 恰好「一」個呼叫回傳檔案;另一個得到 409 'already used'(markConsumedIfDownloadable CAS 對敗者回傳 0)。(b) 已到期的呼叫回傳 409 expired,consumed_at 維持 null。(c) 驗證到期決策一致(以伺服器為準的單一時鐘來源)——若節點時鐘導致分歧則標示。

### IT-APPR-09 INT-065 通知
- **Given** 一筆 type=RENEWAL 簽發請求,requested_by_email='system+auto-renew'(CREATED_BY_TAG),對應一張將於 10 天後到期的憑證。
- **When** 一位 supervisor 拒絕它;在某個變體中 AutoRenewScheduler.onRenewalRejected 拋出例外。
- **Then** 正常情況:拒絕 -> REJECTED 稽核 + AutoRenewScheduler 記錄退避/升級,使該政策在到期前重試/警示。故障變體:onRenewalRejected 的失敗目前被吞掉(僅記錄)——驗證有一個補償訊號(警示/指標)使憑證不致靜默到期。測試端到端:被拒絕的續約最終必須浮現為一張有風險的憑證。

### IT-APPR-10 INT-066 端到端生命週期
- **Given** userA 透過 POST /certs/domains/{cn}/bindings 送出 CN<->endpoint binding(-> bndreq PENDING)。端點 requires_approval(由 V16 強制,執行期忽略旗標 = 一律需核准)。
- **When** supervisor1 核准該 binding(建立 DeployBindingEntity);接著為該 CN 簽發/核准一張憑證;部署作業自動排入 PENDING_APPROVAL;userA 也觸發手動部署;supervisor1 核准該部署作業。
- **Then** 整條鏈運作「且」SoD 主體正確:binding SoD = requestedBy(userA)!=approver(sup1);部署作業 SoD = triggeredBy!=approver。驗證單一人類無法獨自滿足兩道閘,且不論 requires_approval 旗標為何,100% 的部署皆命中 PENDING_APPROVAL。驗證部署驅動程式僅在作業 QUEUED 後才觸發。

### IT-APPR-11 INT-067 安全
- **Given** 某管理員針對 CN=leak.example.com 擁有多張 ISSUED 憑證(3 筆訂單)。Mock CA 設定為前兩筆成功撤銷,第三筆則逾時。
- **When** 管理員以 reason='key leak' 呼叫 POST /admin/revoke-requests/break-glass/by-cn;另外也測試填寫空白原因,以及帶入無效 reasonCode(例如 7)的破窗操作。
- **Then** 空白原因 -> 400(必填)。無效 reasonCode -> 400 且發生在任何儲存之前(不會留下懸空的 APPROVED 列)。依 CN:CA 優先排序代表每次成功撤銷會儲存一筆 brk_ APPROVED 列;第 3 筆的 CA 逾時會讓該憑證維持 ISSUED 且不會留下孤兒 brk_ 列,而批次結果/計數 + REVOKE_BREAK_GLASS_BATCH 稽核會精確反映 2 筆已撤銷 / 1 筆失敗(而非誤導性的 3 筆)。驗證 bypassedSoD=true 且 SIEM 轉送已觸發。

### IT-APPR-12 INT-068 授權強制
- **Given** 在授權有效期間提交了數筆 PENDING NEW 簽發請求。授權隨後轉為到期/失效(license.canIssue()=false),或 FQDN 配額現已超出。
- **When** 授權變更後,某核准者核准一筆積壓的請求;另外,多名核准者在配額邊界上同時核准不同 CN 的請求。
- **Then** 到期後的核准必須以授權受阻訊息被拒絕(不呼叫 CA)—— 證明 FD-02 的重新檢查確實生效,而不只是提交時的檢查。在配額邊界上,驗證系統不會因為跨多筆併發核准的「檢查後動作」非原子性而超出 quotaFqdn(定義預期行為:序列化或拒絕溢出的部分)。

### IT-APPR-13 INT-069 API 合約
- **Given** 四個核准控制器及其 @ExceptionHandler 對應(DuplicateRequestException->409 duplicate_request、IllegalState->409 conflict、找不到->404、SecurityException->403、驗證->400)。
- **When** 觸發每一種錯誤:核准一筆非 PENDING 的請求(已是 APPROVED/REJECTED)、核准一個不存在的 requestId、提交重複的 CN、以一般使用者身分請求 pfx 下載、以 null 內文進行拒絕。
- **Then** 對每一種情況驗證精確的 HTTP 狀態 + JSON 錯誤代碼/訊息,涵蓋全部四個控制器,且彼此一致(例如 conflict 與 not_found 不會互換)。驗證 PENDING_APPROVAL 提交回傳 202 並帶 requestId。將這些鎖定為合約,使 SPA 與 CI/CD 整合不致中斷。

### IT-APPR-14 INT-070 可觀測性
- **Given** 一個完整的簽發生命週期:SUBMITTED -> APPROVED(-> 建立訂單),以及另一條獨立的 SUBMITTED -> REJECTED,外加一筆破窗 REVOKE_BREAK_GLASS。
- **When** 驅動每條生命週期,然後匯出/驗證 HMAC 稽核鏈。
- **Then** 驗證每個事件(CERT_REQUEST_SUBMITTED/APPROVED/REJECTED、REVOKE_BREAK_GLASS、SECRET_DOWNLOADED)皆存在,且具有正確的前/後狀態、核准者 email、requestId、createdOrderId,以及 self_approved/bypassedSoD 旗標;驗證 HMAC 鏈通過驗證(無斷裂/竄改),且竄改其中一列可被偵測。確認 DNS 綁定變更事件具有對等的稽核(或標記出此結構缺口)。

---

## 法遵政策引擎

### IT-COMP-01 INT-071 端到端生命週期
- **Given** 某 ADMIN 啟用了一個全域政策,enforcement=BLOCK 且 rulesJson {"san.no.wildcard":true};使用者 'alice' 已通過驗證並具有有效的帳號/provider。
- **When** alice 以 commonName=app.example.com 與 subjectAlternativeNames=[*.example.com] 對 /api/v1/acme/{provider}/accounts/{accountId}/orders 發出 POST。
- **Then** 回應為 HTTP 400,body.error=='policy_violation',violations[] 包含 ruleKey=san.no.wildcard、enforcement=BLOCK;不會建立任何 OrderEntity;恰好存在一筆 clm_policy_violation 列,severity=ERROR、orderId=null、commonName=app.example.com、resolvedAt=null。

### IT-COMP-02 INT-072 韌性/故障轉移
- **Given** 啟用了一個帶 {"key.size.min":2048} 的 BLOCK 政策,且該 CN 至少存在一個部署綁定;某訂單被驅動至 ISSUED,其葉憑證金鑰為 1024 位元(透過測試 poller 接縫/stub CA 使用一張預先備妥的 1024 位元憑證)。
- **When** poller 抵達 ISSUED 終態並執行 evaluatePostIssuance。
- **Then** 訂單狀態維持 ISSUED(不失敗、不撤銷);寫入一筆 clm_policy_violation,severity=ERROR、ruleKey=key.size.min 且已設定 orderId;記錄一筆稽核事件 POST_ISSUANCE_POLICY_VIOLATION,action 為 'auto-deploy skipped';不會排入任何部署工作(DeploymentService.enqueueForOrder 未被呼叫);一封 cert.failed email 被排入給擁有者;憑證 PEM 仍可透過憑證 API 取得。

### IT-COMP-03 INT-073 負向/邊界
- **Given** 啟用了一個 BLOCK 政策 {"signature.algorithm.blacklist":["sha1"],"key.size.min":2048}。
- **When** 某管理員透過匯入 API 匯入(單筆 + 批次)一張 SHA1 簽署及一張 1024 位元的 PEM 憑證。
- **Then** 確認缺陷的斷言:今日匯入會成功且建立零筆 clm_policy_violation 列(CertImportBatchService 沒有呼叫 PolicyEngine)。欲驅動的期望行為:匯入至少應為匯入的葉憑證記錄簽發後型態的違規(severity ERROR),並在 /api/v1/admin/policies/violations 中呈現。

### IT-COMP-04 INT-075 安全
- **Given** 使用者:'user' 角色 bob、'supervisor' 角色 carol、'admin' dave;dave 建立一個 group:{G1} 政策,而群組 G2 擁有自己的憑證/違規。
- **When** bob 與 carol 對 /api/v1/admin/policies 呼叫 GET/POST/PUT/DELETE,以及 GET /api/v1/admin/policies/violations 和 POST .../violations/{id}/resolve。
- **Then** bob 與 carol 在每個政策端點上皆收到 HTTP 403(僅允許 ADMIN);對 dave,確認違規端點回傳所有群組的違規(記錄政策管理缺乏群組範圍隔離),以便產品團隊決定 admin 全域可見性是否可接受。

### IT-COMP-05 INT-074 資料遷移
- **Given** 100 張既有的 ISSUED 憑證,金鑰為 2048 位元,且有一個啟用的政策 {"key.size.min":2048}(無違規)。
- **When** 管理員以 PUT 將政策改為 {"key.size.min":4096}(BLOCK)。
- **Then** 目前:違規清單仍為空 —— 沒有任何既有憑證被重新評估;只有下一筆新簽發會被檢查。測試明確斷言此缺口,並定義目標:一個重新評估既有的端點/工作應為這些現已過小的憑證產生 100 筆開放的 ERROR 違規。

### IT-COMP-06 INT-076 API 合約
- **Given** 管理員,使用 'full' 階授權。
- **When** 管理員 POST 一個 rulesJson {"key.size.minimum":2048} 的政策(打錯字),接著 {"key.size.min":"2048"}(型別錯誤,字串而非整數),再接著 {"key.size.min":2048}(有效)。
- **Then** 前兩者回傳 HTTP 400,訊息列出有問題的鍵與合法鍵集合(KNOWN_RULES);第三者回傳 200 並持久化。確認一個拼錯的規則無法被無聲地儲存而後永不觸發。

### IT-COMP-07 INT-077 負向/邊界
- **Given** 一個政策 scope='domain:*.example.com' enforcement=BLOCK 帶 {"san.no.wildcard":true}。
- **When** 建立一筆 CN='a.example.com.attacker.net'(以及另外一筆 CN='svc.example.com')的訂單。
- **Then** 記錄實際行為:因為 '*' 被替換為 '.*'(跨越點),'a.example.com.attacker.net' 匹配該範圍並被評估/阻擋 —— 相較於 DNS 萬用字元語意過於寬泛。svc.example.com 正確匹配。測試釘住此行為並將其標記為範圍界定風險。

### IT-COMP-08 INT-078 負向/邊界
- **Given** 一個 BLOCK 政策 {"subject.cn.pattern":"^(a+)+$"}(災難性回溯),以及另外一個無效正規表示式 {"subject.cn.pattern":"(["}。
- **When** 針對 ReDoS pattern 建立一筆帶有像 'aaaaaaaaaaaaaaaaaaaaaaaa!' 這種長 CN 的訂單;以及對無效正規表示式政策建立任何訂單。
- **Then** 無效正規表示式:evalCnPattern 捕捉 PatternSyntaxException,記錄 WARN,不記錄任何違規且不阻擋(已記錄的 fail-open)。ReDoS pattern:測量請求延遲 —— 斷言其在某個界限內完成(例如 <2s),否則讓測試失敗,以證明引擎需要正規表示式逾時/預先編譯的防護。

### IT-COMP-09 INT-079 併發/競態
- **Given** BLOCK 政策 {"san.max.count":2};一名使用者發出 20 筆併發、相同、帶 5 個 SAN 的訂單。
- **When** 全部 20 筆請求同時抵達 createOrder。
- **Then** 全部 20 筆回傳 HTTP 400(併發下強制執行一致,無訂單建立);clm_policy_violation 新增 20 筆獨立的開放列(記錄缺少去重/關聯 —— 標記為資料成長風險)。在 JPA 儲存負載下無死結/例外。

### IT-COMP-10 INT-080 授權強制
- **Given** 授權 complianceLevel='core7';存在一個系統植入的 WARN 基準政策。
- **When** 管理員 POST 一個新政策、PUT 那個已植入的政策、GET violations export.xlsx,並 DELETE 該已植入的基準政策。
- **Then** POST/PUT/匯出回傳 HTTP 400 'Professional 以上方案'(僅 full 把關);DELETE 成功(未受階別把關)並移除基準 —— 暴露出 Standard 管理員可剝除該階別所承諾的合規本身。測試將此釘為一個需修正的把關不一致。

### IT-COMP-11 INT-082 功能整合
- **Given** 同一筆訂單的 CN 上有三個啟用的全域政策:P1 BLOCK {"ca.blacklist":["letsencrypt"]}、P2 WARN {"san.max.count":1}、P3 AUDIT_ONLY {"subject.cn.pattern":"^prod-.*"};訂單使用 provider letsencrypt、3 個 SAN、CN test-app。
- **When** POST 該訂單。
- **Then** HTTP 400(P1 BLOCK 存在,故 hasBlocking=true);持久化三筆違規,嚴重度分別為 ERROR(P1)、WARNING(P2)、INFO(P3);訂單未建立。接著停用 P1 並重新 POST:HTTP 200/訂單建立,但 P2(WARNING)與 P3(INFO)違規仍被記錄(不阻擋的規則會記錄但不會阻止簽發)。

### IT-COMP-12 INT-081 冪等性
- **Given** 空的 clm_policy 資料表;授權階別 basic。
- **When** App 啟動(run/seedIfNeeded),接著啟用一個升級至 core7 的授權(LicenseController 再次呼叫 seedIfNeeded),然後 app 重新啟動。
- **Then** 首次啟動後:恰好 1 個 WARN 全域政策,帶 BASIC_RULES(3 條規則)。啟用重新植入後:因為 count()>0,所以不會覆寫(仍為 basic 基準)—— 記錄升級不會自動將 basic 擴充為 core7 規則。重新啟動後:仍恰好 1 個政策(無重複植入)。

---

## 探勘 / 撤銷狀態

### IT-DISC-01 INT-083 端到端生命週期
- **Given** 一個 'local' 端點,其 certPath 指向一個含有未受管 leaf.pem(完整 PEM,含 SAN)的目錄,而該憑證在 acme_orders 中沒有對應列。
- **When** 以 ADMIN 身分 POST /api/v1/admin/scan/endpoints/{id},接著 GET /discoveries?unmanaged=true,然後 POST /discoveries/{discId}/import。
- **Then** 掃描回應 managed=0 unmanaged=1;持久化一筆 discovery 列,cert_pem 已填入且 managed=false;匯入回傳 201,orderId 前綴為 'imp_' 且 status=IMPORTED;discovery 列現為 imported=true、managed=true、已設定 managed_order;重新執行掃描會維持 imported=true 且不會把 managed 翻回 false;稽核鏈含有 SCAN_ENDPOINT + DISCOVERY_IMPORTED。

### IT-DISC-02 INT-084 負向/邊界
- **Given** 一個 'f5' 端點,其 iControl REST 回傳 ssl-cert 物件(指紋、CN、簽發者、到期)但沒有 PEM 本體(F5 17.5.x 行為),產生 cert_pem=null 的 discovery 列。
- **When** POST 掃描,接著對一個 F5 發現的未受管憑證 POST /discoveries/{id}/import。
- **Then** 掃描成功並建立列(以儲存在 serial 欄位的指紋識別);匯入回傳 HTTP 409 conflict,訊息為 'discovery has no cert PEM, cannot import'。測試斷言這是目前行為,以便產品團隊明確決定要修正(抓取 PEM)或在 GA 前界定 F5 上架的範圍。

### IT-DISC-03 INT-085 權限/多租戶隔離
- **Given** 兩個群組 G1、G2;G1 中的 endpointA 帶有一個 discovery discA;一個僅限定於 G2 的 SUPERVISOR 使用者。
- **When** 以 G2 supervisor 身分:GET /discoveries(清單)、GET /discoveries/{discA}(PEM)、POST /discoveries/{discA}/import、POST /discoveries/{discA}/acknowledge,以及 POST /scan/endpoints/{endpointA}。
- **Then** 清單完全省略 discA;各個按 id 的端點皆回傳 404 'not_found'(而非 403,以避免洩漏存在性/PEM);掃描 endpointA 回傳 404。一個 USER 角色權杖在每個 scan 端點上皆得到 403(類別層級 @PreAuthorize)。不發生任何 clm_discoveries 變更。

### IT-DISC-04 INT-086 授權強制
- **Given** 一份已簽署的授權,quotaFqdn=N;acme_orders 已管理 N 個相異 FQDN;一個未受管的 discovery(含 PEM),其 CN/SAN 引入至少一個新的相異 FQDN。
- **When** POST /discoveries/{id}/import。
- **Then** 回傳 400,帶 FQDN 上限訊息;不建立任何 IMPORTED 訂單;discovery 維持 imported=false。接著匯入一個 CN/SAN 皆已在受管集合內(無新 FQDN)的 discovery,即使在上限處仍成功。

### IT-DISC-05 INT-087 負向/邊界
- **Given** 授權 quotaFqdn 恰在上限;一個 SSH/F5 型的 discovery,其底層憑證實際涵蓋多個 SAN,但其 sans_json 為 null(driver 省略了 SAN)。
- **When** POST /discoveries/{id}/import(PEM 存在的情況,例如已植入),量測匯入前/後的 FQDN 使用量。
- **Then** 測試展示:因為 sansJson 為 null,wouldExceed 只計算 CN,因此一張多 SAN 憑證可被匯入卻只消耗 1 個 FQDN 額度 —— 暴露此低估。斷言此缺口以便修正(於匯入時從 PEM 解析 SAN)。

### IT-DISC-06 INT-088 併發/競態
- **Given** 一個共用單一 MSSQL ShedLock 資料表的 2 節點叢集;授權 discoveryMode 依序設為 'manual'、'scheduled'、'auto';數個啟用 + 一個停用的端點。
- **When** 在兩個節點上幾乎同時觸發 scheduledScan() cron。
- **Then** 對 mode='manual'/'none',該工作立即回傳且不掃描任何項目;對 'scheduled'/'auto',恰有一個節點取得 lock name='discovery-scan' 並只掃描啟用的端點(停用者略過),第二個節點則為 no-op。逐端點的失敗會累加失敗計數而不中止整次執行。

### IT-DISC-07 INT-089 韌性/故障轉移
- **Given** 某端點的目標無法連線(TCP 遭拒)或其 driver 拋出例外(F5 回傳 HTTP 500／SSH 主機金鑰失敗)。
- **When** POST /scan/endpoints/{id}。
- **Then** 回傳 HTTP 200,且 totalFound=0,errors[] 含有 'driver.listAllCerts failed: ...'(或 'buildContext failed');寫入一筆 SCAN_ENDPOINT 失敗稽核事件;不會持久化任何部分／垃圾探索資料列;在目標恢復後再次掃描時可正常持久化資料列。

### IT-DISC-08 INT-090 功能整合
- **Given** 一次部署已將新憑證寫入控制平面(driver verify 通過),但本機 TLS 伺服器在 :443 上仍呈現舊的葉憑證;設定 deploy.live-verify.enabled=true;訂單 CN 是非萬用字元且可連線的主機。
- **When** 執行部署後驗證。
- **Then** liveTlsProbe 回傳的舊葉憑證指紋 != 預期的新指紋,且驗證紀錄記下一筆 live-verify 不符(部署被標記);反之若伺服器無法連線,探測回傳 null 且略過即時驗證(非失敗)。並斷言有送出 SNI,且 trust-all 不會拒絕自簽對端。

### IT-DISC-09 INT-091 功能整合
- **Given** 三張測試憑證:(a) 序號列在其 CDP 上可連線的 CRL 中、(b) 序號未列在可連線的 CRL 中、(c) 沒有 CDP 擴充欄位／CDP 主機無法連線的憑證。
- **When** 對每張憑證呼叫 checkViaCrl(cert)(搭配 stub HTTP CRL 端點)。
- **Then** (a)->REVOKED、(b)->GOOD、(c)->UNKNOWN;當無法下載任何 CRL 時絕不出現假性 GOOD;多個 CDP URL 都會嘗試,且任一列出即判定撤銷。此外斷言沒有任何正式環境呼叫端串接此服務(已記錄的死碼缺口),使藍圖要嘛提供排程對帳作業,要嘛移除該銷售主張。

### IT-DISC-10 INT-092 冪等性
- **Given** 某端點掃描一次後產生探索 D(acknowledged=true、imported=false),隨後同一張憑證以更新後的中繼資料再次被掃描(例如 issuer 字串變更、新的 found_at)。
- **When** 第二次 POST /scan/endpoints/{id}。
- **Then** 不會建立重複資料列(以「序號或指紋」+ source_ref 去重);found_at 前進至新的掃描時間;中繼資料(CN/issuer/notAfter/PEM)更新;acknowledged 維持 true,imported 維持原狀;若該憑證同時於 acme_orders 中被比對到,managed 翻轉為 true。若該憑證從目標消失,既有資料列仍保留(不自動刪除)——斷言此點,使維運人員知道探索不會被自動清除。

### IT-DISC-11 INT-093 功能整合
- **Given** acme_orders 中含有一筆 ISSUED 訂單,其 cert_fingerprint_sha256=FP 且 cert_serial=S;一筆 F5 探索僅帶有 fingerprint=FP(無序號),一筆 Local 探索僅帶有 serial=S(大寫／冒號格式的變體)。
- **When** 對每個端點 POST 掃描。
- **Then** 兩者皆分類為 managed=true,且 managed_order 指向同一筆訂單;序號比對透過 normalizeSerial(去除非十六進位字元、轉小寫)以及原始序號備援皆成功;一張既無 FP 也無序號相符的憑證分類為 unmanaged=true。

### IT-DISC-12 INT-094 效能/負載
- **Given** 在 20 個群組的 500 個端點上植入 50k 筆 clm_discoveries 資料列;一位範圍限縮於 1 個群組、約有 100 筆可見探索的 supervisor。
- **When** 重複 GET /discoveries?unmanaged=true,量測延遲、堆積記憶體與 SQL。
- **Then** 確立目前實作會載入全部 50k 筆資料列並在記憶體中過濾(無 WHERE 群組／受管下推、無分頁),並擷取 p95 延遲／堆積記憶體;定義 SLO 並斷言 GA 之前需要分頁／群組範圍的查詢。確認一個在小資料量下看不見的 DoS／效能懸崖。

### IT-DISC-13 INT-095 API 合約
- **Given** 各種不良輸入:未知的 endpointId、未知的 discoveryId、重複匯入已匯入的探索、授權 discoveryMode=='none' 時掃描。
- **When** 以 ADMIN 身分對每個端點送出不良輸入。
- **Then** 未知端點／探索 -> 404 {error:'not_found'};已匯入 -> 409 {error:'conflict'};discoveryMode none -> 400(授權訊息);匯入成功 -> 201,且 JSON 鍵穩定(orderId、commonName、certSerial、certNotAfter、status)。回應結構在各版本間維持穩定。

---

## 驗證 / Session / 速率限制

### IT-AUTH-01 INT-096 安全
- **Given** 一位使用者已登入(POST /api/v1/auth/login)並持有更新權杖 R1;clm_refresh_token_registry 對 R1 有一筆 active 資料列。
- **When** 用戶端 A 以 R1 呼叫 POST /api/v1/auth/refresh -> 取得 R2(R1 資料列被設為 revoked_reason=ROTATED)。接著攻擊者將已輪換的 R1 重放至 /refresh。
- **Then** 重放必須以 401 refresh_token_revoked 被拒絕,且系統應將已輪換權杖的重用視為遭入侵,並撤銷整個權杖家族(R2)——在 DB 中驗證 R2 是否也被撤銷。記載實際行為:目前程式碼僅拒絕 R1,R2 仍維持 active(缺口)。

### IT-AUTH-02 INT-097 安全
- **Given** userB 已登入,持有有效的 clm_access JWT(30 分鐘 TTL)與一個 active 的更新權杖。
- **When** admin 呼叫 POST /api/v1/users/{userB}/revoke-sessions,接著 userB 僅使用仍有效的存取 cookie(無更新權杖)呼叫受保護端點(例如 GET /api/v1/auth/me)。
- **Then** 確認 /me 在存取 TTL 到期前仍回傳 200(證明存取權杖為無狀態且不經 registry 檢查),而 /api/v1/auth/refresh 回傳 401 refresh_token_revoked。這量化了該撤銷缺口,使其可被記載／訂定 SLA,或以縮短存取 TTL 修正。

### IT-AUTH-03 INT-102 安全
- **Given** 應用程式以 prod 設定檔啟動(SPRING_PROFILES_ACTIVE=prod)且 CLM_JWT_SECRET 已設定。
- **When** 呼叫 GET /api/v1/auth/dev-users;另外,啟動應用程式並檢查紀錄／DB 是否有密碼重設。
- **Then** /api/v1/auth/dev-users 必須回傳 404／handler-missing(因 @Profile(dev) 故 DevAuthController bean 未註冊),即使該 URL 在 SecurityConfig 中為 permitAll;AuthBootstrapRunner 不得將任何使用者密碼重設為 'testtest'(resetAdminPasswordInDevProfile 僅在 dev 下觸發)。新增一項斷言:字面值 'testtest' 在 prod 中絕不為有效登入。

### IT-AUTH-04 INT-103 安全
- **Given** 應用程式以固定的 CLM_JWT_SECRET 執行;使用者持有有效的 clm_access cookie。
- **When** (a) 出示以不同金鑰簽署的權杖;(b) 出示 alg=none 的權杖;(c) 將 role 宣告從 user 竄改為 admin 並重新 base64 而不重新簽署;(d) 向受保護的非 auth 端點出示 refresh 型別的權杖。
- **Then** 四者都必須導致匿名情境 -> 入口點回傳 401(或在存取路徑上對型別不符回傳 403);具體而言,tryAuthenticateJwt 必須拒絕 type!=access 以及任何簽章／簽發者(issuer)失敗。不會透過宣告竄改達成權限提升。

### IT-AUTH-05 INT-098 負向/邊界
- **Given** login.rate.limit.capacity=10、window.sec=60(透過 SettingsService 植入的預設值)。
- **When** 從單一來源 IP 在 60 秒內以錯誤憑證送出 11 次 POST /api/v1/auth/login。
- **Then** 第 1-10 次請求回傳 401 invalid_credentials;第 11 次請求回傳 HTTP 429,內文 error=rate_limited 且帶有 Retry-After 標頭 > 0。窗口重設後,登入再次被允許。並驗證成功的登入仍會計入桶(bucket)。

### IT-AUTH-06 INT-099 併發/競態
- **Given** 兩個 RXCLM 實例位於輪詢(round-robin)負載平衡器之後,各自擁有自己的記憶體內桶;capacity=10/60s。
- **When** 透過 LB 送出 20 次錯誤密碼的 POST /api/v1/auth/login,使每個實例約收到 10 次。
- **Then** 證明在出現任何 429 之前,最多有 20 次嘗試能成功抵達 controller(每實例 10 次),證明每 JVM 的限制不會彙總。這驗證了已記載的 HA 限制,以及對 sticky session 或分散式桶的需求。

### IT-AUTH-07 INT-100 功能整合
- **Given** account.lockout.threshold=5、account.lockout.minutes=15;userC 已啟用。
- **When** 對 userC 提交 5 次錯誤密碼,然後即便以正確密碼進行第 6 次嘗試。
- **Then** 在第 5 次失敗後,clm_users.locked_until 設為約 15 分鐘後,且 failed_logins 重設為 0;鎖定期間以正確密碼的嘗試回傳 403 account_locked 並帶 lockedUntil。在 locked_until 過後(或 admin 重設清除後),正確登入回傳 200。驗證每次嘗試都有一筆 LOGIN 失敗稽核資料列。

### IT-AUTH-08 INT-101 安全
- **Given** 攻擊者得知 admin@local 存在;帳號鎖定是針對每帳號且獨立於 IP 速率限制。
- **When** 攻擊者從輪替的 IP 反覆對 admin@local 提交 5 次錯誤密碼(維持在每 IP 速率限制之下)、等待、再重複。
- **Then** 驗證 admin 在每個週期被鎖定 15 分鐘,實質上拒絕了合法的 admin 登入——確認一項可用性風險。擷取是否存在任何緩解措施(IP 為基礎、驗證碼、告警);若無則記載為發現項。

### IT-AUTH-09 INT-104 端到端生命週期
- **Given** userD 在兩個用戶端登入(registry 中有兩個 active 更新權杖 R_a、R_b)。
- **When** userD 以有效的舊／新密碼呼叫 POST /api/v1/auth/change-password;然後兩個用戶端各以其各自的權杖呼叫 POST /api/v1/auth/refresh。
- **Then** change-password 回傳 ok 並帶 revokedSessions=2;後續兩次 /refresh 呼叫皆回傳 401 refresh_token_revoked(DB 中原因為 PASSWORD_CHANGED);新密碼可在新登入上運作,而舊密碼被拒絕。確認跨裝置 session 終止。

### IT-AUTH-10 INT-105 韌性/故障轉移
- **Given** OIDC 已啟用且授權 oidcSso 開啟;一個可使 token/userinfo 端點停滯並輪換其 JWKS 金鑰的 mock IdP。
- **When** (a) IdP token 端點延遲 > 15 秒;(b) IdP 回傳由尚未在快取 JWKS 中的 kid 簽署的 id_token;(c) IdP 省略 jwks_uri。
- **Then** (a) 回呼優雅失敗並 302 重新導向至 /login?reason=oidc_error(無懸置／500 外洩);(b) OidcService 強制重新整理 JWKS 一次並成功,若仍未知則乾淨拒絕;(c) 登入被拒絕(拒絕略過簽章)。一次性 state 必須被消耗(重放相同的 code+state 會失敗,'invalid or unknown OAuth2 state')。

### IT-AUTH-11 INT-107 API 合約
- **Given** 一個透過 clm_access cookie 驗證的瀏覽器 session;CSRF 使用 CookieCsrfTokenRepository(XSRF-TOKEN cookie)。
- **When** (a) 以 cookie 但不帶 X-XSRF-TOKEN 標頭 POST 一個會改變狀態的非 auth 端點(例如建立訂單);(b) 同上但帶有透過 GET /api/v1/csrf 預備好的有效權杖;(c) 同一請求改以 Bearer 標頭驗證(而非 cookie)且不帶 XSRF 權杖。
- **Then** (a) 回傳 403(CSRF 被拒絕);(b) 成功 2xx;(c) 成功,因為帶 Authorization 標頭的請求依 ignoringRequestMatchers 規則免於 CSRF。確認 CSRF 保護 cookie 流程但不破壞 API 用戶端。

### IT-AUTH-12 INT-108 併發/競態
- **Given** userE 持有更新權杖 R1;兩個 SPA 分頁同時偵測到存取權杖到期。
- **When** 在毫秒內以相同的 R1 同時發出兩個並行的 POST /api/v1/auth/refresh。
- **Then** 定義並驗證預期行為:理想情況下恰好一個成功而另一個得到 401(單一輪換),且 registry 絕不以不一致狀態結束(無孤立的 active 家族)。由於 revokeOne 與 register 是各自分離的操作,擷取兩者是否都能鑄造權杖(競態),以及唯一的 token_hash 約束或 jti 是否防止重複;將結果記載為通過/發現項。

### IT-AUTH-13 INT-106 功能整合
- **Given** 單一實例,auth.access.token.ttl.minutes=30 由 V15 植入。
- **When** admin PUT /api/v1/admin/settings/auth.access.token.ttl.minutes value=2,然後登入並解碼新存取權杖的到期時間;在第二個實例上,變更該值並檢查第一個實例的快取。
- **Then** 在同一實例上,新發行權杖的 exp 反映 2 分鐘(invalidate(key) 生效);在不同實例上,快取的值維持陳舊,直到該節點於本機失效——記載多節點快取一致性的限制。拒絕無效(非數值)的值,改回退至預設值。

### IT-AUTH-14 INT-109 端到端生命週期
- **Given** 一個空資料庫、prod 設定檔、CLM_JWT_SECRET 已設定。
- **When** 首次啟動應用程式,然後重新啟動它。
- **Then** 首次啟動會建立 'default' 群組與 admin@local,並以恰好一次(FIRST BOOT ONLY 橫幅)記錄一個 12 字元的隨機密碼,該帳號為 enabled、role=admin、加入 default 群組;該隨機密碼可成功登入。重新啟動時,不會建立新的 admin,且既有密碼維持不變(existsByEmail 短路)。臨時密碼絕不以明文持久化(clm_users 中只有 BCrypt 雜湊)。

---

## MFA / OIDC 進階

### IT-MFA-ADV-01 INT-110 安全
- **Given** 一位 mfa_enabled=true(已註冊 TOTP)的使用者 U,以及一位在 auth.mfa.required.for.admin=true 之下的 admin A;OIDC 已啟用,且 U/A 以本機使用者存在,因此它們可依電子郵件即時(JIT)比對。
- **When** U(以及 A)透過 stub IdP 完成 OIDC Authorization-Code 流程,且回呼 /api/v1/auth/oidc/callback 成功。
- **Then** 對於可銷售的產品,預期:回呼不應發行完全驗證的存取 JWT;它應要求一個 TOTP 步驟或被政策封鎖。實際(斷言並升級為缺陷):回呼回傳 302 並帶有效 auth cookie,且 /api/v1/auth/me 在無第二因素的情況下成功——證明 SSO 將 MFA 降級為單一因素。

### IT-MFA-ADV-02 INT-111 端到端生命週期
- **Given** 使用者 U 已註冊 TOTP,但其密鑰現已遺失(裝置遺失);auth.mfa.required.for.admin=true 且 U 是唯一已註冊的管理員。
- **When** U 登入(取得 mfaToken)但無法產生有效驗證碼;U 嘗試呼叫 /me/mfa/disable;某管理員嘗試透過任何管理 API 清除 U 的 MFA。
- **Then** mfa-verify 一律回 401 mfa_code_invalid;/me/mfa/disable 回 401(需要當下有效驗證碼);UserController 並未提供任何重設 MFA 的端點 -> U 永久遭鎖死。本測試斷言不存在任何復原途徑,並將其標記為發行阻擋項。

### IT-MFA-ADV-03 INT-114 安全
- **Given** OIDC 已啟用,對接一個樁 IdP;一次合法的 authorize 起始建立了一筆 clm_oidc_state 資料列。
- **When** (a) 以未知/偽造的 state 進行 callback;(b) 重放一個已被消耗過一次的 state;(c) callback 的 id_token nonce 與儲存的 nonce 不同;(d) 以一個已過期(>5 分鐘)的 state 進行 callback。
- **Then** (a)『invalid or unknown OAuth2 state』-> 302 /login?reason=oidc_error;(b) 第二次使用被拒絕(資料列在第一次後即刪除);(c)『id_token nonce mismatch』拒絕;(d)『state expired』拒絕 + 刪除資料列。四者皆必須重導至登入錯誤頁,絕不發出 JWT。

### IT-MFA-ADV-04 INT-115 安全
- **Given** oidc.verify.idtoken.signature=true(預設);一份帶有已知金鑰對的測試 JWKS。
- **When** callback 收到以下 id_token:(a) 以不在 JWKS 內的攻擊者金鑰簽署;(b) alg=none / 未簽署;(c) header 的 kid 即使在強制刷新一次後仍不在 JWKS 內;(d) 正確簽署但 exp 已超過 60 秒容差而過期。
- **Then** (a)-(d) 全部被拒絕(『signature verification failed』/『JWKS kid not found』/『id_token expired』);只有正確簽署、未過期且 nonce/aud/iss 相符的 token 才被接受。另斷言 oidc.verify.idtoken.signature=false 會危險地接受偽造 token(記錄此關閉開關)。

### IT-MFA-ADV-05 INT-112 負向/邊界
- **Given** 使用者 U 的 mfa_enabled=true;測試已知 TOTP 密鑰與時鐘,因此可計算出有效的當下驗證碼 C。
- **When** 流程 1:login -> mfaToken1 -> mfa-verify(mfaToken1, C) 成功。緊接著(同一個 30 秒步進、<90 秒)流程 2:login -> mfaToken2 -> mfa-verify(mfaToken2, C),重用驗證碼 C。
- **Then** 預期:驗證碼 C 在第二次使用時應被拒絕(強制單次使用)。實際(斷言 + 標記):兩次驗證皆成功,因為不存在已消耗驗證碼/最後計數器的追蹤 -> 攔截到的驗證碼在視窗內可重放。

### IT-MFA-ADV-06 INT-113 併發/競態
- **Given** 使用者 U 的 mfa_enabled=true;已發出一個 mfaToken;auth.mfa.verify.max.attempts=5;速率限制容量設得夠高以免遮蔽本測試。
- **When** 以同一個 mfaToken 與錯誤驗證碼觸發約 20 個並行的 mfa-verify 請求(多執行緒猜測)。
- **Then** 在 pending 資料列被刪除前,被接受的錯誤驗證碼驗證總數必須 <=5。實際風險:attempt_count 的非原子讀取-修改-寫入,讓交錯的請求得以在該 token 失效前對單一 token 執行遠多於 5 次的驗證。

### IT-MFA-ADV-07 INT-116 權限/多租戶隔離
- **Given** OIDC 已啟用,oidc.jit.provision=true、oidc.jit.default.role=user、oidc.jit.default.group=groupX;一個全新的 IdP email 不存在於 clm_users。
- **When** 一位首次使用者完成 SSO;接著第二種情況將 oidc.jit.default.role=admin 並重複進行。
- **Then** 情況 1:新建一筆 clm_users 資料列,role=user、密碼哨兵值『__OIDC_PROVISIONED_NO_LOCAL_PASSWORD__』(無法以密碼登入)、連結至 groupX、JWT 範圍僅限 groupX;對另一群組資源進行群組範圍的 GET 回 403/空。情況 2:斷言 role=admin 確實被套用(記錄此過度授權的隱患),且無效的角色字串會回退為 user。

### IT-MFA-ADV-08 INT-117 負向/邊界
- **Given** OIDC 已啟用;userinfo 回應為:(a) email_verified=false;(b) 沒有 email 宣告;(c) email 對應到一個 enabled=false 的本機使用者;(d) email 存在但缺少 email_verified 宣告。
- **When** 處理每一個 callback。
- **Then** (a) 被拒絕『email not verified』;(b) 被拒絕『IdP did not return email』;(c) 被拒絕『user disabled』;(d) 記錄其『被接受』,因為 email_verified 預設為 true(asBoolean(true)) — 標記為一項強化缺口。

### IT-MFA-ADV-09 INT-118 韌性/故障轉移
- **Given** OIDC 已啟用;一個可被弄得緩慢/無法使用、且可輪替其 JWKS 簽署金鑰的 mock IdP。
- **When** (a) token_endpoint 回 5xx / 緩慢超過逾時;(b) userinfo 回 401;(c) IdP 輪替其金鑰使 id_token 的 kid 不在快取的 JWKS 內(強制刷新一次);(d) discovery 端點停機。
- **Then** (a)/(b)/(d) callback 攔截錯誤並 302 -> /login?reason=oidc_error 並帶安全訊息,不發 JWT、不留半成形工作階段;(c) keyLocator 中那次強制的 JWKS 刷新成功,登入完成。另斷言請求執行緒不會超出所設定的 10-15 秒逾時而懸掛。

### IT-MFA-ADV-10 INT-119 功能整合
- **Given** 已驗證的使用者 U,mfa_enabled=false。
- **When** POST /me/mfa/setup 回傳 {secret, otpauthUrl};(a) 以由 secret 計算出的正確 TOTP 驗證碼進行 verify-and-enable;(b) 另一次以錯誤驗證碼嘗試;(c) 在已啟用的情況下再次 setup。
- **Then** (a) 200、mfa_enabled=1、mfa_secret_enc 以 enc_v2: 前綴儲存(斷言它『不是』明文 Base32 且能解密回去)、寫入 MFA_ENABLED 稽核事件;(b) 401 code_invalid,不持久化任何東西;(c) 409 already_enabled。接著下一次 /auth/login 回 202 mfaRequired。

### IT-MFA-ADV-11 INT-121 資料遷移
- **Given** 一個基線於 V35 的資料庫(clm_mfa_pending『不含』attempt_count),然後向前遷移直到 V82。
- **When** 在 V35 時代的 schema 上執行完整的 Flyway 集,接著進行一次真實的 MFA 登入(login -> mfa-verify,先錯誤再正確驗證碼)。
- **Then** V82 新增 attempt_count 預設 0;錯誤驗證碼路徑會將其遞增而不出錯;正確驗證碼路徑發出 JWT。模擬半套升級(未跑 V82)必須顯示 Hibernate 讀/寫 attempt_count 失敗(500),證明執行順序至關重要。

### IT-MFA-ADV-12 INT-122 安全
- **Given** OIDC 已啟用並對接一個可控 IdP;前端在登入後重導時讀取『#oidc=』片段。
- **When** (a) login?returnUrl=//evil.com、returnUrl=/\evil.com,以及一個絕對的 http URL;(b) IdP 回傳一個含引號 / 類腳本酬載的 displayName。
- **Then** (a) safeReturnUrl 將所有站外形式收斂為『/』(斷言 302 Location 的 host 為同源);(b) 記錄 displayName 在被 URL 編碼進片段前僅做引號跳脫,並斷言 SPA 如何消費它 — 一個未經編碼的接收點即為發行阻擋級的 XSS。

### IT-MFA-ADV-13 INT-120 可觀測性
- **Given** 使用者 U 的 mfa_enabled=true;auth.mfa.verify.max.attempts=5。
- **When** login -> 202 mfaToken(斷言 LOGIN_MFA_PENDING 稽核 + pending 資料列);mfa-verify 正確(斷言 LOGIN_MFA_VERIFIED + 刪除資料列 + JWT);另外讓一個 pending 過期 >5 分鐘後再 mfa-verify(斷言 mfa_token_expired + 刪除資料列);執行 cleanupExpired 並斷言過期資料列被清除。
- **Then** 稽核事件在 HMAC 鏈中依序出現;pending 資料列完全依規格建立/刪除;過期 token 絕不產生 JWT;此 cron 會降低 clm_mfa_pending 的資料列數。

---

## RBAC / 多租戶隔離進階

### IT-RBAC-01 INT-123 安全
- **Given** 群組 A 有 supervisorA 與一個 ACME 帳號 accA,該帳號有一個 ISSUED 的 order ordA(CN a.example.com);群組 B 有 supervisorB,且不屬於 A。全部直接植入 clm_groups/clm_user_groups/acme_accounts/acme_orders。
- **When** supervisorB(有效 JWT、role=supervisor、groups=[B])以真實 ID(透過旁路取得)呼叫 GET /api/v1/twca/accounts/{accA}、GET /api/v1/twca/accounts/{accA}/orders/{ordA}、GET /api/v1/twca/accounts/{accA}/orders/{ordA}/certificate,以及 DELETE /api/v1/certs/{ordA}。
- **Then** 每一次呼叫都回 HTTP 404 並帶錯誤『account_not_found』/『order_not_found』(隱藏存在性),憑證 PEM『不會』被回傳,order『不會』被刪除,且 acme_orders 中無任何資料列被變更。在適用之處會為被拒的嘗試產生一筆稽核項。

### IT-RBAC-02 INT-124 安全
- **Given** clm_dns_provider_account 中有群組 A 的帳號(type=cloudflare、zones=[a.example.com])與群組 B 的帳號(type=route53、zones=[b.example.com]);一個普通『user』角色的 userA 僅屬於群組 A。
- **When** userA(role=user、groups=[grpA])呼叫 GET /api/v1/admin/dns-providers。
- **Then** 預期(要朝其建構的正確行為):回應『僅』含群組 A 的連接器。今日實際:回應包含群組 B 的顯示名稱、type、受管 zones 與委派 zone(憑證已遮罩)。本測試斷言回應『絕不可』包含任何 group_id 在 userA 的 visibleGroupIds 之外的連接器;若失敗即為一個已確認的、應提報的跨租戶資料洩漏缺陷。

### IT-RBAC-03 INT-126 權限/多租戶隔離
- **Given** supervisorA 僅在群組 A;群組 B 存在且有一個成員 userB。
- **When** supervisorA 嘗試:(a) POST /api/v1/admin/users 帶 role=admin;(b) POST /api/v1/admin/users 帶 groupIds=[grpB];(c) PATCH /api/v1/admin/users/{userB}(一個群組 B 的使用者)變更 displayName;(d) PATCH /api/v1/admin/users/{ownGroupUser} 帶 role=supervisor。
- **Then** (a) 400『supervisor can only create role=user』;(b) 400『you can only assign your own groups』;(c) 400『user not found』(群組 B 使用者不可見);(d) 400『only admin can change role』。clm_users 或 clm_user_groups 中無任何資料列反映所嘗試的提升。

### IT-RBAC-04 INT-129 權限/多租戶隔離
- **Given** 群組 A 有 userU1 與 userU2(皆 role=user)外加 supervisorA。Orders:ord1 assignee=U1、ord2 assignee=U2,兩者皆群組 A、皆 ISSUED。
- **When** userU1 呼叫 GET /api/v1/certs、GET /api/v1/certs/dashboard、GET /api/v1/certs/domains,以及 GET /api/v1/certs/domains/{ord2-CN};接著 supervisorA 呼叫同一個清單。
- **Then** userU1 的清單/儀表板計數僅含 ord1(不含 ord2);GET domains/{ord2-CN} 對 U1 回 404 domain_not_found;supervisorA 的清單同時包含 ord1 與 ord2。U1 的儀表板『total』僅反映其自身被指派的憑證。

### IT-RBAC-05 INT-264 權限/多租戶隔離
- **Given** userA 已通過認證(JWT cookie)且僅屬於群組 A;ordB 屬於群組 B
- **When** userA 使用 ordB 的真實 id 呼叫 GET /api/v1/twca/accounts/{accB}/orders/{ordB}、GET .../certificate 與 POST .../{ordB}/trigger
- **Then** 每個呼叫皆回傳 404,內文為 {error:account_not_found|order_not_found}(「不是」403,以避免揭露存在性)——由 requireAccountAccess→AccountMismatchException 驅動;沒有外洩任何憑證 PEM、status 或 DNS 資料;由 userB(在群組 B、受指派人)進行的相同呼叫則成功(200)

### IT-RBAC-06 INT-265 權限/多租戶隔離
- **Given** userA1 以 role=user 身分通過驗證,隸屬於 Group A
- **When** userA1 呼叫 GET /api/v1/certs、GET /api/v1/certs/dashboard、GET /api/v1/certs/export.xlsx,以及 GET /api/v1/twca/accounts/{accA}/orders/{ordA2}
- **Then** 清單／儀表板／匯出僅包含 ordA1 而排除 ordA2(assigneeFilterUserId() 將 user 限制為 assignee==self);直接 GET ordA2 回傳 404(requireOrderAccess 的指派人檢查)。接著 supA 重複該清單操作,可同時看到 ordA1 與 ordA2(supervisor:群組層級、不受指派人限制)

### IT-RBAC-07 INT-266 權限/多租戶隔離
- **Given** supA 以 supervisor 身分通過驗證,範圍限定於 Group A
- **When** supA 呼叫 GET /api/v1/admin/cert-issuance-requests、/admin/revoke-requests、/admin/binding-requests;接著 POST .../cert-issuance-requests/{cirB}/approve 與 .../revoke-requests/{rvkB}/approve
- **Then** 三個清單端點皆略過 Group B 的列(由 canAccess→canAccessGroup 過濾);每一次跨群組核准回傳 404「request not found」(findAccessiblePending／canAccess 採行 fail-closed)。admin(對照組)可看到並可對三者執行操作。被拒絕的嘗試不會觸發任何 CA 呼叫或 DB 狀態變更

### IT-RBAC-08 INT-271 權限/多租戶隔離
- **Given** adminX 是 cirSelf 的申請人,同時也是核准人
- **When** adminX 呼叫 POST /api/v1/admin/cert-issuance-requests/{cirSelf}/approve(自我核准嘗試);接著 adminY 核准同一個請求
- **Then** adminX 的自我核准回傳 409 conflict,訊息為「SoD 職責分離:申請人不可核准自己的申請」,且該 CIR 維持 PENDING、狀態不變、未建立任何訂單(從未抵達 markApprovedIfPending)。adminY 的核准成功(CAS PENDING→APPROVED)。對照:對於 REVOKE 請求,admin 的自我核准「是」被允許的(selfApproved=true)——也要斷言此非對稱性

### IT-RBAC-09 INT-272 安全
- **Given** 一張 ISSUED 訂單,其私鑰僅能透過已核准的一次性 JIT 流程離開系統
- **When** (1) userA1 POST /api/v1/download-requests format=domain-key;(2) userA1 POST format=pfx;(3) supA 提交 format=pfx 後 supA 自我核准;(4) supA 核准後,userA2(非申請人)呼叫 GET /api/v1/download-requests/{id}/download
- **Then** (1)+(2) 回傳 403「pfx/domain-key 僅限主管申請」(user 角色被阻擋於帶鑰格式之外);(3) supA 的自我核准回傳 409 SoD(非 admin 不可自我核准下載)——僅 ADMIN 可緊急自我核准;(4) userA2 的下載回傳「this download is for the original requester only」(申請人綁定),而在 15 分鐘 TTL 之後的下載嘗試、或對已取用連結的第二次使用回傳 409。成功路徑會發出 SECRET_DOWNLOADED 稽核事件

### IT-RBAC-10 INT-125 安全
- **Given** userX 以 role=admin 登入並持有一個有效的 30 分鐘存取 token;接著某管理員 PATCH /api/v1/admin/users/{userX} 將 role 設為 user。
- **When** userX 在『不』刷新的情況下,以仍然有效的存取 token 呼叫一個僅管理員可用的端點(例如 POST /api/v1/admin/groups 或 POST /api/v1/admin/dns-providers);接著另外 userX 呼叫 /api/v1/auth/refresh 並重試。
- **Then** 刷新前的呼叫『成功』(展現此陳舊視窗 — 必須加以記錄與設限);刷新後的呼叫回 403 forbidden,因為重新發出的 token 帶有 role=user。本測試斷言此視窗以存取 TTL 為界,且 revoke-sessions 會立即關閉它(在 revoke-sessions 之後,refresh 回 401 refresh_token_revoked)。

### IT-RBAC-11 INT-127 權限/多租戶隔離
- **Given** 群組 A 的 CN a.example.com 有一個 PENDING 的 DnsBindingRequest(由 supervisorA 建立)。supervisorB 僅屬於群組 B。
- **When** supervisorB 呼叫 GET 取得待處理的 DNS 綁定佇列,接著對群組 A 的請求 id(透過旁路取得)POST approve 與 POST reject。
- **Then** supervisorB 的佇列清單『不』包含 a.example.com 的請求;approve/reject 皆回『request not found』(IllegalArgumentException -> not found);該 DnsBindingRequest 狀態維持 PENDING,且沒有任何 DNS_BINDING_CHANGE_APPROVED/REJECTED 稽核事件將 supervisorB 列為審批者。

### IT-RBAC-12 INT-128 負向/邊界
- **Given** 群組 A 擁有 CN a.example.com,帶有一個 DnsBinding、一個 AutoRenewPolicy 與一個 IMPORTED 的 order;supervisorA 可看到/編輯兩者。
- **When** 管理員(或 supervisorA)透過 DELETE /api/v1/certs/{orderId} 刪除該 CN 唯一的 IMPORTED order,接著 supervisorA 呼叫 GET/PUT /api/v1/autorenew/policies/a.example.com 並 GET 該 CN 的 DNS 綁定。
- **Then** supervisorA 現在對該政策與綁定收到『not found』(因為已無 order 可據以推導群組存取權),而管理員仍能看到它們。本測試斷言並記錄此行為為一個已知的隔離邊界,以便產品決定是否要給 CN 資源一個原生 group_id;並驗證該資源『不會』在無擁有者下默默自動續約。

### IT-RBAC-13 INT-130 安全
- **Given** 一個範圍化的 API key『k1|groups=grpA』(解析為 role=supervisor、scoped=true)與一個 legacy 純 key『k2』(role=admin、scoped=false)。群組 A 與 B 中皆有帳號。
- **When** 以 X-API-Key=k1:GET /api/v1/twca/accounts(應僅列 grpA)、POST /api/v1/admin/groups(僅管理員),以及 GET grpB 中的某帳號。以 X-API-Key=k2:同樣的呼叫。
- **Then** k1:帳號清單僅含 grpA 帳號;POST /admin/groups -> 403 forbidden(ROLE_SUPERVISOR 不通過 hasRole ADMIN);grpB 帳號 -> 404。k2:全部成功(記錄 legacy key 的全域管理員波及範圍,斷言發出了啟動警告並建議使用範圍化 key)。

### IT-RBAC-14 INT-132 功能整合
- **Given** 群組 A 有 1 位使用者與 1 個 ACME 帳號;群組 C 為空。
- **When** 管理員呼叫 DELETE /api/v1/admin/groups/{grpA},接著 DELETE /api/v1/admin/groups/{grpC}。
- **Then** grpA 刪除回 409 group_not_empty 並帶計數 {users:1, accounts:1},且群組資料列保留;grpC 刪除回 200 deleted:true。將 grpA 的使用者與帳號移出後,對 grpA 重試成功。確認沒有任何路徑會讓帳號/order 帶著懸空的 group_id 而被遺落(那會使它們變為僅管理員可見)。

### IT-RBAC-15 INT-133 權限/多租戶隔離
- **Given** userU1 在隸屬群組 A 時登入(token 的 groups claim=[grpA]);群組 B 內有帳號 accB 並含訂單 ordB。
- **When** 管理員以 PATCH 為 userU1 加入 grpB(clm_user_groups 新增一筆)。在 U1 沒有重新登入或重新整理 token 的情況下,U1 呼叫 GET /api/v1/certs 與 GET /api/v1/twca/accounts。
- **Then** U1 在下一次請求時立即看到它身為承辦人的群組 B 資源(若為 supervisor,則為群組 B 的全部),證明 visibleGroupIds 讀取的是即時 DB 而非過時的 token claim;反之,將 U1 從 grpA 移除後,無需登出即立即隱藏 grpA 的資源。

### IT-RBAC-16 INT-134 資料遷移
- **Given** 一份 V14 之前的快照,內含:一個 group_id 為 NULL 的帳號;若干 group_id 為 NULL 且 assignee 為 NULL 的訂單(部分有 created_by、部分沒有,包含一筆由自動續期/API-key 建立的訂單);一個沒有任何啟用使用者的群組;以及存在的 'default' 群組。
- **When** 在該快照上先執行 V14 再執行 V21 遷移,然後重新執行 V14(冪等性檢查)。
- **Then** 每個帳號/訂單最終都具備非空的 group_id(無法解析者落到 default);有 created_by 的訂單取得 assignee=created_by;其餘者僅在其自身群組的啟用使用者範圍內輪流分派(絕不取用其他群組的使用者);沒有啟用使用者的群組則退而採用第一位 admin(文件記為 3c)——須斷言此退路絕不取用不同群組的使用者;V21 回填帳號 assignee=created_by;第二次執行 V14 不造成任何變更(冪等且具決定性,ORDER BY 以 order_id 打破平手)。

### IT-RBAC-17 INT-267 權限/多租戶隔離
- **Given** 一筆其推導出的 groupId 為 null 的資源
- **When** supX 呼叫 GET /api/v1/admin/cert-issuance-requests 與 GET /api/v1/twca/accounts/{accNull}/orders/{ord};接著 admin 呼叫相同端點
- **Then** supX 既看不到待處理請求,也看不到該訂單(canAccessGroup(null) 對非 admin 回傳 false)→ 404／被略過;admin 兩者皆可見(canAccessGroup(null) 對 admin／service 為 true)。這證明了「針對 QA R6 P3」所加入的 fail-closed 邊界

### IT-RBAC-18 INT-268 安全
- **Given** userA 以僅隸屬於 Group A 的一般 user 身分通過驗證
- **When** userA 呼叫 GET /api/v1/admin/dns-providers
- **Then** 記錄 AS-IS 缺口:回應目前同時回傳 cfA 與 r53B,包含 type、displayName、managedZones、delegationZone、groupId(憑證已遮罩)——屬於跨租戶拓撲揭露／IDOR。修正後所期望／應斷言的行為是:清單應依群組過濾(userA 僅看到 cfA)或限定為 ADMIN。測試在目前的建置上必須「失敗」以標示此揭露,之後再守護修正

### IT-RBAC-19 INT-269 安全
- **Given** userA 以無 Group B 成員資格的 user 身分通過驗證
- **When** userA 呼叫 GET /api/v1/dns-bindings/auto-detect?fqdn=host.secret-b.example
- **Then** 記錄 AS-IS:matched=true,並帶有 Group B 連接器的 dnsProviderAccountId、displayName、type 與 zone——外洩出 Group B 管理該 zone 一事,以及是哪個連接器。修正後應斷言:此端點必須要求呼叫者具備所解析出之 zone 群組的存取權(否則對非擁有者回傳 404 matched=false)

### IT-RBAC-20 INT-270 權限/多租戶隔離
- **Given** userA1(role=user)可「讀取」cnA(自己為指派人)但不得變更基礎設施
- **When** userA1 呼叫 PUT /api/v1/dns-bindings/{cnA} 與 PUT /api/v1/autorenew/policies/{cnA};接著 supA 呼叫 PUT /api/v1/dns-bindings/{cnB}(另一個群組的 CN)
- **Then** userA1 的 PUT 回傳 403(角色閘門:僅 ADMIN/SUPERVISOR 可設定綁定／政策)——注意 userA1 對 cnA 的 GET 仍可運作(讀取被允許)。supA 對 cnB 的寫入回傳 404(CnAccessGuard.canAccessCn 為 false:cnB 沒有 Group A 的訂單),確認在角色閘門之上再疊加的跨群組寫入隔離

### IT-RBAC-21 INT-273 權限/多租戶隔離
- **Given** supA 以 Group A 的 supervisor 身分通過驗證
- **When** supA 呼叫 GET /api/v1/admin/users(清單);PATCH /admin/users/{userB}(跨群組編輯);PATCH /admin/users/{userA1} 並帶 role=supervisor(提權);POST /admin/users 並帶 role=admin;POST /admin/users/{userB}/revoke-sessions
- **Then** 清單略過 userB(僅可見自己群組的成員);PATCH/{userB} 與 revoke-sessions/{userB} 回傳 400/404「user not found」(跨群組 fail-closed);將 userA1 提權回傳 400「only admin can change role」;建立 admin 回傳 400「supervisor can only create role=user」。admin 對照組則全部通過

### IT-RBAC-22 INT-275 權限/多租戶隔離
- **Given** supA 想要交接 ordA
- **When** supA PATCH /api/v1/twca/accounts/{accA}/orders/{ordA}/assignee 並帶 assigneeUserId=userB(群組外);接著帶 assigneeUserId=userA2(群組內);另外 supB 對 ordA 嘗試相同的移轉
- **Then** 移轉至 userB 回傳 400「target user must be in the same group as the cert」(防止建立一張其群組內無人可見的憑證);移轉至 userA2 成功(200)並寫入 ASSIGNEE_TRANSFERRED 稽核事件;supB 對 ordA 的嘗試回傳 404(在抵達移轉之前即被跨群組帳戶存取拒絕)。createdBy 維持不可變

### IT-RBAC-23 INT-276 安全
- **Given** 兩個範圍劃分不同的 service principal
- **When** 以 X-API-Key=keyScoped 呼叫 GET /api/v1/certs 與一個 Group B 訂單的 GET;接著以 keyScoped 呼叫 POST /api/v1/admin/dns-providers(僅限 ADMIN 的寫入);以 keyLegacy 重複該 Group B 讀取與該 admin 寫入
- **Then** keyScoped 僅看到 Group A 的憑證(filterGroupIds 回傳 ['A']),對 Group B 訂單取得 404,對僅限 ADMIN 的 DNS-provider 建立取得 403(絕不提升為 admin)。keyLegacy 看到所有群組(filterGroupIds 為 null)且可執行 admin 寫入。確認範圍化 key 永遠無法觸及僅限 admin 的端點,亦無法跨越其群組集

### IT-RBAC-24 INT-277 權限/多租戶隔離
- **Given** 稽核列不帶 group_id,因此在設計上無法進行依群組過濾
- **When** supX 呼叫 GET /api/v1/audit 與 GET /api/v1/audit/export.xlsx;supX 呼叫 GET /api/v1/audit/verify-integrity;admin 呼叫全部三者
- **Then** supX 的 /audit 清單與 /audit/export.xlsx 回傳 403(hasRole ADMIN)——supervisor 絕不可看到跨群組的 SECRET_DOWNLOADED 事件 + CN;supX 的 /audit/verify-integrity「成功」(hasAnyRole ADMIN,SUPERVISOR——完整性檢查刻意不綁定群組)。admin 可執行全部三者

### IT-RBAC-25 INT-278 權限/多租戶隔離
- **Given** userA 以一般 user(最低角色)身分通過驗證
- **When** userA 發動寫入／admin 矩陣:POST /api/v1/admin/endpoints;POST /api/v1/twca/accounts;POST /api/v1/providers;POST /api/v1/admin/policies;POST /api/v1/admin/license/activate;POST /api/v1/certs/import;POST /api/v1/admin/scan/endpoints/{epA};POST /api/v1/admin/scan/discoveries/{discA}/import;GET /api/v1/admin/dns-providers/types
- **Then** 全部回傳 403(各由 hasRole ADMIN 或 hasAnyRole ADMIN,SUPERVISOR 守衛)——user 角色無法建立 endpoints/accounts/CA-providers/policies、無法啟用授權、無法匯入憑證、無法觸發或匯入掃描,也無法讀取 DNS 外掛的 type schema。對照:同一使用者被允許的讀取(GET /api/v1/certs、GET /api/v1/auth/me)回傳 200

### IT-RBAC-26 INT-131 併發/競態
- **Given** 群組 A 中的 ord1 assignee=U1;管理員在 U1 正輪詢 GET /api/v1/certs 的同時將 ord1 轉移給 U2(亦在群組 A)。
- **When** 並行觸發 PATCH /orders/{ord1}/assignee {assigneeUserId:U2}(N 條執行緒),同時以 U1 與 U2 身分重複 GET /api/v1/certs。
- **Then** 恰好一個最終的 assignee_user_id(U2)持久化,且除了真實轉換外沒有重複的 ASSIGNEE_TRANSFERRED 稽核;轉移完成後,U1 的清單不再顯示 ord1 而 U2 的會顯示;該 order 絕不落到群組 A 之外的使用者上。無死結/500。

### IT-RBAC-27 INT-274 權限/多租戶隔離
- **Given** userAB 是群組 A 與 B 的 supervisor,但非 C 的
- **When** userAB 呼叫 GET /api/v1/certs 與 GET /api/v1/twca/accounts/{accC}/orders/{ordC}
- **Then** 憑證清單包含 ordA 與 ordB(兩個成員資格的聯集)並排除 ordC;直接 GET ordC 回傳 404。這證明 filterGroupIds() 回傳成員資格的聯集,且該聯集不會滲漏到第三個租戶

---

## Secret 加密 / HSM / SSRF

### IT-HSM-01 INT-135 功能整合
- **Given** 一個運行中的應用程式,已設定 CLM_ENCRYPTION_KEY,並已註冊一個 ACME 帳號(acme_accounts 列),外加一筆含已產生網域金鑰的訂單(acme_orders.domain_key_pem)
- **When** 以原始 JDBC SELECT 讀取 account_key_pem 與 domain_key_pem(繞過 converter),然後透過 JPA repository 載入相同的列
- **Then** 原始欄位值以 'enc_v2:' 開頭且為有效的 base64(IV‖ct‖tag),其中不含 '-----BEGIN';JPA 載入的 entity 暴露原始明文 PEM;竄改一個密文位元組後再載入會擲出 'decrypt failed'(GCM 驗證)

### IT-HSM-02 INT-136 資料遷移
- **Given** 預先植入的 acme_accounts/acme_orders,內含:(a) 明文 '-----BEGIN' PEM、(b) 以 SHA-256(secret) 製作的 enc_v1: 密文、(c) 一個刻意無法解密的垃圾值
- **When** 啟動應用程式使 KeyEncryptionMigrationRunner 執行,然後再重啟第二次
- **Then** 列 (a) 與 (b) 變成 enc_v2: 且解密後明文完全相同;列 (c) 維持不變並僅產生一筆 WARN,且不會中止交易;第二次開機記錄為無操作(全為 v2)且不造成任何變更

### IT-HSM-03 INT-137 韌性/故障轉移
- **Given** 一個以密鑰 A 加密的已填充 DB(帳號/網域金鑰、端點與 DNS 憑證的 enc_v2: 列)
- **When** 將 CLM_ENCRYPTION_KEY 改為 B 後重啟應用程式(未執行任何重新加密)
- **Then** 讀取任何密鑰皆擲出 'decrypt failed';此失敗可被觀察且受到控制(例如帳號金鑰下載回傳 5xx、DNS 健康檢查失敗),而非靜默回傳損毀/空白的材料;產品文件記載變更密鑰需要(目前尚不存在的)重新加密程序

### IT-HSM-04 INT-138 安全
- **Given** 一個已認證的 SUPERVISOR(或 ADMIN)連線,並具備建立端點的權限
- **When** 建立一個 connection.host=169.254.169.254 的 F5 端點(或一個 webhook,其主機名稱在驗證時解析為公開 IP,隨後重新綁定到 169.254.169.254)並觸發 testConnect/deploy;另外建立一個 url=http://169.254.169.254/latest/meta-data/ 的 webhook
- **Then** 指向字面 metadata IP 的 webhook/k8s 被 SafeOutboundUrl 拒絕(IllegalArgumentException),但 F5/SSH/DNS 供應商呼叫卻能觸及 metadata/內部位址,因為那些程式路徑從未調用 validate()——此不對稱性必須在銷售前修補(預期修正方案會讓所有 driver 出向流量都經過防護,並在連線時進行 IP 釘定)

### IT-HSM-05 INT-139 合規強制
- **Given** clm_settings 中 private-key.export.forbidden=true、一筆含網域金鑰的已簽發訂單、以及一個含金鑰的 ACME 帳號
- **When** 以 ADMIN 身分呼叫:GET 訂單下載 format=domain-key 與 format=pfx;GET /accounts/{id}/key.pem;GET /accounts/{id}/backup.json(無 passphrase);GET /admin/dr/account-keys(無 passphrase);然後以 ?passphrase=Strong1! 重複 backup.json 與 dr
- **Then** 所有明文私鑰回應皆回傳 409(透過 handler 的 IllegalStateException);僅含憑證的格式(leaf/fullchain/chain/der/cer)仍成功;帶 passphrase 的變體成功並回傳一個 PassphraseBox 封套(encrypted:true),其可用該 passphrase 解回、而以錯誤的 passphrase 則失敗

### IT-HSM-06 INT-140 災難復原/備份
- **Given** Node-1 含數個帳號(group_id、createdBy、assignee 各異),以密鑰 A 加密
- **When** GET /admin/dr/account-keys?passphrase=P(封套);在一個以密鑰 B 設定的全新 Node-2 上,以封套+passphrase 先 dryRun=true 再 dryRun=false POST /admin/dr/account-keys/restore;然後以非 admin 使用者登入並列出帳號
- **Then** Dry-run 正確回報 wouldCreate/wouldUpdate;實際還原重建帳號,其 accountKeyPem 以 B 重新加密(原始欄位為 enc_v2:,解密為原始 PEM);createdBy/assignee/group_id 皆還原,使該使用者的 RBAC 可見性與 Node-1 完全相同;使用還原金鑰的 ACME 操作對 CA 成功

### IT-HSM-07 INT-141 授權強制
- **Given** 三種授權狀態:dev-bypass、Standard token(featHsmSupport=false)、Enterprise token(featHsmSupport=true);hsm.enabled 切換為 true 並設有 SoftHSM 函式庫路徑
- **When** 以 ADMIN 在每種狀態下 GET /api/v1/admin/hsm/status;以 SUPERVISOR/USER GET 相同端點;驗證用來加密一個新存的金鑰的作用中 KeyProtector 之 backendName()
- **Then** Standard → {licensed:false, enabled:false} 且不嘗試連線;Enterprise+hsm.enabled+SoftHSM 存在 → {licensed:true, enabled:true, available:true};Enterprise 但無函式庫 → available:false 並附錯誤訊息;非 admin → 403;在所有情況下,新寫入的密鑰皆維持 'software:AES-256-GCM/PBKDF2'(HSM 僅供連線檢查,並非加密後端)

### IT-HSM-08 INT-142 安全
- **Given** 透過 API 建立一個 F5 端點(credentials.password)、一個 SSH 端點(credentials.privateKey)、以及一個 DNS 供應商帳號(token map)
- **When** 讀取 endpoints/dns_provider_accounts 的原始欄位;GET 清單/明細 API 視圖;在不重送憑證的情況下更新一個端點
- **Then** credentials_encrypted 欄位為 enc_v2:(無明文 password/key/token);API 視圖僅回傳 hasCredentials/遮罩欄位(DnsAccountService.toView)且絕不含密鑰;不帶憑證更新時保留既有密文(不會意外清空)

### IT-HSM-09 INT-143 併發/競態
- **Given** 一個跨執行緒共享的單一 CredentialCipher bean(一把 AES 金鑰),如同生產環境
- **When** 在執行緒池中對相異明文執行 N=500 次並行 encrypt() 後接 decrypt(),並同時演練 DnsAccountService 的 encryptCreds/decryptCreds
- **Then** 每個值都來回轉換回其原始值;所有密文都具備唯一的 12 位元組 IV(無碰撞);無因 Cipher 實例共享而產生的例外(每次呼叫須使用新的 Cipher),且無跨執行緒明文外洩

### IT-HSM-10 INT-144 權限/多租戶隔離
- **Given** 由兩位 supervisor 設定、位於不同群組且具不同主機/憑證的兩個 F5 端點
- **When** 在 5 分鐘 token TTL 內對兩者觸發 testConnect/deploy,包含兩個端點共用同一個由 id 衍生的快取鍵邊界或同一主機的情境
- **Then** TOKEN_CACHE/CLIENT_CACHE 項目以端點 id 為鍵(client 另以 host/port/TLS 為鍵),使端點 A 的 F5 認證 token 絕不被端點 B 重用;輪替端點 B 的憑證會使其 client 失效/重建;在 401 重試路徑上無跨端點 token 重用

### IT-HSM-11 INT-145 負向/邊界
- **Given** 一個由 PassphraseBox.seal 產生的加密 DR/備份封套
- **When** 以下列情形 POST 還原:(a) 正確的 passphrase、(b) 錯誤的 passphrase、(c) 密文中翻轉一個位元組、(d) 缺少 passphrase 但 encrypted:true、(e) 降低 iterations 欄位
- **Then** (a) 解密並還原;(b)/(c) → 400 'envelope decrypt failed'(GCM 驗證失敗),無部分寫入;(d) → 400 'passphrase missing';(e) 因衍生金鑰不同而解密失敗——絕不靜默接受

### IT-HSM-12 INT-146 安全
- **Given** 三種開機設定:(a) prod profile、密鑰未設定;(b) prod、ACME_DEV_INSECURE_CRYPTO=true、密鑰未設定;(c) dev profile、密鑰未設定
- **When** 在每種設定下啟動應用程式
- **Then** (a) 以 IllegalStateException 快速失敗並指明 acme.security.endpoint-secret;(b) 必須仍拒絕在生產環境使用硬編碼的 dev 金鑰(devInsecureFallback 不應在 prod profile 中靜默啟用等同明文的加密)——斷言它不會用 dev 金鑰開機;(c) 以 dev 金鑰開機並附一筆明顯的 WARN

### IT-HSM-13 INT-147 效能/負載
- **Given** acme_orders 植入 5 萬列舊版 v1/明文網域金鑰
- **When** 冷啟動應用程式並計時 KeyEncryptionMigrationRunner.migrate()
- **Then** 遷移在可接受的範圍內完成,且不會在整個執行期間持有單一的整表鎖(每列 200k 迭代的 PBKDF2 為 CPU 密集);記錄升級的列數;後續開機為無操作且快速

### IT-HSM-14 INT-148 韌性/故障轉移
- **Given** 一個 skipTlsVerify=true 的 k8s 端點、一個 verifyTls=false 但設有 expectedFingerprint 的 F5 端點,外加一個沒有 expectedHostKey 的 SSH 端點
- **When** 對每一個各自針對一個 MITM/自簽伺服器執行 testConnect/deploy
- **Then** k8s trust-all 連線成功(且產品於介面上呈現一筆驗證被略過的警告/稽核);F5 在釘定相符時連線成功,但不相符的憑證被拒絕(CertificateException);沒有 expectedHostKey 的 SSH 被拒絕(FD-06 失敗關閉),除非 allowUnknownHostKey=true

---

## 防竄改稽核鏈

### IT-AUDIT-CHAIN-01 INT-149 端到端生命週期
- **Given** 一個乾淨的 DB,含 N 筆既有且完整的稽核列;已設定 CLM_AUDIT_HMAC_SECRET;一筆隸屬群組 G 的已簽發憑證/訂單;admin 連線。
- **When** 透過某端點執行一次真實的變更動作(例如經由撤銷流程的 ORDER_REVOKED),然後 GET /api/v1/audit?action=ORDER_REVOKED 取得新列,再 GET /api/v1/audit/verify-integrity。
- **Then** 存在一筆新的 acme_audit_log 列,其 result=SUCCESS、actor=該 admin email、request_ip 已填入、previous_hash 等於前一尾列的簽章、且有一個非空的 64 字元簽章;verify-integrity 回傳 intact=true 且 verifiedRows=totalRows=N+1。

### IT-AUDIT-CHAIN-02 INT-150 安全
- **Given** 至少 5 筆完整的稽核列;HMAC 密鑰僅應用程式知曉;admin 連線。
- **When** 在 acme_audit_log 中直接 UPDATE 一筆鏈中段的列(例如透過 SQL 變更第 3 列的 after_data 或 action,模擬一個沒有金鑰的攻擊者),然後呼叫 GET /api/v1/audit/verify-integrity。
- **Then** intact=false、firstBreakAt = 第 3 列的 UUID、firstBreakReason 含 'signature mismatch — row tampered'、且 verifiedRows = 2(竄改之前的列)。

### IT-AUDIT-CHAIN-03 INT-151 安全
- **Given** 7 筆完整的列。
- **When** (a) DELETE 一筆鏈中段的列(#4)並呼叫 verify-integrity;(b) 另外 DELETE 最新的 2 筆列(尾端截斷)並呼叫 verify-integrity。
- **Then** (a) intact=false 且 firstBreakAt = 第 5 列(previousHash 不符)。(b) intact=true——暴露尾端截斷無法被偵測。測試斷言兩者,證明此已記錄的限制並迫使產品做出決策(例如定期錨定尾端簽章)。

### IT-AUDIT-CHAIN-04 INT-153 併發/競態
- **Given** 兩個指向同一 MSSQL 的 RXCLM 應用程式執行個體(搭配 ShedLock 的受支援 HA 拓撲);HMAC 密鑰共享。
- **When** 在同一毫秒視窗內,將 M 次並行變更動作分散到兩個節點上驅動(例如節點 A 上 50 次 ENDPOINT_UPDATED、節點 B 上 50 次),然後呼叫 verify-integrity。
- **Then** 要嘛鏈結完整且 verifiedRows=totalRows,要嘛測試重現一次分叉(兩列具有相同的 previous_hash,且 verify-integrity 回報一次誤判的 'previousHash mismatch')。結果必須被斷言;分叉是一個 P0 缺陷,因為 chainLock 是 JVM 本機的,而非叢集範圍的。

### IT-AUDIT-CHAIN-05 INT-155 資料遷移
- **Given** 一條完整的鏈;接著在 SQL 中竄改鏈中段的某一列(其簽章現在已失效)。
- **When** 重新啟動應用程式,使 AuditChainBackfillRunner.backfill() 執行,接著呼叫 verify-integrity 並檢視日誌。
- **Then** 目前的程式碼路徑:回填會對毀損的列重新簽章,而 verify-integrity 回報 intact=true,日誌中僅有一則 WARN '[audit-chain-backfill] REPAIRED broken signature'。測試斷言此危險行為,並驅動修正(改採隔離/標記而非自動修復,或在未經操作人員確認前拒絕修復)。

### IT-AUDIT-CHAIN-06 INT-157 韌性/故障轉移
- **Given** 一個會呼叫 AuditLogger 的變更型端點;在鏈寫入中注入故障(例如關閉 DB 連線,或讓 chainWriter.append 拋出例外)。
- **When** 呼叫該變更型動作,並觀察該動作是否提交,以及是否有任何錯誤被呈現出來。
- **Then** 斷言目前行為:persist() 吞掉了例外(只有 log.warn 'Failed to write audit log'),而業務動作仍然成功——亦即一次未經稽核的變更。測試記錄此 fail-open 行為,並驅動一項政策決定(對安全關鍵動作採 fail-closed,或採用至少一次(at-least-once)的 outbox)。

### IT-AUDIT-CHAIN-07 INT-158 權限/多租戶隔離
- **Given** 使用者:admin;群組 G1 的 supervisor;群組 G2 的一般 user。兩個群組都存在稽核列,包含一筆 CN/主體屬於 G1 的 ACCOUNT_KEY_DOWNLOADED 事件。
- **When** 每個角色分別呼叫 GET /api/v1/audit、GET /api/v1/audit/export.xlsx 以及 GET /api/v1/audit/verify-integrity。
- **Then** admit:三者皆 200。supervisor:list + export 為 403,verify-integrity 為 200。user:三者皆 403。沒有任何路徑會回傳另一個群組的列(確認 ADMIN-only 的鎖定確實封住了程式碼註解所描述的跨群組 SECRET_DOWNLOADED 外洩)。

### IT-AUDIT-CHAIN-08 INT-152 安全
- **Given** 一個完整的鏈,含一筆其 actor_user_id 與 group_id 已填入的列。
- **When** 在 SQL 中 UPDATE 該列的 actor_user_id(或 group_id),然後呼叫 verify-integrity。
- **Then** intact=true——證明這些與鑑識相關的欄位位於 HMAC 標準化輸入之外,且可被靜默變更。測試記錄此缺口並推動將它們加入 AuditChainService.canonical()。

### IT-AUDIT-CHAIN-09 INT-154 併發/競態
- **Given** 一個應用程式節點、空的稽核資料表、已設定密鑰。
- **When** 從 50 個執行緒同時發出 500 個稽核事件(成功/失敗的 builder 呼叫混合),然後 verify-integrity。
- **Then** verifiedRows == totalRows == 500、每個 previous_hash 都恰好連結到一個前驅、無重複的 previous_hash、無 NULL 簽章。證實 chainLock + REQUIRES_NEW + (timestamp,id) 平手裁決器在單節點的競爭下成立。

### IT-AUDIT-CHAIN-10 INT-156 資料遷移
- **Given** 一個資料庫,其中植入了 V67 之前、previous_hash/signature 為 NULL 的列,並以已知的時間戳記順序排列,且至少有一對相同微秒的列(以演練 id 平手判定機制)。
- **When** 啟動應用程式(V67 遷移 + 回填執行),接著呼叫 verify-integrity,再重新啟動以確認冪等性。
- **Then** 第一次啟動:所有舊版列都取得 previous_hash+signature,verify-integrity intact=true,日誌回報 'signed N legacy row(s)'。第二次啟動:執行器為無操作(signed=0、repaired=0)。相同微秒的列依 id 以決定性方式排序,且仍可驗證通過。

### IT-AUDIT-CHAIN-11 INT-159 安全
- **Given** prod profile。
- **When** (a)在未設定 CLM_AUDIT_HMAC_SECRET 且未設定 ACME_DEV_INSECURE_CRYPTO 的情況下啟動;(b)在未設定密鑰但 ACME_DEV_INSECURE_CRYPTO=true 的情況下啟動。
- **Then** (a)環境啟動失敗並拋出 IllegalStateException,點名 acme.security.audit-hmac-secret(快速失敗——鏈不可靜默地無金鑰)。(b)應用程式啟動,但記錄一則關於 DEV 預設金鑰的明確 WARN;測試斷言此警告有發出,以便部署掃描能捕捉到設定錯誤的 prod。

### IT-AUDIT-CHAIN-12 INT-160 相容性
- **Given** 一條以密鑰 K1 簽章的完整鏈。
- **When** 以 CLM_AUDIT_HMAC_SECRET=K2(已輪替)重新啟動,並呼叫 verify-integrity。
- **Then** 斷言目前行為:verify-integrity 在最開頭的那一列就回傳 intact=false(簽章不符),證明沒有金鑰版本控管。驅動「在現場支援輪替之前,需要一個 key-id 欄位/輪替程序」的需求。

### IT-AUDIT-CHAIN-13 INT-162 授權強制
- **Given** siem.enabled=true,且 siem.host 指向一個被黑洞掉/無法連線的位址;在(a)Standard 授權與(b)Enterprise 授權下測試。
- **When** 執行一個會觸發稽核寫入的變更型動作。
- **Then** (a)Standard:SiemForwarder.forward 為無操作(license.featSiemExport()=false),且 DB 鏈列仍被寫入且可驗證。(b)Enterprise:在守護執行緒上嘗試轉發;即使 SIEM 主機無法連線,稽核 DB 寫入仍然成功,且 verify-integrity 維持完整(盡力而為,絕不阻擋)。

### IT-AUDIT-CHAIN-14 INT-161 效能/負載
- **Given** acme_audit_log 植入了 1,000,000+ 列。
- **When** 呼叫 GET /api/v1/audit/verify-integrity,並同時併發發出多次呼叫。
- **Then** 量測回應時間與堆積;斷言它在 SLO 內完成且不會 OOM。由於它未分頁地載入所有列並對每一列重新簽章,這很可能超出可接受的延遲/記憶體——驅動採用漸進式/設置檢查點的驗證設計。

---

## 授權 / 配額管控

### IT-LIC-01 INT-163 安全
- **Given** 已啟用一個有效的 STANDARD 授權(clm_license id=1:tier=STANDARD、quota_ca_types=2、feat_hsm_support=0)。原始的 license_jwt 未經更動。
- **When** 一個 DBA 層級的 UPDATE 直接在 clm_license 中設定 quota_ca_types=99、tier='ENTERPRISE'、feat_hsm_support=1、expires_at='9999-...',接著重新啟動應用程式,使 @PostConstruct loadAndReverify 執行。
- **Then** GET /api/v1/license/entitlements(以 ADMIN)回傳 tier=STANDARD、quota.caTypes=2、features.hsmSupport=false(自權杖重新計算,而非從被編輯的列);POST /api/v1/providers 仍被擋在 2 種 CA;GET /api/v1/admin/hsm/status 回傳 licensed:false。被編輯的 DB 值被捨棄。

### IT-LIC-02 INT-164 安全
- **Given** clm_license id=1 持有一個 license_jwt,其載荷的某個位元組已被翻轉(簽章不再相符),產品已重新啟動。
- **When** 應用程式啟動(loadAndReverify),接著有使用者呼叫 POST .../orders。
- **Then** GET /api/v1/license/status 回傳 state=NOT_ACTIVATED、blocksNewOrders=true;訂單建立回傳「新訂單已封鎖」的錯誤;一則 ERROR 日誌記錄了重新驗證失敗。不會從這個無法驗證的列授予任何授權。

### IT-LIC-03 INT-165 端到端生命週期
- **Given** 一個在時間 T 到期的授權。存在一筆 ISSUED 憑證,且有一筆在授權仍有效時提交、正處於 PENDING 核准的憑證簽發請求。
- **When** 系統時間越過 T(授權 EXPIRED)。接著(a)使用者 POST 一筆新訂單,(b)ADMIN 核准那筆待處理的簽發請求,(c)使用者下載既有的 ISSUED 憑證 PEM。
- **Then** (a)與(b)兩者皆以「License EXPIRED — new orders blocked」失敗(canIssue 在核准時依 CertIssuanceApprovalService:175 重新檢查);(c)成功——既有憑證的下載/部署不受影響。status() 顯示 EXPIRED、daysAgo>0。

### IT-LIC-04 INT-166 安全
- **Given** 一個 VALID 且接近到期的授權。ClockGuard 已將 HWM 推進到目前時間(clm_settings _license.clock.hwm = epoch|hmac)。
- **When** 將作業系統時鐘回撥超過容差範圍(例如 -10 分鐘),使 exp 看起來像在未來;使用者 POST 一筆新訂單。另外,DBA 在 clm_settings 中將已儲存的 epoch 調降以符合回撥後的時間,但未重新計算 HMAC,並發生重啟。
- **Then** 時鐘回撥時:status()=CLOCK_TAMPERED、canIssue=false、訂單被封鎖。DB epoch 竄改後:loadVerified 偵測到 HMAC 不符、記錄 ERROR、將 HWM 視為 0,並在下一次 tick 時從當下重建一個正當的 HWM——被調降的值不被信任,因此一旦真實時間再度推進,回撥仍然無法延長授權。

### IT-LIC-05 INT-167 授權強制
- **Given** 一個已啟用的 STANDARD 授權(quotaCaTypes=2、quotaDnsTypes=3)。
- **When** ADMIN 建立 CA 提供者,數量達到並超過 2,且建立橫跨 4 種不同類型的 DNS 提供者帳號(例如 cloudflare、route53、godaddy,然後是第 4 種類型)。
- **Then** 第 3 個 CA 提供者的建立回傳「CA 連接器種類上限(2 種)」;第 4 種不同的 DNS 類型回傳「DNS 連接器種類上限(3 種)」,但對一個已被計入的 DNS 類型建立第二個帳號則被允許(依不同類型計算的語意)。entitlements 反映出 caTypesUsed/dnsTypesUsed 與強制執行的分母相符。

### IT-LIC-06 INT-170 授權強制
- **Given** 一個已啟用的 STANDARD 授權(featHsmSupport=false、featSiemExport=false),且在 clm_settings 中設有 hsm.enabled=true、siem.enabled=true、siem.host。
- **When** ADMIN 呼叫 GET /api/v1/admin/hsm/status,且一個可稽核的動作(例如 LOGIN)產生一個由 SiemForwarder.forward 處理的 AuditEntity;在一次金鑰操作期間評估 Pkcs11KeyProtector.isEnabled()。
- **Then** HSM 狀態回傳 enabled:false、available:false、licensed:false,且不嘗試 PKCS#11 連線;isEnabled() 回傳 false;SiemForwarder 為無操作(不送出 CEF 封包),儘管 siem.enabled=true。接著重新啟用一個 ENTERPRISE 權杖,並確認 HSM 嘗試連線且 SIEM 進行轉發。

### IT-LIC-07 INT-174 安全
- **Given** 已設定 acme.license.allow-unlicensed=true,且應用程式以 DEFAULT(非 dev/test)Spring profile 運行,並且沒有已啟用的授權。
- **When** 應用程式啟動,且一個使用者 POST 一筆新訂單。
- **Then** init() 記錄 ERROR(「production 不允許用旗標關閉授權」),enforcementDisabled 維持 false,status=NOT_ACTIVATED,canIssue=false,訂單被封鎖。在 'dev' profile 下以相同旗標重新執行則產生 UNLICENSED_DEV 且 canIssue=true。

### IT-LIC-08 INT-168 授權強制
- **Given** 一個 quotaFqdn=5 的授權。既有的受管訂單已涵蓋 3 個不同的網域。
- **When** 一筆新訂單請求帶有 CN=a.example.com 與 SAN [b.example.com, a.example.com](去重後新增一個新網域)——投影總計 4,允許。接著提交一筆新增 3 個全新網域的請求(投影 7 > 5)。接著將一筆較早的訂單移至 REVOKED 並重試該超配額請求。
- **Then** 第一筆請求成功(去重意味著它新增 1 個,而非 2 個)。第二筆被「網域數上限(5 個)」封鎖。在 REVOKE 釋放一個網域後,重試的請求現在容得下且成功。entitlements 中的 usedCount 與 managedDomains().size() 相符。

### IT-LIC-09 INT-169 併發/競態
- **Given** 一個 quotaFqdn=N 的授權,且容量恰為 N-? ,使得再多一張多網域憑證剛好容得下,但兩張就會超過。
- **When** 從兩個工作階段同時提交兩筆(或更多)訂單建立請求,各自新增最後可用的網域空間。
- **Then** 揭露是否兩者都通過 wouldExceed 檢查(該檢查為非交易性、無鎖)且兩者都持久化,使 managedDomains().size() > quota。預期的強化行為:最多達到 quota;實際行為必須被量測,且若發生超配額,則標記為一個需要交易性/加鎖防護的缺陷。

### IT-LIC-10 INT-171 負向/邊界
- **Given** 一個真實簽章為 tier='ENTERPRISE' 但缺少 quotaFqdn/quotaCaTypes/quotaDnsTypes/quotaEndpointTypes/complianceLevel/discovery 的權杖(供應商簽發錯誤)。
- **When** 它被啟用,接著一個使用者嘗試一筆新訂單、一個 admin 嘗試 CA 建立、一次 HSM 狀態檢查,以及一次自訂政策建立。
- **Then** applyClaims 記錄 ERROR 列出缺少的欄位;quotaFqdn()=0,因此每一筆新訂單都被封鎖(「網域數上限(0)」);非本機的 endpointTypeAllowed=false;featHsmSupport()=false;complianceLevel='basic',因此自訂政策建立被拒絕。系統不會靜默地授予 Enterprise/無限制。

### IT-LIC-11 INT-172 資料遷移
- **Given** 一個沒有 'tier' 聲明的真實授權權杖(分層前的格式),如同把一個舊部署升級到分層感知組建後會存在的那種。
- **When** 啟用/啟動後,讀取 entitlements 並演練配額/功能(建立大量 CA/DNS、大 SAN 訂單、HSM 狀態)。
- **Then** tier=null;quotaFqdn/CaTypes/DnsTypes()=-1(無限制);所有的 endpointTypeAllowed=true;featHsmSupport/SiemExport=true(沿用既有權限)。entitlements 顯示 -1 上限。這必須是 EXPECTED 且被明確斷言的,使升級不會破壞既有客戶——並被標記出來,讓產品意識到舊權杖會授予完整能力。

### IT-LIC-12 INT-173 安全
- **Given** 一個已啟用的授權,customer_name='MegaBank' 且具已知到期日。
- **When** 一個未驗證的用戶端 GET /api/v1/license/status;一個 USER 角色(非 admin)的用戶端 GET /api/v1/license/entitlements 並 POST /api/v1/admin/license/activate。
- **Then** status 回傳 200,且向匿名呼叫者曝露 customerName 與 expiresAt(已記錄的 permitAll)——確認外洩客戶/到期日是否可接受;entitlements 對 USER 回傳 403(僅限 ADMIN/SUPERVISOR);activate 對 USER 與 SUPERVISOR 回傳 403(僅限 ADMIN)。任何偏差(例如 user 觸及 activate)都是一個 P0 授權缺陷。

### IT-LIC-13 INT-176 韌性/故障轉移
- **Given** 一個即將到期的授權,以及在門檻視窗(renewal.threshold.days)內到期應續約的憑證。
- **When** 續約週期在授權 EXPIRED(或時鐘回撥)時執行。
- **Then** runOnce 短路:記錄「[renewal] license EXPIRED — skipping all renewals」,report.skipped 含「(all): ...」,且不建立任何續約訂單/簽發請求。當一張接近到期的憑證因授權封鎖而無法在關鍵視窗內續約時,SLA 違反稽核(ORDER_RENEWAL_SLA_BREACH)與擁有者通知路徑是預期的升級處理——斷言它有觸發,使過期的授權不會靜默地讓憑證失效卻沒有操作人員警報。

### IT-LIC-14 INT-175 功能整合
- **Given** 一個 ENTERPRISE 授權處於使用中,已設定 6 個 CA 提供者並具完整合規(允許自訂政策)。DB 沒有已植入的基線(完整層級不會植入)。
- **When** ADMIN 啟用一個 STANDARD 權杖(quotaCaTypes=2、complianceLevel=core7)。
- **Then** entitlements 立即顯示 caTypes=2(超配額:caTypesUsed=6 > 2)——確認既有提供者持續運作但 NEW CA 建立被封鎖;自訂政策建立現在回傳「Professional 以上」升級錯誤;CompliancePolicySeedRunner.seedIfNeeded 僅在沒有政策存在時植入一個 core7 WARN 基線。不需要重啟(快取在 activate 中刷新)。

---

## 通知 / SIEM 轉發

### IT-SIEM-01 INT-177 端到端生命週期
- **Given** mail.enabled=true 指向一個內嵌的假 SMTP(GreenMail/MailHog);mail.expiry.days='30,14,7,1';一筆 ISSUED 的 OrderEntity,已設定 assignee_email 且 cert_not_after = now+6 天;expiry_notified_thresholds=NULL。
- **When** 在模擬的多天期間呼叫 CertExpiryNotifier.scan() 三次(daysLeft 6、6,接著推進憑證/now 使 daysLeft=5 然後 0)。
- **Then** 第一次掃描針對最小已跨越的門檻(7)正好寄出一封 email,並持久化 expiry_notified_thresholds 包含所有已跨越的門檻(30,14,7);在 daysLeft=6 時重新執行不會額外寄出任何 email;跨越到 <=1 時再正好多寄一封(門檻 1)。clm_email_log 總計顯示正好兩列 SENT,且 GreenMail 收到兩封訊息。

### IT-SIEM-02 INT-178 韌性/故障轉移
- **Given** 一個假 SMTP 會拒絕前 2 次連線然後接受;mail.enabled=true;ENQUEUE_SWEEP_GRACE 行為生效中。
- **When** NotificationService.notify('cert.issued', ...) 將一封 email 排入佇列;讓非同步派送失敗兩次,接著驅動 retrySweeper() 越過退避時間窗。
- **Then** clm_email_log 列從 QUEUED->QUEUED(next_retry_at 依 BACKOFF_MIN 先 1 後 5 分鐘推進,attempt_count 先 1 後 2)->SENT(attempt_count 3) 轉換;假 SMTP 正好收到一封投遞的訊息(無重複寄送),中間嘗試時 error_message 有填值,之後清除/設定 sent_at。

### IT-SIEM-03 INT-181 授權強制
- **Given** 一個在 siem.host/siem.port 上的本機 UDP 監聽器;siem.enabled=true;啟用一個 STANDARD 階的已簽署 license(featSiemExport 不存在/為 false)。
- **When** 執行任何被稽核的動作(例如登入 / 建立訂單)以使 AuditLogger.persist 執行。
- **Then** 資料庫的稽核鏈列被寫入,但 UDP 監聽器收到零個資料包(featSiemExport()==false 在 submit 之前短路)。重新啟用一個 featSiemExport=true 的 ENTERPRISE license 並重複:監聽器現在針對同一動作收到一個 CEF:0 訊框。

### IT-SIEM-04 INT-184 安全
- **Given** 一個 ADMIN 透過 PUT /api/v1/admin/settings/mail.smtp.password 設定 mail.smtp.password='S3cretPw!'。
- **When** 呼叫 GET /api/v1/admin/settings。
- **Then** 重現缺陷:回應目前原封不動回傳 setting_value='S3cretPw!'(無遮罩,值以明文儲存在 DB)。測試應斷言所期望的安全行為(密碼欄位遮罩為 '****' 或唯寫),藉此標記出目前的洩漏;同時斷言該值並非以明文儲存(需要 EncryptedConverter)。

### IT-SIEM-05 INT-185 權限/多租戶隔離
- **Given** 一個 'user' 角色的有效 JWT cookie,以及另一個 'supervisor' 角色的有效 JWT cookie。
- **When** 各自呼叫 POST /api/v1/admin/mail/test、GET /api/v1/admin/mail/log、GET /api/v1/admin/mail/status、GET/PUT /api/v1/admin/settings。
- **Then** 全部回傳 403(方法安全拒絕非 ADMIN)。匿名(無 cookie)回傳 401。ADMIN 成功。確認由於 SecurityConfig 沒有 /api/v1/admin/** 匹配器,僅靠註解的授權仍成立。

### IT-SIEM-06 INT-179 韌性/故障轉移
- **Given** 永遠拒絕連線的假 SMTP;mail.enabled=true;一封已排入佇列的 cert.expiring email。
- **When** 驅動 dispatch + retrySweeper 經過 5 次嘗試(快轉 next_retry_at)。
- **Then** 該列以 status=FAILED、attempt_count=5、next_retry_at=NULL、error_message 已設定告終;GET /api/v1/admin/mail/log 回傳該列為 FAILED。測試驗證產品意圖中的死信可見性——並記載缺口:死信時不發出任何 SIEM 事件 / 不發出任何 admin 告警(提議的回歸防護)。

### IT-SIEM-07 INT-180 併發/競態
- **Given** 兩個 EmailService/CertExpiryNotifier 實例共用一個啟用了 ShedLock 的 MSSQL;一張到期該發到期警示的 ISSUED 憑證,以及數列 QUEUED 的重試列。
- **When** 兩個節點在 lockAtMostFor 視窗內同時觸發 retrySweeper() 與 scan()。
- **Then** 只有一個節點取得 email-retry-sweeper / cert-expiry-notifier 鎖;寄出的 email 總數 == 單節點基準;沒有任何 clm_email_log 列在同一視窗內被遞增 attempt_count 兩次;expiry_notified_thresholds 正好被推進一次。

### IT-SIEM-08 INT-182 安全
- **Given** Enterprise license;siem.enabled=true;一個 AuditEntity,其 subject/actor/error 含有 CEF/syslog 中繼字元(pipe、backslash、equals、CR、LF),例如 subject='evil|=\\\nCEF:0|spoof'。
- **When** AuditLogger.persist 透過 UDP 與 TCP 兩種傳輸轉發該事件。
- **Then** 擷取到的訊框解析為單一一筆 CEF 記錄:來自 subject 的 header pipe 被跳脫(\|),extension 的 equals/backslash 被跳脫,CR/LF 被收斂為空格,因此不會注入第二行 syslog / 偽造的 CEF 記錄;FAILURE 時 severity=7,rt= epoch 毫秒存在,suser/src/act/outcome/cs1..cs3 標籤正確。

### IT-SIEM-09 INT-183 韌性/故障轉移
- **Given** Enterprise license;siem.host 指向一個無法觸及/黑洞位址(UDP)或一個不接受連線的 TCP port;那唯一一條轉發執行緒阻塞/變慢。
- **When** 快速產生超過 1000 個稽核事件。
- **Then** 稽核資料庫鏈保持完整且可驗證(轉發絕不阻塞 persist);SIEM 端收到較少的事件,且最舊的被靜默丟棄(DiscardOldestPolicy),只有 log.debug。測試斷言稽核完整性並明確擷取此缺口:沒有持久化的 SIEM outbox/重放,丟棄也沒有任何度量指標。

### IT-SIEM-10 INT-186 API 合約
- **Given** 先 mail.enabled=false,然後 true 並搭配一個可運作的假 SMTP。
- **When** 在停用時 POST /api/v1/admin/mail/test {recipient:'a@b.com'},然後在啟用時;同時以空白收件人 POST。
- **Then** 停用 -> HTTP 409 body error 'mail.enabled = false';空白收件人 -> HTTP 400 'recipient required';啟用 -> HTTP 200 ok:true,建立一筆 'test' 的 clm_email_log 列並(在非同步後)翻轉為 SENT,假 SMTP 收到一封主旨為 '[CLM] 測試信件' 的訊息。

### IT-SIEM-11 INT-187 迴歸
- **Given** mail.enabled=true;mail.expiry.escalate.to 已設定;一張在 renewal.critical.days 內續約失敗的憑證,以及一張在升級視窗內的憑證。
- **When** alertRenewalFailureIfCritical 觸發 notify('renewal.failed.critical',...),且 maybeEscalate 觸發 notify('cert.expiring.escalated',...)。
- **Then** 重現缺陷:渲染出的主旨是裸的 '[CLM] renewal.failed.critical' / body 為 'Event: renewal.failed.critical',而 daysLeft/cn/reason 被丟棄(這些鍵在 NotificationService 的 switch 中不存在)。測試斷言所期望的行為(主旨+body 包含 CN、daysLeft、reason),藉此標記出這兩個維運上最緊急的警示缺少範本。

### IT-SIEM-12 INT-188 資料遷移
- **Given** 一個全新 migrate 到最新版的 DB;Enterprise license 啟用中。
- **When** Admin 嘗試 PUT /api/v1/admin/settings/siem.enabled value='true'(以及 siem.host)。
- **Then** 重現缺陷:回傳 404 setting_not_found,因為沒有任何 V-migration 植入 siem.*(SiemForwarder javadoc 中引用的 V90 seed 並不存在)。測試斷言所期望的佈建(siem.* 列應在 migration 後存在,讓 Enterprise 客戶能透過文件記載的設定路徑啟用 SIEM)。

### IT-SIEM-13 INT-189 效能/負載
- **Given** 10 萬筆混合 status 的 acme_orders 列,約 2% 落在最大的門檻視窗內。
- **When** CertExpiryNotifier.scan() 執行一次。
- **Then** 量測掃描在可接受的界限內完成,並浮現出它目前是執行 orderRepo.findAll() 並在 JVM 內過濾(對 status/cert_not_after 沒有 DB 述詞,忽略 idx_cert_not_after)。斷言需要一個以查詢為範圍的擷取(status IN (ISSUED,IMPORTED) AND cert_not_after < cutoff),以避免全表的堆積載入。

---

## 憑證匯入 / 格式轉換 / 下載

### IT-IMPORT-01 INT-190 端到端生命週期
- **Given** 一個已驗證的 ADMIN;群組 G 中 admin 可存取的有效 letsencrypt 帳號 acc_X;一張真實、目前有效、CN=demo.example.com 且帶 1 個 SAN 的葉憑證 PEM。
- **When** POST /api/v1/certs/import {certPem,provider=letsencrypt,accountId=acc_X,owner=ops};接著 GET /api/v1/certs?status=IMPORTED;GET /api/v1/certs/dashboard;接著以同一個 admin POST /api/v1/download-requests {orderId,format=domain-key},透過第二位 supervisor 核准,並下載。
- **Then** 匯入回傳 201 且 status=IMPORTED 並含解析出的 SAN;該訂單可被查詢為 IMPORTED;dashboard 的 'total' 與 byStatus.IMPORTED 遞增,且 FQDN used-count 上升了 CN∪SAN 的相異數;domain-key 下載在 produce() 處失敗,因為匯入的憑證有 null 的 domainKeyPem(預期 'domain key missing' 4xx/5xx),證明匯入的憑證絕不暴露私鑰。

### IT-IMPORT-02 INT-191 功能整合
- **Given** ADMIN;一個範本格式的 .xlsx,含 6 筆資料列:(1)有效憑證 + 有效帳號;(2)過期憑證;(3)格式錯誤的 PEM 垃圾;(4)accountId 屬於與 A 欄不同的 provider;(5)第 1 列 CN 的重複;(6)有效憑證但帳號在 admin 無法存取的群組中。
- **When** POST /api/v1/certs/import/batch/preview(multipart file);接著 POST /api/v1/certs/import/batch/commit,rowIndexes 列出全部六個工作表列號。
- **Then** Preview 回傳每列結果:第 1 列 READY/valid=true;第 2-6 列 INVALID 並帶各自的特定錯誤(過期 / PEM 解析失敗 / provider 不符 / 批次內 CN 重複 / 無群組存取)。Commit 只將第 1 列寫入為 IMPORTED(importedCount=1,skippedCount=5);沒有部分/垃圾列被持久化;CERT_IMPORT_BATCH 稽核顯示 imported=1。

### IT-IMPORT-03 INT-192 權限/多租戶隔離
- **Given** SUPERVISOR sup_A 僅限於群組 A;一個帳號 acc_B 與一筆 ISSUED 訂單 ord_B,兩者都在群組 B 中。
- **When** sup_A 呼叫:POST /api/v1/certs/import 帶 accountId=acc_B;GET /api/v1/certs/import/template.xlsx;POST /api/v1/download-requests {orderId=ord_B,format=leaf};GET /api/v1/{provider}/accounts/acc_B/orders/ord_B/leaf-only.pem;DELETE /api/v1/certs/ord_B。
- **Then** 匯入被拒絕(無群組存取);範本帳號下拉只含群組 A 的帳號(acc_B 不存在);download-request 提交 -> 403/404(查無訂單);直接的 leaf-only.pem -> not-found(mustHaveCert 中的群組防護);DELETE -> 404 not-found(不洩漏存在性)。

### IT-IMPORT-04 INT-193 安全
- **Given** 群組 G 中一筆 ISSUED 的 ACME 訂單 ord_X(CLM 簽發,具有 domainKeyPem);群組 G 中的 SUPERVISOR sup1 與 sup2;一個 ADMIN。
- **When** (a) sup1 提交 format=pfx 的 download-request 並嘗試核准自己的請求;(b) sup2 核准;sup1 下載兩次;(c) sup1 再次提交,sup2 核准,等待 >15 分鐘,sup1 下載;(d) ADMIN 提交 + 自我核准自己的請求。
- **Then** (a) 非 admin 的自我核准 -> 409 SoD 錯誤;(b) sup2 核准成功,第一次下載回傳 .pfx 位元組 + SECRET_DOWNLOADED 稽核,第二次下載 -> 409 'already used'(一次性 CAS);(c) TTL 過後的下載 -> 409 'expired';(d) ADMIN 自我核准成功,稽核中帶 selfApproved=true + emergencySelfApprove。PFX 密碼是脆弱的預設值 'changeit'。

### IT-IMPORT-05 INT-194 併發/競態
- **Given** 一筆 PENDING 的 pfx download-request;兩位 supervisor sup2、sup3(都不是請求者);核准後,請求者開啟兩個並行的下載呼叫。
- **When** sup2 與 sup3 並行 POST 核准;接著請求者觸發 2 個並行的 GET .../download。
- **Then** 正好一個核准變更該列(markApprovedIfPending 回傳 1;落敗者得到 409 'already processed');正好一個下載消耗(markConsumedIfDownloadable 回傳 1,另一個 409)。沒有重複的 SECRET_DOWNLOADED,沒有產出第二個憑證檔案。

### IT-IMPORT-06 INT-195 授權強制
- **Given** 一個 quotaFqdn=10 的分階 license;現有的受管訂單涵蓋 8 個相異網域。
- **When** (a) 匯入一張 5-SAN 憑證(5 個新網域)-> 將會合計 13;(b) 匯入一張 2 網域憑證 -> 合計 10;(c) 批次上傳兩列,在只剩 1 個額度時,每列各新增 2 個新網域;(d) 在只剩 2 個額度時,並行觸發兩個各為不相交 2 網域憑證的單張匯入。
- **Then** (a) 被拒絕並帶 'reached licensed domain limit (10)' 訊息;(b) 被接受;(c) 批次接受第一個能容納的列,並將超配額的列標記為 INVALID 並帶配額錯誤(投影網域累計);(d) 並行案例可能超量提交超過 10——斷言並把它歸檔為一個有記載的競態(wouldExceed 讀取的是過時快照,沒有鎖)。

### IT-IMPORT-07 INT-196 負向/邊界
- **Given** 一個 role=user(最低權限)的已驗證工作階段;一個 50MB 的垃圾 payload 與一個貼上的 RSA PRIVATE KEY 區塊。
- **When** role=user POST /api/v1/cert-inspect,帶 (a) 一個有效的 3 憑證鏈,(b) 一個貼上的私鑰,(c) 一個 50MB 的 body,(d) 深度巢狀/垃圾的 PEM 標記。
- **Then** (a) 回傳解析出的鏈,具有正確的 leaf/intermediate/root 位置、具型別的 SAN、EKU 映射、指紋;(b) 回傳 parseError 'input is a private key, not a certificate'(不崩潰,不回顯 key);(c) 該端點目前沒有大小上限(不像 32KB 的批次上限)——斷言並記載這個 DoS 缺口並建議加上 body 上限;(d) 優雅地回傳 parseError。

### IT-IMPORT-08 INT-197 API 合約
- **Given** 一筆 ISSUED 訂單 ord_X,其 certPem 是一張 leaf + 1 張 intermediate;ADMIN 呼叫者。
- **When** GET certificate.der、leaf-only.pem、chain-only.pem、certificate.cer(pem 與 ?format=der);各自獨立地以 OpenSSL/Java 解碼。
- **Then** DER 解碼為與 leaf-only.pem 相同的 leaf(位元相同的 SPKI/serial);chain-only.pem 只含 intermediate(排除 leaf);.cer 預設是 base64 PEM leaf,?format=der 是二進位;Content-Disposition 檔名使用 CN。一筆單憑證(無 intermediate)訂單對 chain-only 回傳一個清楚的 'no intermediate cert in PEM' 錯誤。

### IT-IMPORT-09 INT-198 負向/邊界
- **Given** ADMIN;一張 CN=evil.example.com 的自簽憑證(subject==issuer),其語法有效且未過期。
- **When** 以自簽 PEM 與一個有效帳號 POST /api/v1/certs/import;接著對它 POST /api/v1/cert-inspect。
- **Then** 匯入成功(201,IMPORTED)——只檢查格式良好性 + notAfter>now;檢查器依 BasicConstraints 回報 selfSigned=true 與 isCa。斷言並記載沒有執行任何鏈建構 / 簽章 / 信任錨點 / 撤銷檢查;建議在 GA 前加上一個信任驗證或合規政策關卡。

### IT-IMPORT-10 INT-199 功能整合
- **Given** 一筆 CN=app.example.com、狀態為 ISSUED 的 ACME 訂單 ord_live，附帶一個 PENDING_APPROVAL 的部署工作 dj1；ADMIN。
- **When** 以同一個 CN=app.example.com、createdAt 較新的憑證執行 POST /api/v1/certs/import。
- **Then** 建立一筆新的 IMPORTED 訂單；ord_live 翻轉為 SUPERSEDED；dj1 轉為 SKIPPED 並寫入紀錄 'order superseded by <newId>'；預設憑證清單（排除 SUPERSEDED）不再將 ord_live 顯示為作用中；並輸出一行取代紀錄。驗證此連鎖效應「不會」觸及 RUNNING/SUCCEEDED 的工作，且只能透過重新簽發來還原。

### IT-IMPORT-11 INT-201 可觀測性
- **Given** ADMIN 執行：單筆匯入、批次提交（匯入 1 筆）、送出 pfx 請求、核准（由第二位 supervisor）、一次性下載。
- **When** 執行該序列，接著查詢稽核紀錄並驗證 HMAC 鏈。
- **Then** 存在依序排列的相異稽核事件：CERT_IMPORT_BATCH（匯入/略過計數）、DOWNLOAD_REQUEST_SUBMITTED、DOWNLOAD_APPROVED（含 expiresAt + emergencySelfApprove）、SECRET_DOWNLOADED（downloadedBy、format），每筆都帶有 subject=CN 與 before/after；且 HMAC 稽核鏈跨越全部事件驗證為未中斷。

### IT-IMPORT-12 INT-200 併發/競態
- **Given** ADMIN；一份 2 列的工作簿，兩列在預覽時都針對帳戶 acc_X 顯示為 READY。
- **When** 執行預覽（兩列皆 READY）；在提交前，刪除/停用 acc_X 或移除該 admin 的群組存取權；接著以兩個 rowIndex 執行提交。
- **Then** 提交時重新驗證，並將兩列標記為 INVALID（帳戶已消失／無群組存取權），或者若寫入已開始，則攔截例外並設定 status=ERROR_WRITE 'DB write failed'——「不會」留下任何部分／孤立的 IMPORTED 列，且 importedCount 僅反映真正寫入的列。

### IT-IMPORT-13 INT-202 負向/邊界
- **Given** ADMIN；(a) 一份 600 列的範本；(b) 一列帶有 40KB PEM 儲存格；(c) 一張 CN/owner 為 '=HYPERLINK("http://evil")' 的憑證，並透過 export.xlsx 往返一輪；(d) 一份刻意製作的大型/zip 炸彈式 xlsx。
- **When** 對每個輸入執行預覽/提交，並在匯入注入酬載列後呼叫 GET /api/v1/certs/export.xlsx。
- **Then** (a) 僅處理前 500 列（MAX_ROWS）；(b) 該 40KB 儲存格列被標記為 INVALID 'PEM too large'；(c) 匯出目前會逐字寫入公式字串——斷言並標記 CSV 注入風險（建議將開頭的 =、+、-、@ 中和）；(d) 伺服器保持回應／回傳乾淨的錯誤而非 OOM。

---

## SPA / 前端安全

### IT-SPA-01 INT-203 端到端生命週期
- **Given** 一個將 Angular 打包進 static/ 的已建置 JAR、一個已登入的 admin 工作階段（存在 clm_access cookie）
- **When** 對 app-routing-module.ts 中全部 25 條路由「以及」已知有問題的 slug（/admin/ca-providers、裸 /certs、裸 /accounts），執行硬式瀏覽器導覽（完整 GET，非應用程式內 router）與重新整理
- **Then** 每一條有效路由都回傳 index.html（200）「並且」算繪其元件（斷言一個路由專屬的 DOM 標記）；/admin/ca-providers 與任何未知路徑必須算繪一個明確的 404/找不到頁面，而非空白外殼；裸 /certs 與 /accounts「不得」回 404

### IT-SPA-02 INT-205 併發/競態
- **Given** 一個已驗證的工作階段，其 clm_access 剛過期但 clm_refresh 仍有效；一個在載入時發出 5 個以上並行 GET 的儀表板頁面
- **When** 所有進行中的請求同時收到 401
- **Then** 恰好觀察到一次 POST /api/v1/auth/refresh（透過網路擷取斷言）；全部 5 個原始請求在更新後重試並成功；沒有登出、沒有 /login 重新導向；輪替後的 XSRF-TOKEN 被重新讀取以用於會改變狀態的重試

### IT-SPA-03 INT-206 韌性/故障轉移
- **Given** 已驗證的使用者
- **When** (a) BE 對一個 admin API 回傳 403；(b) /auth/refresh 回傳 401/被拒絕（refresh 過期）；(c) /auth/refresh 無法到達／回傳 5xx（BE 重啟）
- **Then** (a) 不登出、錯誤浮現、使用者留在頁面上；(b) clearLocal + 重新導向 /login；(c) 網路錯誤：重新拋出原始的 401、使用者「不」被踢出、保留狀態以供重試

### IT-SPA-04 INT-207 安全
- **Given** 一個尚無 XSRF-TOKEN cookie 的全新工作階段
- **When** 第一個動作是一個會改變狀態的 POST（例如建立訂單／註冊帳戶），另外再單獨送出一個標頭被移除/token 錯誤的 POST
- **Then** 攔截器預備 /csrf 一次，接著附加 X-XSRF-TOKEN；合法的 POST 成功；一個缺少/不正確 X-XSRF-TOKEN 的 POST 被伺服器以 403 拒絕；login/refresh/mfa-verify/i18n 維持豁免 CSRF

### IT-SPA-05 INT-208 權限/多租戶隔離
- **Given** 一個 'user' 角色的工作階段
- **When** 攻擊者將 localStorage clm.currentUser.role 編輯為 'admin'，導覽至 /admin/settings、/admin/license、/audit，並直接呼叫 GET /api/v1/audit/export.xlsx 與 POST /api/v1/admin/providers
- **Then** 用戶端守衛可能算繪頁面（僅供參考），但「每一個」admin API 都回傳 403（伺服器從已簽章的 JWT 讀取角色，而非 localStorage）；無 admin 資料外洩；稽核匯出下載以 403 失敗

### IT-SPA-06 INT-209 權限/多租戶隔離
- **Given** 兩個各有憑證的群組 G1、G2；一位範圍僅限於 G1 的 supervisor
- **When** Supervisor 呼叫 GET /api/v1/certs/export.xlsx（並帶上指向 G2 資源、刻意製作的 accountId/provider 參數）
- **Then** 回傳的 xlsx 僅含 G1 憑證；無論查詢參數為何，G2 列從不出現；admin 匯出含全部群組；列數與篩選後的清單端點對得起來

### IT-SPA-07 INT-211 安全
- **Given** 已啟用 OIDC、一個模擬 IdP、returnUrl=/admin/users、依環境設定的 cookie-samesite
- **When** 使用者點擊 SSO 登入、於 IdP 驗證，並被重新導向回 returnUrl#oidc=<userJson>
- **Then** SPA 從 #oidc 片段（或 JS 可讀的 cookie）將使用者資訊攝入 localStorage，使 authGuard 看到 isLoggedIn()=true 並落在 returnUrl 上，「而不會」跳轉回 /login；在 SameSite=Strict 時重現並記錄迴圈回登入的缺陷；開放重新導向（returnUrl=//evil.com）被 safeReturnUrl 中和

### IT-SPA-08 INT-204 負向/邊界
- **Given** 已登入的 admin
- **When** （冷啟動）導覽至 /admin/dr、/admin/api-keys（已宣告元件、無路由）以及一個拼錯的 URL /admni/users
- **Then** 使用者看到一個已定義的 NotFound 元件，帶有「返回儀表板」連結；AdminDr/AdminApiKeys 要嘛被加上路由＋導覽連結，要嘛從 NgModule 移除；沒有空白的白頁

### IT-SPA-09 INT-210 授權強制
- **Given** complianceLevel 為 basic/core7 的 Standard 授權上的 admin，以及另外一個 Enterprise/full 授權
- **When** Admin 在 /admin/policies/violations 點擊『下載 Excel』
- **Then** 在 basic/core7 時：請求被拒絕（403/授權錯誤）且 UI 顯示需升級訊息、無檔案；在 full 時：xlsx 下載，帶有含時間戳的檔名與開放中的違規

### IT-SPA-10 INT-212 功能整合
- **Given** 一個啟用了 TOTP MFA 的使用者
- **When** 登入回傳 {mfaRequired:true, mfaToken}；使用者送出一個有效的 6 位數代碼，接著一個無效代碼，再等待 mfaToken 過期
- **Then** 有效代碼 -> 設定 cookie、填入 user 訊號、導覽至 returnUrl；無效代碼 -> 'mfa_code_invalid' 訊息、停留在 MFA 步驟；過期的 token -> 'mfa_token_expired'、返回密碼步驟（mfaToken 已清除）

### IT-SPA-11 INT-213 功能整合
- **Given** 應用程式針對一個 i18n 端點在首次命中時緩慢/無法使用、之後恢復的 BE 做冷啟動
- **When** 第一次 /i18n/messages 失敗兩次後成功；稍後使用者在頂列切換語系 zh-TW <-> en
- **Then** 退避重試最終填入 messages（不被鎖死在 {}）；切換後，所有可見字串「以及」document.title 都重新算繪為新語系而不需重新載入頁面；任何在 zh-TW 存在但在 en 缺少的鍵都被標記（無原始鍵外洩）

### IT-SPA-12 INT-214 安全
- **Given** 同一個工作階段的兩個瀏覽器分頁；auth.access.token.ttl.minutes 設為一個小的測試值
- **When** 兩個分頁都閒置超過 TTL；另外單獨地，一個分頁有活動而另一個閒置
- **Then** TTL 過後兩個分頁都重新導向至 /login?reason=idle 並清除快取的使用者；一個分頁的活動使兩者都保持存活（共享時間戳）；單純的背景輪詢「不會」延長工作階段；登入頁顯示閒置逾時訊息

### IT-SPA-13 INT-215 相容性
- **Given** 正式環境建置（apiBaseUrl=''）打包進 Spring static/
- **When** 發出任何 API 呼叫，並作為反例，將 basePath 強制為一個跨來源主機
- **Then** 同源呼叫成功；跨來源 basePath 被 CSP connect-src 'self' 封鎖（斷言主控台 CSP 違規 + 失敗的請求），證明 FE/BE 同源的部署契約，且硬寫的 http://localhost:8090 預設值在正式環境中從不被使用

### IT-SPA-14 INT-216 API 合約
- **Given** 已驗證的 admin 與一個未驗證/已過期的工作階段
- **When** 觸發 /api/v1/certs/export.xlsx、/api/v1/audit/export.xlsx（anchor href，原始 fetch/anchor 上無 withCredentials 攔截器），以及對一份大型指南執行 help-guide exportPdf
- **Then** xlsx 下載帶有憑證並可解析為有效的 OOXML，含預期的工作表/欄位與 Content-Disposition 檔名；audit/violations 的 anchor 包含驗證 cookie（驗證 anchor 導覽會送出 cookie，因為攔截器只涵蓋 HttpClient，不涵蓋原始 anchor/fetch）；過期的工作階段回傳 401/重新導向而非損壞的檔案；help PDF 產生時無 OOM/空白頁

---

## Flyway 遷移 / 備份還原

### IT-MIGR-01 INT-217 資料遷移
- **Given** 一個從早期版本還原的 MSSQL DB 快照，具有 V1 時代的結構（含 tenant_id 欄位、缺少 before_json/after_json、無稽核簽章）並有已填入的 acme_orders/acme_accounts/acme_audit_log 列
- **When** 應用程式在 spring.flyway.baseline-on-migrate=true/baseline-version=1 下開機，且 FlywayConfig repair()+migrate() 將完整的 SQL+Java 鏈執行到 V96，接著是 AuditChainBackfillRunner 與 OrderFingerprintBackfillRunner
- **Then** 所有遷移依序成功（V3 移除 tenant_id、V17 設定 SUPERSEDED、V61 重新命名為 before_data/after_data + actor_user_id、V63 新增 subject）、Hibernate validate 通過（實體與結構相符）、舊有稽核列取得 previous_hash/signature、訂單取得 cert_fingerprint_sha256，且無資料遺失或重複

### IT-MIGR-02 INT-218 冪等性
- **Given** 一個 DB，其中一個 ADD COLUMN 遷移（例如 V37 acme_dns_* 或 V60 before_json）的 DDL 已套用但其 flyway_schema_history 列被標記為失敗（success=0），重現一次被中斷的升級
- **When** 應用程式重新開機，FlywayConfig.repair() 移除失敗的歷史列，接著 migrate() 嘗試再次執行該版本
- **Then** 該遷移要嘛可被證明為冪等（IF NOT EXISTS／可安全重新執行）且開機成功，要嘛該測試展示阻擋啟動的重複欄位失敗——精確量化究竟哪些遷移重新執行不安全

### IT-MIGR-03 INT-219 災難復原/備份
- **Given** 兩個已註冊的 ACME 帳戶（LE staging + 一個 TWCA 風格的 EAB 帳戶），已設定 account_key_pem、owner 與 assignee
- **When** Admin GET /api/v1/admin/dr/account-keys?passphrase=P（AES-GCM 信封），接著 acme_accounts 資料表被截斷（truncate），然後 admin 以 passphrase=P 與 dryRun=false POST 該信封至 /account-keys/restore
- **Then** 還原以相同的 account_key_pem 重建所有帳戶並保留 created_by/assignee 欄位，後續使用還原帳戶的續期/簽發針對該 CA 成功（帳戶被識別），且寫入 DR_EXPORT/DR_RESTORE 稽核事件

### IT-MIGR-04 INT-221 安全
- **Given** 一個已登入的 'user' 角色 JWT、一個 'supervisor' JWT,以及無 token 的狀態
- **When** 各自呼叫 GET /api/v1/admin/dr/account-keys 與 POST /account-keys/restore,並由 user 呼叫 GET /api/v1/audit 與 /audit/verify-integrity
- **Then** user/supervisor/匿名者在 DR 上均取得 403/401;user 在 /api/v1/audit 上取得 403;supervisor 被允許存取 /audit/verify-integrity,但不允許存取 /api/v1/audit 清單(符合 hasRole 與 hasAnyRole 的差異)——證明私鑰絕不會洩漏給較低權限角色

### IT-MIGR-05 INT-222 安全
- **Given** 一條已填入資料且完整簽署的 audit 鏈(每一列都有 previous_hash/signature)
- **When** 在 DB 中直接竄改某一列的 details/result,然後 (1) 在不重啟的情況下呼叫 /audit/verify-integrity,並 (2) 重啟應用程式讓 AuditChainBackfillRunner 執行,再次呼叫 verify-integrity
- **Then** (1) verify-integrity 在被竄改的那一列回報 firstBreakAt(偵測到竄改);(2) 重啟後 runner 重新簽署損壞的那一列(記錄一則 REPAIRED WARN),verify-integrity 此時回報乾淨——這說明了重啟時的自我修復會掩蓋竄改,因此必須觸發警報/轉送至 SIEM

### IT-MIGR-06 INT-225 安全
- **Given** SPRING_PROFILES_ACTIVE=prod 且 CLM_JWT_SECRET / CLM_ENCRYPTION_KEY / CLM_AUDIT_HMAC_SECRET 未設定
- **When** 應用程式開機
- **Then** 啟動快速失敗,並針對每個缺少的 secret 給出清楚訊息(JwtUtil.init、AuditChainService.init),allow-unlicensed 即使被覆寫也被強制為 false,cookie-secure 預設為 true,且 application-dev.yml 中寫死的 dev secret 明顯地未生效

### IT-MIGR-07 INT-220 負向/邊界
- **Given** 一個有效的加密 DR 信封與正確的密語
- **When** (a) 以錯誤的密語 POST 還原；(b) 以一個 accounts 陣列含有一筆 accountId 為 null 的紀錄與一筆 accountKeyPem 為垃圾非 PEM 的紀錄的備份 POST 還原；(c) 省略 accounts 與 envelope 來 POST 還原
- **Then** (a) 回傳 400 'envelope decrypt failed'；(b) 該 null-id 列出現在 conflicts 中且不被寫入，且系統要嘛驗證、要嘛清楚地記錄那個壞掉的 PEM，而非靜默地覆寫一把運作中的金鑰；(c) 回傳 400 'missing accounts'——且在所有情況下，於 dryRun=false 下都不發生部分/損壞的寫入

### IT-MIGR-08 INT-223 併發/競態
- **Given** 鏈序列化設計(synchronized chainLock + REQUIRES_NEW 在解鎖前提交 append)
- **When** 50 個並行的業務操作,各自發出一個 audit 事件(例如平行的訂單建立 / 核准),對一個共用 DB 執行
- **Then** 事後 verify-integrity 回報 verifiedRows == totalRows,且無 previous_hash 不符或分叉,確認 lock+REQUIRES_NEW 的排序在負載下,以及在 timestamp+id 平手判定下都能維持

### IT-MIGR-09 INT-224 併發/競態
- **Given** 兩個應用程式實例指向同一個 MSSQL DB(叢集部署),並啟用續期/清理排程器
- **When** 兩個實例的排程器同時觸發同一個任務時間窗
- **Then** 恰好只有一個實例取得 shedlock 列並執行;另一個無動作——以僅發生單次續期掃描 / 單次 audit 清理(而非兩次)來驗證

### IT-MIGR-10 INT-226 安全
- **Given** application.yml 設定 management.endpoint.health.show-details=always,且 SecurityConfig 允許未驗證存取 /actuator/health,並無 prod 覆寫
- **When** 一個未驗證的用戶端對 prod-profile 實例 GET /actuator/health
- **Then** 回應不得向匿名使用者揭露 DB 連線細節/元件內部資訊(預期目前會失敗,證明缺少 show-details=when-authorized 的 prod 覆寫)

### IT-MIGR-11 INT-227 災難復原/備份
- **Given** 一個在 T 時刻取得的原生 DB 備份,以及一個在 T-1 時刻取得的 DR 帳號金鑰匯出,期間在 T-1 與 T 之間建立了一個新帳號+訂單
- **When** DB 被還原至 T(內含參照新帳號的新訂單),而帳號金鑰從 T-1 的匯出還原(缺少新帳號)
- **Then** 系統浮現此不一致(訂單參照了一個沒有金鑰的 account_id),而非在下次續期時無聲地失敗——確立了兩份備份必須一起擷取的營運需求

### IT-MIGR-12 INT-228 授權強制
- **Given** 一個 Standard 階的 license(以及另外一個 legacy 的 null-flag license)
- **When** 讀取 GET /api/v1/license/status,並由 admin 呼叫 GET /api/v1/admin/dr/account-keys
- **Then** license status 對所有階別都回報 drBackup=true(依設計如此),且 DR 匯出無論階別都能運作——確認 DR 是刻意全階別開放的,且未強制執行的 feat_dr_backup 旗標與此意圖一致(對比 HSM/SIEM 是 Enterprise 閘控的)

### IT-MIGR-13 INT-229 冪等性
- **Given** 一個已含有 V90/V92 種子設定鍵(例如 hsm.enabled、siem.enabled)的 DB
- **When** 嘗試重放 migration 或手動重新種入那些裸 INSERT 述句(如同在一次失敗的還原/修復序列中可能發生的)
- **Then** 此行為被刻劃出來:裸 INSERT 會撞上 clm_settings PK 違反,顯示任何 seed 重放路徑都必須以 NOT EXISTS 防護(不同於 V65 風格的有防護 insert)

---

## 端到端業務旅程

### IT-E2E-01 INT-230 端到端生命週期
- **Given** admin 登入,並在一次上線流程中,POST /api/v1/letsencrypt/accounts(無金鑰 LE)、POST /api/v1/admin/dns-providers(Cloudflare,健康檢查綠燈)、POST /api/v1/admin/endpoints(type=f5)、PUT /api/v1/dns-bindings/{cn}(AUTO_PLUGIN -> Cloudflare,首次設定立即套用)、POST /api/v1/admin/policies(一個 WARN 基準使簽發不被阻擋)、以及 PUT /api/v1/autorenew/policies/{cn}
- **When** user1 POST /api/v1/letsencrypt/accounts/{accountId}/orders(預期 HTTP 202 pendingApproval=true,body.orderId = cir_*);sup1 GET /api/v1/admin/cert-issuance-requests 看到它並 POST .../{requestId}/approve;系統透過 Cloudflare 發佈 _acme-challenge TXT,dnsPublishStatus 走過 NONE->PUBLISHING->PUBLISHED,訂單走過 PENDING_DNS/VERIFYING->PROCESSING->ISSUED(輪詢 GET .../orders/{realOrderId});一個到 F5 endpoint 的部署綁定被核准,部署工作走過 PENDING_APPROVAL->QUEUED->RUNNING->SUCCESS 且 LiveTlsProbe 確認所提供的葉憑證指紋;接著自動續期排程器(或 POST /api/v1/certs/renew-now)觸發,一個 RENEWAL CIR 被核准,一個新的 ISSUED 訂單到達
- **Then** 原始 CIR 的 createdOrderId 被設定;訂單抵達 ISSUED 且具備非 null 的 cert_fingerprint_sha256 與 cert_pem;dnsPublishStatus 最終為 CLEANED(TXT 已從 Cloudflare 移除);部署工作為 SUCCESS 且 afterSerial 與所簽發的憑證相符;續期後,舊訂單翻轉為 SUPERSEDED,其任何待處理的部署工作變為 SKIPPED,新訂單為 ISSUED 並經即時探測;一封 cert.issued email 被排入佇列(clm_email_log status SENT)且不會為被取代的憑證寄出到期 email;HMAC audit 鏈包含 CERT_REQUEST_SUBMITTED/APPROVED、DNS 發佈、DEPLOY_APPROVED/SUCCESS,且 GET /api/v1/audit/verify-integrity 回傳 intact=true

### IT-E2E-02 INT-231 端到端生命週期
- **Given** admin POST /api/v1/twca/accounts 但「不帶」eabKeyId/eabHmacKey 並預期被拒絕(對 requiresEab 的 provider 而言 EAB 為必填),接著再次 POST 並「帶」一個有效的一次性 EAB -> 帳號已註冊,account_key_pem 在靜態時加密
- **When** 一個 user 提交 POST /api/v1/twca/accounts/{accountId}/orders(HTTP 202 CIR),supervisor 核准 -> AcmeOrderService.createOrder 對 TWCA 執行 DNS-01 流程,訂單抵達 ISSUED;接著作為負向補償探測,嘗試用「同一個」已燒毀的 EAB 註冊「第二個」帳號,並嘗試 DELETE 後再重新註冊
- **Then** 第一次簽發在 provider=twca 之下端對端成功,具備正確的 provider/帳號連結,且 group_id 反正規化至該訂單;第二次重用已消耗 EAB 的註冊在 CA 處失敗;測試斷言「沒有」本地回滾/補償(在 AccountBuilder.create 之後但 DB 儲存之前的一次網路中斷會燒毀 EAB 而無本地紀錄),因此 runbook 的預期「失去帳號 => 需要一個全新的 EAB」成立;audit 鏈記錄 ACCOUNT 註冊與 provider=twca 的 CERT_REQUEST_APPROVED

### IT-E2E-03 INT-232 權限/多租戶隔離
- **Given** admin@local 提交一個訂單 POST /api/v1/letsencrypt/accounts/{accA}/orders(HTTP 202,CIR requestedByEmail=admin@local);另外 sup1 在 group A 提交一個 CIR
- **When** admin@local 呼叫 POST /api/v1/admin/cert-issuance-requests/{ownRequestId}/approve;且 sup1 對 sup1 自己提交的 CIR 呼叫 approve
- **Then** 兩個 approve 呼叫都以 HTTP 409 衝突失敗,並帶訊息 'SoD 職責分離:申請人不可核准自己的申請';CIR 維持 PENDING 且 createdOrderId=null;沒有 OrderEntity 被建立,也沒有 CA 呼叫被發出(斷言 acme_orders 數量不變);一個「不同」的核准者(sup1 核准 admin 的申請,或 admin 核准 sup1 的)會成功並建立訂單——證明該閘特別阻擋自我核准,而非阻擋所有核准;audit 記錄失敗的 approve 嘗試

### IT-E2E-04 INT-233 權限/多租戶隔離
- **Given** sup1(supervisor,僅為 group B 的成員)登入
- **When** sup1 嘗試跨群組的這組操作:GET/PATCH/DELETE /api/v1/letsencrypt/accounts/{accA}、GET orderA1 及其 /deploys 與 /certificate、GET /api/v1/admin/cert-issuance-requests(group A 的 CIR)、對 group A 的 CIR POST approve、GET /api/v1/dns-bindings/{a1.example.com}、PUT 該綁定、GET a1.example.com 的 autorenew 政策、GET /api/v1/admin/deploy-approvals 並核准 group A 的工作,以及 GET /api/v1/admin/dns-providers
- **Then** 每個 group A 物件的讀/寫都回傳 404 not-found(隱藏存在性,而非 403),且每個 group A 佇列對 sup1 而言都是空的;對 group A 的 CIR 與 group A 的部署工作的核准都因 not-found 而失敗;group A 之 CN 的 autorenew/DNS 綁定不可見(CnAccessGuard);關鍵地斷言所記錄的「洩漏」:GET /api/v1/admin/dns-providers 對 sup1 回傳了 group A 連接器的顯示名稱/類型/zone(無群組篩選)——這必須被標記為一個失敗的跨租戶揭露;group A 中的 user1 仍只看見 assignee==self(orderA1)而非 orderA2

### IT-E2E-05 INT-234 合規強制
- **Given** BLOCK 政策已啟用且範圍為 global
- **When** (a) 一個 user 提交該 wildcard/60-SAN 訂單 POST .../orders;(b) 對於簽發後這一段,提交一個在申請時符規的 CN,被核准並由 CA 簽發,其葉憑證觸發一條憑證內容規則
- **Then** (a) 該提交(或在簽發前評估點的核准)回傳 HTTP 400 error=policy_violation 並帶違規的 ruleKey/enforcement,沒有 CIR 進到 CA 呼叫,且寫入一筆新的開啟中 PolicyViolation(severity ERROR);(b) 簽發後的違規「不會」阻擋簽發——憑證抵達 ISSUED 並維持可下載,自動部署被 SUPPRESSED(沒有部署工作排入佇列 / blockDeploy=true),產生一個 POST_ISSUANCE_POLICY_VIOLATION audit + cert.failed email,且憑證「不會」被自動撤銷;GET /api/v1/admin/policies/violations 同時列出兩者

### IT-E2E-06 INT-235 授權強制
- **Given** FQDN 用量略低於 300 上限,且 CA/DNS 類型計數處於其 Standard 限制
- **When** 提交一個其 CN-union-SAN 使不重複 FQDN 超過上限的訂單;提交數個未達上限的 CIR,然後接連核准它們以探測 check-then-act 競態;POST /api/v1/certs/import 一個超出配額的 5-SAN PEM;POST /api/v1/providers 一個第 3 個 CA 類型,並 POST /api/v1/admin/dns-providers 一個第 4 個 DNS 類型;最後切換至過期的 license 並重試一個「新」訂單提交與一個「續期」核准
- **Then** 超上限的「新」提交/核准以 'FQDN 上限' 訊息被拒絕,且不建立任何訂單;超出配額的匯入被拒絕;第 3 個 CA 類型與第 4 個 DNS 類型被配額阻擋;續期「不」消耗配額(一個 RENEWAL CIR 即使在上限也能核准);在「過期」的 license 之下 canIssue()=false,因此新提交與續期核准都被阻擋,而現有的 ISSUED 憑證仍可下載/部署;並行探測記錄接連核准是否能超過上限(事實指出 check-then-insert 不是原子的)

### IT-E2E-07 INT-236 安全
- **Given** user1 擁有(為其 assignee)該 ISSUED 訂單
- **When** user1 GET /api/v1/letsencrypt/accounts/{acc}/orders/{id}/domain-key(舊版直接端點);user1 POST /api/v1/download-requests format=domain-key(預期對一般 user 被阻擋),接著 sup1 提交 format=domain-key;申請者嘗試自我核准;一個第二核准者核准(設定 15 分鐘 TTL);申請者透過 GET /api/v1/download-requests/{id}/download 下載一次,然後再下載第二次;最後讓一個 APPROVED 申請超過 15 分鐘後嘗試下載
- **Then** 直接的 /domain-key 端點對所有人回傳 403 approval_required;一般 user 甚至無法申請 domain-key/pfx(僅限 admin/supervisor);自我核准被 SoD 阻擋;在一個不同的核准者核准後,第一次下載成功且所產生的金鑰與該訂單已解密的 domain_key_pem 相符;第二次下載失敗(已消耗,單次使用 CAS);過期時間窗的下載失敗;一個 SECRET_DOWNLOADED audit 事件記下原始申請者與時間,且 verify-integrity 維持 intact

### IT-E2E-08 INT-237 端到端生命週期
- **Given** group A 中一個 ISSUED 憑證
- **When** sup1 POST /api/v1/letsencrypt/accounts/{acc}/orders/{id}/revoke(HTTP 202 PENDING_APPROVAL,訂單維持 ISSUED);申請者嘗試自我核准(被阻擋);一個第二核准者核准且 CA 確認撤銷;「另外」,注入一個 CA 失敗並核准另一個撤銷;接著 admin POST /api/v1/admin/revoke-requests/break-glass 對一個金鑰外洩的訂單,並 POST .../break-glass/by-cn 以撤銷該 CN 的所有 ISSUED 憑證
- **Then** 在 PENDING 期間訂單維持 ISSUED;在 CA 確認撤銷時訂單翻轉為 REVOKED 且 FQDN 配額被釋放;在 CA 失敗時訂單「維持」ISSUED 且 operator 看到一個失敗(沒有虛假的『已撤銷』);緊急破窗建立一筆 selfApproved=true 的 APPROVED 列與 bypassedSoD audit,無第二核准者即撤銷;break-glass-by-CN 迴圈處理所有相符的 ISSUED 訂單(若某次 CA 呼叫在迴圈中失敗,斷言部分失敗行為);寫入 REVOKE_BREAK_GLASS audit,且(若為 Enterprise)轉送至 SIEM

### IT-E2E-09 INT-239 安全
- **Given** 一個 user 透過 POST /api/v1/me/mfa/setup 然後 /verify-and-enable 註冊 MFA
- **When** 該 user 以密碼登入(POST /api/v1/auth/login -> 202 mfaRequired+mfaToken)並以一個有效的 TOTP 完成 POST /api/v1/auth/mfa-verify;在一次新的登入中於 90 秒內重放同一個 TOTP 碼;設定 auth.mfa.required.for.admin=true,讓一個「尚未」註冊的 admin 嘗試 admin 密碼登入;接著同一個已註冊 MFA 的 user(或被強制 MFA 的 admin)透過 GET /api/v1/auth/oidc/login -> /callback 登入
- **Then** 密碼+TOTP 登入發出 JWT cookie 並寫入 LOGIN_MFA_VERIFIED;在強制政策下未註冊的 admin 在密碼登入時被阻擋並帶 mfa_required_not_enabled(且「沒有」admin 重設路徑——斷言此復原缺口);關鍵地,OIDC callback 即使對已註冊/被強制 MFA 的 user 也會在「沒有」MFA 挑戰的情況下發出一個完整 session——將其標記為一個失敗的安全降級;TOTP 重放探測記錄同一個碼是否在時間窗內被接受兩次(事實指出沒有已消耗碼的追蹤)

### IT-E2E-10 INT-238 資料遷移
- **Given** 一個現有的憑證要納入管理
- **When** POST /api/v1/certs/import(單筆)-> IMPORTED 訂單;透過 /import/batch/preview 然後 /commit 選取有效列來批次處理;匯入弱 PEM;接著為該匯入的 CN PUT 一個 DNS 綁定 + PUT 一個 autorenew 政策並執行 POST /api/v1/certs/renew-now;最後為「同一個」CN 匯入一個更新的憑證,如同一個即時的 ISSUED ACME 憑證
- **Then** 匯入的憑證顯示為 IMPORTED,計入儀表板總數與 FQDN 配額(5-SAN 消耗 5);弱 PEM 在「沒有」任何合規違規的情況下匯入(匯入繞過 PolicyEngine——明確斷言此缺口);在綁定前,renew-now 「不會」續期該匯入的 CN;在綁定+政策之後,RENEWAL CIR 被排入佇列且(在核准時)續期;同一 CN 的更新匯入使較舊的 ISSUED ACME 憑證翻轉為 SUPERSEDED 並串接 SKIP 其待處理的部署工作(展示按錯鍵降級的風險);批次拒絕重複 CN 列與超出配額列並附逐列錯誤

### IT-E2E-11 INT-240 安全
- **Given** 該 user 有一個有效的 access token 與一個登記於 clm_refresh_token_registry 的有效 refresh token
- **When** 該 user POST /api/v1/auth/change-password(正確的舊密碼);另外一個 admin POST /api/v1/users/{userId}/revoke-sessions;在每次之後立即,受影響的 user 以「舊」access token 呼叫一個受保護端點,並也嘗試以舊 refresh token POST /api/v1/auth/refresh;最後在 access TTL 過後重試該受保護呼叫
- **Then** change-password 撤銷該 user 的「所有」refresh token(reason PASSWORD_CHANGED)且 admin 撤銷設定 ADMIN_REVOKE;舊 refresh token 在 /refresh 失敗(isActive=false);「但」仍有效的 access token 會繼續授權受保護呼叫直到其 TTL 過期,之後該呼叫 401 且無靜默 refresh 成功;audit 記錄 PASSWORD_CHANGED 與 ADMIN_REVOKE_SESSIONS;斷言所記錄的預期『即時清除』其實是『在 access TTL 內清除』

### IT-E2E-12 INT-241 韌性/故障轉移
- **Given** 應用程式被重啟(恢復 runner 執行 findAll 並恢復進行中的訂單)
- **When** 恢復 runner 在啟動時對種入的訂單表執行
- **Then** 新的 VERIFYING 訂單被硬性恢復(重新輪詢),>6h 過期的 VERIFYING 訂單被標記為 FAILED(安全/可重試,而非無聲卡住),PENDING_DNS 訂單維持靜止(刻意不自動恢復)並維持等待手動 /trigger;關鍵地,孤兒 CIR(APPROVED,createdOrderId=null)被顯示為「沒有」恢復 sweeper——該憑證無聲地永不簽發——測試將其斷言為一個已知缺口(讓產品/QA 可決定加上一個 sweeper);單節點執行不發生重複的 order.execute()

### IT-E2E-13 INT-242 韌性/故障轉移
- **Given** 外部相依被強制進入失敗模式
- **When** (a) 自動 DNS 管線發佈 TXT 但傳播在 2.5 分鐘時間窗內從未收斂;(b) CA 驗證在 VERIFYING/PROCESSING 期間逾時或回傳一個速率限制錯誤;(c) 在一次成功簽發後,部署工作對一個停機的 endpoint 執行,或(webhook)回傳沒有指紋
- **Then** (a) 寬鬆的自動管線在警告後「仍」觸發 CA(斷言相對於嚴格手動預檢所記錄的不對稱),且所產生的 CA 失敗將訂單標記為 FAILED 並帶 error_message——而非永久 VERIFYING;(b) CA 逾時/429 浮現為 markFailed(FAILED),而非一個無聲卡住的執行緒(且測試指出沒有 429/Retry-After 退避,因此快速重試可能加重一次封禁);(c) 部署工作以 FAILED 結束(webhook inventory()=null 一律 fail-close;F5/webhook 在沒有先前金鑰的情況下無法回滾),憑證維持 ISSUED,且一個 cert.failed/owner 通知被排入佇列;audit 擷取每個失敗

### IT-E2E-14 INT-243 可觀測性
- **Given** 由一段完整商業流程所累積出的稽核鏈
- **When** 對完整無缺的稽核鏈呼叫 GET /api/v1/audit/verify-integrity;接著直接在 MSSQL 中 UPDATE acme_audit_log 內某一格 before_data/after_data,並重新執行 verify-integrity;重新啟動應用程式(AuditChainBackfillRunner 會執行)後再次驗證;若有兩個應用程式節點可用,則在兩個節點上同時驅動受稽核的寫入;在啟用 Enterprise+SIEM 的情況下,確認事件以 CEF 鏡像送至 syslog sink
- **Then** 完整無缺的鏈回報 intact=true 且 verifiedRows==totalRows;被竄改的列會使 verify-integrity 回報 intact=false,firstBreakAt 指向被編輯的列,原因為 signature/previousHash 不符;關鍵在於重新啟動後,backfill 會「靜默地」重新簽署該損壞列,verify-integrity 又回報乾淨——本測試斷言此自我修復會遮蔽竄改(一項已記載的誠實性注意事項);兩節點同時寫入會產生兩列具有相同 previous_hash 並引發誤判的竄改警報(鏈分岔)——標記為叢集部署的限制;SIEM 鏡像顯示 severity 7 的 FAILURE 事件

### IT-E2E-15 INT-244 災難復原/備份
- **Given** 一套運作中的來源系統,其帳號金鑰是無可取代的 DR 資產
- **When** admin 呼叫 GET /api/v1/admin/dr/account-keys?passphrase=X(加密信封);在 private-key.export.forbidden=true 下嘗試明文匯出(必須被阻擋);在一個全新的目標節點上,以 dryRun=true(預設)再以 dryRun=false 呼叫 POST /api/v1/admin/dr/account-keys/restore;還原後,對 CA 在已還原的帳號上提交並核准一張新訂單
- **Then** passphrase 匯出回傳一個 AES-GCM 信封,並稽核為 DR_EXPORT_ACCOUNT_KEYS(僅含 count+sha256,不含金鑰素材);原則開啟時明文匯出為 fail-closed;dryRun=true 回傳差異(wouldCreate/wouldUpdate/conflicts)且不寫入任何資料;dryRun=false 進行 upsert,且還原的 account_key_pem 以「目標」節點的金鑰重新加密(在目標端可解密,證明 API 還原路徑可跨加密金鑰移植——不同於原始 DB 複製);在已還原帳號上的新簽發能端對端成功,證明 CA 帳號的延續性;verify-integrity 保持 intact

---

## 安全 / 滲透測試

### IT-PEN-01 INT-245 安全
- **Given** userA 已通過認證(JWT cookie),且 order_B 屬於群組 B 中的 account_B,而 userA 並非群組 B 的成員
- **When** userA 呼叫 GET /api/v1/{provider}/accounts/{account_B}/orders/{order_B}/certificate(並對 domain-key 透過 download-request、DELETE、/trigger、/revoke 重複進行)
- **Then** 伺服器對每個動詞皆回傳 404 account_not_found(AccountMismatchException -> 404 以避免揭露存在性);userA 從未取得 order_B 的 cert_pem、status、CN 或 SAN,且沒有任何稽核列顯示成功的跨群組讀取

### IT-PEN-02 INT-246 權限/多租戶隔離
- **Given** userA 持有有效的 user 角色 JWT,且 SPA 隱藏了 admin 路由,但 API 才是真正的邊界
- **When** userA 直接 POST /api/v1/admin/cert-issuance-requests/{id}/approve、POST /api/v1/providers、POST /api/v1/admin/dns-providers、DELETE /api/v1/admin/policies/{id},以及 GET /api/v1/audit
- **Then** 每個呼叫在任何副作用發生前都被 @PreAuthorize 以 403 forbidden 拒絕;沒有任何 CIR 從 PENDING->APPROVED、沒有任何 provider/DNS 帳號被建立、沒有任何 policy 被刪除,也沒有回傳任何稽核紀錄

### IT-PEN-03 INT-247 安全
- **Given** adminX 透過 POST /api/v1/{provider}/accounts/{accountId}/orders 提交一張新訂單,建立一個 PENDING 的 clm_cert_issuance_request,其 requested_by_email = adminX
- **When** 同一個 adminX 對其自身的請求呼叫 POST /api/v1/admin/cert-issuance-requests/{requestId}/approve
- **Then** 核准對「所有」角色(包含 admin)硬性封鎖(CertIssuanceApprovalService 沒有 admin 自我核准的例外);請求保持 PENDING、createOrder/AcmeOrderService 從未被呼叫、沒有任何訂單列被建立,且該拒絕被稽核

### IT-PEN-04 INT-249 安全
- **Given** 建立一個由攻擊者控制或設定錯誤的 F5 endpoint,其 connection.host=169.254.169.254(或某個內部 metadata host),verifyTls=false
- **When** 執行 POST /api/v1/admin/endpoints/{id}/test-connect(F5Driver.baseUrl 構建出 https://169.254.169.254:443,「沒有」SafeOutboundUrl.validate,不同於 webhook/k8s)
- **Then** 測試斷言目前的程式碼會抵達 metadata IP(發現),並釘選所需修正:F5Driver.baseUrl(以及 SshDriver host、DNS provider URL)必須呼叫 SafeOutboundUrl.validate,並以阻擋錯誤拒絕 169.254.169.254/168.63.129.16/100.100.100.200,與 WebhookDriver/K8sSecretDriver 一致

### IT-PEN-05 INT-251 安全
- **Given** 將一個 ssh endpoint 的 installSpec.reloadCmd 設定為串接一個被注入命令的酬載
- **When** 一個已核准的部署作業執行,SshDriver.deploy 透過 exec(sess, reloadCmd) 執行 reloadCmd,且「沒有」shEscape(檔案路徑使用 shEscape,reloadCmd 則否)
- **Then** 測試斷言被注入的命令會執行(目前行為 = 發現),並釘選需求:reloadCmd 必須被驗證/escape 或限制為一份允許清單;同時斷言唯有帶核准的 admin/supervisor 才能抵達此路徑

### IT-PEN-06 INT-253 安全
- **Given** 一個有效的 role=user clm_access JWT
- **When** 測試者提交:(a) role claim 被編輯為 admin 而簽章未變的 JWT;(b) 以 alg=none 重新簽署的 token;(c) 以公鑰作為 HMAC 金鑰、以 RS256 重新簽署的 token(alg-confusion);(d) 超過 access TTL 的過期 JWT
- **Then** 四者皆被拒絕:parseSignedClaims 要求有效的 HS256 簽章,因此酬載竄改與 alg=none/RS256 confusion 皆會通不過簽章驗證(401);過期 token 透過標準 exp 檢查被拒絕(401)。沒有任何請求以偽造身分被服務

### IT-PEN-07 INT-255 安全
- **Given** maker-checker 的 JIT 下載流程對所有含金鑰的格式設下閘門
- **When** 測試者嘗試:(a) 直接 GET /orders/{id}/domain-key;(b) 直接 GET .../certificate.pfx;(c) role=user 請求 format=domain-key/pfx;(d) 已核准的一次性連結被第二次重放;(e) 由不同於請求者的使用者下載;(f) 在 15 分鐘 TTL 之後使用;(g) admin 自我核准自身的請求
- **Then** (a)(b) 回傳 403 approval_required(硬性停用直接拉取);(c) 被阻擋(pfx/domain-key 僅 admin/supervisor 可請求);(d) 第二次下載被 markConsumedIfDownloadable CAS 阻擋(一次性);(e) 僅原始請求者可下載;(f) 過期連結被拒絕;(g) admin 自我核准成功,但寫入 SECRET_DOWNLOADED + emergencySelfApprove=true 的稽核——斷言此稽核證據存在,並標記單一 admin 的金鑰提取為一項殘餘的控管缺口

### IT-PEN-08 INT-260 安全
- **Given** 一個以正式 profile 設定的實例
- **When** 未認證的測試者 GET /api/v1/auth/dev-users、/api/v1/license/status 與 /actuator/health;以及一個 build/CI 檢查審視 dev 密碼重設(testtest)或 DevAuthController bean 是否可能被載入
- **Then** /api/v1/auth/dev-users 回傳 404/未載入(DevAuthController 為 @Profile(dev),因此即使該 URL 為 permitAll 也沒有 bean);未暴露任何已植入的 email 清單或 testtest 密碼;/actuator/health 不得對匿名呼叫者回傳 show-details=always 的元件/DB 內部資訊(若有則斷言為發現);/api/v1/license/status 在未認證下外洩 customer_name+expiry——標記為資訊揭露,並釘選一個決策:設下閘門或接受它

### IT-PEN-09 INT-248 安全
- **Given** userB 提交一個 CN<->endpoint 綁定(POST /api/v1/certs/domains/{cn}/bindings,requested_by=userB),由 supervisorA 核准,接著 userB 觸發一次手動部署(POST /api/v1/orders/{orderId}/deploy,triggeredBy=userB)
- **When** supervisorA 同時也透過 POST /api/v1/admin/deploy-approvals/{jobId}/approve 核准由此產生的部署作業
- **Then** 測試斷言此已記載的行為:綁定 SoD 使用 requestedByEmail!=approver,而部署作業 SoD 使用 triggeredBy!=approver——因此整條鏈僅以 supervisorA 作為唯一相異核准者即可完成。這必須被標記為一項發現(實質上的單一核准者路徑),並將此預期釘選下來,使修正(要求整條鏈需有兩名相異核准者)可被驗證

### IT-PEN-10 INT-250 安全
- **Given** 一個 webhook endpoint URL 使用 rebind.attacker.test,在驗證時解析為一個公開 IP
- **When** SafeOutboundUrl.validate 通過(公開 IP),接著 JDK HttpClient 在連線時重新解析,而此網域已重新繫結至 169.254.169.254
- **Then** 測試確認此已記載的缺口(驗證時與連線時的解析不符)使請求得以打到 metadata IP,並釘選緩解需求(解析後固定 IP 連線,或在重新解析變更時拒絕)。RFC1918/localhost 仍刻意被允許;唯有 metadata rebinding 才是失效

### IT-PEN-11 INT-252 權限/多租戶隔離
- **Given** userA 已通過認證且非群組 B 的成員
- **When** userA 呼叫 GET /api/v1/admin/dns-providers 與 GET /api/v1/dns-bindings/auto-detect?fqdn=app.groupB-domain.com
- **Then** 斷言發現:GET /api/v1/admin/dns-providers(@PreAuthorize isAuthenticated,無群組篩選)回傳群組 B 的 connector type/displayName/managedZones/delegationZone/groupId;auto-detect(無 @PreAuthorize、無 canAccessCn)回傳群組 B 相符的 accountId/zone。釘選所需修正:依 visibleGroupIds 篩選清單,並對 auto-detect 加上 canAccessCn/群組防護,與受防護的 GET /api/v1/dns-bindings/{cn} 一致

### IT-PEN-12 INT-254 安全
- **Given** 使用者持有 refresh token RT1
- **When** (a) 使用者登出(RT1 以 LOGOUT 撤銷)後在 POST /api/v1/auth/refresh 重放 RT1;(b) 使用者在 /refresh 將 RT1->RT2 輪替,然後重放已被輪替的 RT1;(c) 兩個並發的 /refresh 呼叫並行重用 RT1(多分頁競態)
- **Then** (a) 與 (b) 皆被拒絕:registry.isActive 對已撤銷/已輪替的 token 回傳 false(401,未鑄造新族)。(c) 是已記載的競態:斷言目前行為(可鑄造出兩族)為一項發現,並釘選需求——重放已輪替 token 時應有重放偵測以撤銷整族

### IT-PEN-13 INT-256 授權強制
- **Given** 一個運作中、已授權的實例
- **When** 測試者:(a) 對 /api/v1/admin/license/activate POST 一個手工製作/RS256 簽署的 token;(b) 直接 UPDATE clm_license 以拉高 quota_fqdn/tier/expires_at 後重新啟動;(c) 將 OS 時鐘往回設至超過 _license.clock.hwm 並嘗試一張新訂單
- **Then** (a) 回傳 400 invalid_license(SDK ECDSA 驗證失敗;RS256 參考 token 無法通過驗證);(b) 重新啟動時該列從原始簽署 token 重新推導,因此被竄改的 quota/tier/expiry 被抹除;(c) 狀態翻為 CLOCK_TAMPERED 且 canIssue()=false 阻擋 POST orders

### IT-PEN-14 INT-257 授權強制
- **Given** 對一個 Standard 授權而言,目前相異 FQDN 用量為 N-1
- **When** (a) 嘗試一次將相異 CN∪SAN 推過 N 的單一匯入/訂單;(b) 並發提交/核准兩個消耗 FQDN 的匯入/訂單,各自皆見到 N-1;(c) 建立第 3 個 CA connector 與第 4 個 DNS provider type
- **Then** (a) 以超出配額的『contact Ruenxin』訊息被拒絕;(c) CA 建立在超過 2 個 type 時被拒絕,DNS 建立在超過 3 個相異 type 時被拒絕;(b) 是已記載的競態:斷言兩者是否皆成功(超出,發現),並釘選需求——在核准時進行原子/加鎖的配額檢查

### IT-PEN-15 INT-258 合規強制
- **Given** 一個 BLOCK 強制的原則處於活躍狀態,範圍涵蓋群組/全域
- **When** (a) 使用者透過 POST /api/v1/{provider}/accounts/{accountId}/orders 提交一張其 CN/SAN 違反規則的訂單;(b) 同一張違規憑證透過 POST /api/v1/certs/import 匯入;(c) 一個請求層級的規則(CN regex)使用病態/ReDoS 模式
- **Then** (a) 在簽發前以 HTTP 400 policy_violation 被拒絕,且未建立任何 CIR/訂單;(b) 匯入成功且「沒有」記錄任何違規——斷言此已記載的繞過為一項發現(匯入從不呼叫 PolicyEngine);(c) 一個無效的 regex 會 fail open(該規則靜默地從不觸發)——斷言並標記,並確認病態模式不會使訂單執行緒無上限地懸掛

### IT-PEN-16 INT-259 合規強制
- **Given** 一條已驗證完整無缺的稽核鏈
- **When** (a) 測試者在 DB 中編輯一列 acme_audit_log 的 after_data,然後呼叫 GET /api/v1/audit/verify-integrity;(b) 刪除一列鏈中途的列;(c) 一個 role=user 接著一個 supervisor 呼叫 GET /api/v1/audit 與 /export.xlsx;(d) 竄改後重新啟動應用程式並重新驗證
- **Then** (a) verify-integrity 回報 intact=false,firstBreakAt 指向被竄改的列(簽章不符);(b) 在孤立的連結處回報 previousHash 不符;(c) /api/v1/audit 與 export 對 user 與 supervisor 皆為 403(僅限 ADMIN,因為列不帶 group_id);(d) 是已記載的危害——AuditChainBackfillRunner 於開機時自我修復損壞列並回報乾淨:斷言此舉遮蔽竄改,並釘選需求——不得靜默地重新簽署失敗的列

### IT-PEN-17 INT-261 權限/多租戶隔離
- **Given** 認證優先序為 cookie > Bearer > X-API-Key,且 ApiKeyPrincipal 將舊式 key 對應為 role=admin + isServiceAccount=true
- **When** (a) 舊式 key 呼叫跨群組 admin 端點(GET /api/v1/audit、核准任何 CIR、列出所有憑證、/admin/dr/account-keys 匯出);(b) 範圍化 key 嘗試相同的跨群組/僅限 admin 動作以及一個僅限群組 A 的動作;(c) 一個人類提交一個 CIR,由舊式 key 核准它
- **Then** (a) 舊式 key 處處成功(全域繞過)——斷言並標記為已記載的波及半徑風險,建議使用範圍化 key;(b) 範圍化 key 受限於群組 A,無法打到僅限 admin 的端點或其他群組(canAccessGroup 尊重 ak.groupIds);(c) 舊式 key 可核准一個人類提交的 CIR,因為 SoD 僅比較 email——斷言此單一 key 橡皮圖章路徑為一項發現

### IT-PEN-18 INT-262 安全
- **Given** 一個本機 endpoint 的 installSpec 路徑被設為一個穿越酬載,且一張訂單帶有惡意的 CN/SAN/notes 值
- **When** 一個已核准的部署作業執行 LocalFileDriver 的原子寫入;另外分開地,建立一張訂單,其 CN/SAN/notes 欄位含有 CRLF + 一行偽造的稽核行,以及 SQL/原則注入字元
- **Then** (a) LocalFileDriver 拒絕/正規化穿越,使寫入留在設定的憑證目錄內且無法覆寫系統檔案;(b) 帶有換行/控制字元的 CN/SAN/notes 被儲存並回顯,而不會偽造稽核紀錄行、不會破壞 HMAC 正規化,也不會造成 SQL 注入(參數化 JPA)——驗證一個精心製作的 CN 不會將一筆假條目注入 acme_audit_log 或改變 verify-integrity

### IT-PEN-19 INT-263 安全
- **Given** 一個前面沒有 LB 的單一實例
- **When** (a) 60 秒內從單一 IP 進行 >10 次登入嘗試;(b) 對單一帳號 5 次錯誤密碼;(c) 對一個已知 admin email 反覆輸入錯誤密碼以使其維持鎖定;(d) 若有第 2 個節點可用,在輪詢式 LB 後方進行相同爆量
- **Then** (a) 在達到容量後回傳 429 + Retry-After;(b) 帳號鎖定 15 分鐘(403 account_locked);(c) 斷言已記載的每帳號鎖定 DoS(攻擊者可反覆鎖定一個 admin)為一項發現;(d) 斷言記憶體內的限制器是每 JVM,因此有效上限會乘以節點數——釘選 HA 需求(共享/Redis bucket 或黏著工作階段)

---

## 併發 / 競態進階

### IT-RACE-01 INT-279 併發/競態
- **Given** 兩個 RXCLM 節點 A 與 B 指向同一個 MSSQL。clm_autorenew_policy 中有一列為 CN renew.example.com 而啟用/未暫停/未升級的政策,其目前 ISSUED 訂單的 cert_not_after 落在 threshold_days 之內。shedlock 列 'autorenew-scan' 為空閒。
- **When** 兩個節點的 AutoRenewScheduler tick 在同一秒內觸發(對齊系統時鐘／手動同時觸發兩者)。每個節點對 LockConfiguration('autorenew-scan', lockAtMostFor=30m, lockAtLeastFor=0) 嘗試 executeWithLock,接著執行 findCurrentIssued + hasInFlight(cn) 與 submitRenewal。
- **Then** 恰好「一個」節點取得 ShedLock 並執行 doScan;另一個節點記錄 lock-not-acquired 並略過。為 renew.example.com 恰好建立「一筆」clm_cert_issuance_request(type=RENEWAL)。沒有第二筆 PENDING CIR、沒有第二個 ACME order 被建立。不變式:每個掃描週期恰好提交一次續約。

### IT-RACE-02 INT-281 併發/競態
- **Given** 存在兩列 PENDING clm_cert_issuance_request,具「相同」的 common_name 與 account_id 但不同的 request_id(提交時去重僅阻擋第二筆完全相同的 PENDING,但兩筆在任一提交完成前的近乎同時提交、或兩個 SAN 變體,都可能留下兩列 PENDING 同層列)。該 CN 尚無進行中的 order。
- **When** Supervisor S1 在毫秒內 POST /api/v1/admin/cert-issuance-requests/{cir_A}/approve,而 Supervisor S2 為 {cir_B} POST /approve。各自獨立執行 hasInflightOrderForCn(對兩者皆仍為 false),接著對其「自身」的 requestId 執行 markApprovedIfPending(兩者皆成功——CAS 只保護單一列),然後 AcmeOrderService.createOrder。
- **Then** 該 CN 只能建立「一個」ACME order;第二筆核准必須在 hasInflightOrderForCn 重新檢查或 DB 層級守衛上失敗。若兩者皆成功即為缺陷(重複 CA order、LE 重複憑證速率限制耗用、兩張有效憑證)。不變式:不論同時核准多少同層 CIR,每個 CN 恰好簽發一次。

### IT-RACE-03 INT-282 併發/競態
- **Given** 為 CN single.example.com 存在一列 PENDING clm_cert_issuance_request cir_X,同一群組中的兩位 supervisor S1 與 S2 皆可見。
- **When** S1 與 S2 同時 POST /api/v1/admin/cert-issuance-requests/cir_X/approve。兩者皆通過 PENDING 預先檢查(陳舊讀取),接著在 markApprovedIfPending(cir_X) 上競態。
- **Then** markApprovedIfPending 對恰好一位呼叫者回傳 affected=1(其繼續執行 createOrder),對另一位回傳 affected=0(其取得 HTTP「request not pending: already handled」)且「不」呼叫 CA。恰好建立一個 OrderEntity;created_order_id 僅設定一次。不變式:每 requestId 的 CAS 即使在平手下也保證恰好一次。

### IT-RACE-04 INT-285 併發/競態
- **Given** 兩個 RXCLM 節點各自處理一筆 order,其 CN 解析至「同一」受管理 zone 中、位於 read-modify-write RRset provider(例如 Route53)上的 _acme-challenge.shared.example.com。DnsAutomationService 的 256-stripe rmwLock 在每個節點上皆為 JVM 本地,因此兩個節點「不」共用該鎖。
- **When** 兩個節點同時在相同的 fqdn RRset 上發佈其 challenge TXT(或一個發佈、另一個清理);各自對完整的 TXT 記錄集進行 read-modify-write。
- **Then** 兩個 challenge TXT 值必須在最終 RRset 中共存(沒有覆寫丟掉任一節點的值),使得兩筆 order 的 CA 驗證皆能成功。若某個節點的 RMW 覆寫了另一個的 TXT,該 order 的驗證會間歇性失敗並耗用一次 CA 嘗試。不變式:無孤立/被丟棄的 TXT;兩個 challenge 皆可解析;無跨節點 RRset 覆寫。

### IT-RACE-05 INT-287 併發/競態
- **Given** 有數筆訂單卡在 VERIFYING/PROCESSING,其 updatedAt 在 6 小時內。一個 2 節點叢集(重新)啟動,因此兩個節點在開機時都會執行 AcmeOrderRecoveryRunner(一個沒有 @SchedulerLock 的 ApplicationRunner)。
- **When** 兩個節點都執行 run():各自進行 orderEntityRepo.findAll(),在記憶體中篩選出 VERIFYING/PROCESSING 的訂單,並對「相同的」訂單呼叫 poller.pollOrderAsync(orderId)。
- **Then** 每一筆進行中的訂單都必須恰好被恢復/完成一次;同一筆訂單的 order.execute()/CSR 提交不得在跨節點間觸發兩次。若兩個節點都輪詢且都呼叫 order.execute(),即為缺陷(重複完成的競爭,可能造成 CA 錯誤/憑證重複)。不變條件:全叢集對每筆訂單至多恢復一次(測試應斷言僅有單一效果,或將其標記為發佈阻斷項)。

### IT-RACE-06 INT-288 併發/競態
- **Given** 有一個 DeployJobEntity 處於 PENDING_APPROVAL,對應端點 E(例如本地或 webhook)上訂單 O 的部署,且兩位主管都能看見它。
- **When** 兩位主管同時 POST /api/v1/admin/deploy-approvals/{jobId}/approve。每一個都執行 SoD 檢查,接著 markQueuedIfPending(jobId)(CAS),再 runJobAsync。
- **Then** markQueuedIfPending 對恰好一位核准者回傳 1(作業 -> QUEUED -> RUNNING,對 E 進行一次安裝嘗試),對另一位回傳 0(HTTP 'job not pending')。端點恰好收到一次安裝/PATCH。不變條件:並行核准不會把同一份憑證重複部署到同一個端點。

### IT-RACE-07 INT-290 併發/競態
- **Given** 一位使用者持有一個有效的刷新權杖 R,且在 clm_refresh_token_registry 中註冊為 active。兩個分頁都遇到 401,並幾乎同時帶著「相同的」R 發出 POST /api/v1/auth/refresh。其流程非原子性:isActive(R) -> revokeOne(R,'ROTATED') -> register(newR)。
- **When** 兩個 /refresh 呼叫並行執行:兩者都將 R 讀為 active(revoked_at 仍為 null),兩者都繼續執行 revokeOne(冪等),並都註冊一個全新的刷新權杖家族。
- **Then** 競爭過後,至多應有「一個」新的刷新家族仍可使用;一個被出示兩次的刷新權杖應觸發重用偵測並撤銷該家族。若兩個新家族都為 active,即為權杖重複的缺陷。不變條件:單次使用刷新——每個權杖只輪替一次,並偵測到重用(測試斷言實際行為,並將兩家族的結果標記為發現項)。

### IT-RACE-08 INT-292 併發/競態
- **Given** 一筆 APPROVED、未過期、未消費的 clm_download_request dlr_X,對應一筆 ACME 簽發訂單的 PFX/網域金鑰,由申請者 R 所擁有(15 分鐘 TTL 窗口開啟中)。
- **When** R(或一個持有連結的腳本)發出兩個並行的 GET /api/v1/download-requests/dlr_X/download。每一個都先檢查未消費,接著在 markConsumedIfDownloadable(dlr_X) 上競爭。
- **Then** 恰好一個請求取得 affected=1 並收到金鑰素材;另一個取得 affected=0 -> HTTP 'download link expired or already used'。私鑰被組裝/解密並恰好遞交一次。不變條件:即時下載即使在並行抓取下也嚴格只能使用一次(同時驗證一個 produce() 失敗會正確地將 consumed 回滾為 null,且不會洩漏兩份副本)。

### IT-RACE-09 INT-280 併發/競態
- **Given** 節點 A 持有 'renewal-scheduler' ShedLock(lockAtMostFor=30m, lockAtLeastFor=0)且正在續約中;其 DNS-01 發佈 + 對一個大型/落後 zone 的 waitForPropagation 耗時超過 30 分鐘,使得在 A 仍在工作期間 lock_until 即已過期。
- **When** 在 lock_until 過去之後,節點 B 的續約 tick 觸發,重新取得這個現已過期的 'renewal-scheduler' 鎖,並在節點 A 仍在收尾的同時,對「相同」的 order 集合開始執行 doRenewal。
- **Then** 系統「不可」為同一個 CN 產生兩個 ACME order / 兩次 CSR finalize。要麼 lockAtMostFor 必須超過最壞情況的續約時間,要麼每 CN 去重(existsBy PENDING + hasInflightOrderForCn)必須在 B 提交之前攔截它。不變式:即使鎖提前過期,在緩慢傳播下也不會雙重簽發。

### IT-RACE-10 INT-283 併發/競態
- **Given** 一列 PENDING clm_cert_issuance_request cir_Y,supervisor S1 與 admin A 可見。
- **When** S1 在同一瞬間為 cir_Y POST /reject,而 A POST /approve。approve 使用 markApprovedIfPending(CAS);reject 讀取 PENDING 後執行 setStatus(REJECTED)+save(「非」CAS)。
- **Then** 該列必須一致地以 APPROVED 或 REJECTED 其中之一結束。若 approve 贏得 CAS,reject 的非 CAS save 必不可悄悄將 APPROVED 覆寫回 REJECTED(那將在 ACME order 已建立的情況下把請求標記為已拒絕 -> 孤立 order 搭配 REJECTED CIR)。不變式:不會在 REJECTED 請求下建立 order;單一終態決策。

### IT-RACE-11 INT-284 冪等性
- **Given** 一位使用者(role=user)在訂單建立頁面,其群組中有一個有效帳戶;CN form.example.com 無 PENDING CIR 且無進行中的 order。
- **When** SPA 在毫秒內觸發兩個完全相同的 POST /api/v1/{provider}/accounts/{accountId}/orders 請求(連點兩下／網路緩慢時重試),兩者皆帶 CN=form.example.com。
- **Then** 最多建立一筆 clm_cert_issuance_request PENDING;第二筆必須被 existsByCommonNameAndAccountIdAndStatus(PENDING) 以 DuplicateRequestException(HTTP 4xx)拒絕。核准後恰好簽發一張憑證。不變式:提交具冪等性——儘管 HTTP 呼叫重複,仍為一個邏輯請求。

### IT-RACE-12 INT-286 併發/競態
- **Given** 兩筆名稱重疊的 order 共用同一個 _acme-challenge fqdn/RRset。Order O1 達到 ISSUED 並進入 CLEANUP_PENDING(刪除其 TXT),而 Order O2 仍在 PUBLISHING/PUBLISHED 並需要其 TXT 存在。
- **When** O1 的清理 RMW(刪除其值)與 O2 對同一個 RRset 的發佈/驗證併發執行。
- **Then** O1 的清理必須只移除 O1 的值,而保留 O2 的 TXT 不動;O2 仍能驗證。兩者皆終止後,dnsPublishStatus 對兩者皆為 CLEANED,且該 zone 中「沒有」殘留的 challenge TXT。不變式:清理是以值為範圍而非整組擦除 RRset;無孤立 TXT,也不會提前刪除仍被需要的同層值。

### IT-RACE-13 INT-289 冪等性
- **Given** 訂單 O 為 ISSUED 且與端點 E 有綁定(auto_deploy 可能為 true)。enqueueForOrder 在 jobRepo.save 之前透過 existsByOrderIdAndEndpointIdAndStatusIn(PENDING_APPROVAL/QUEUED/RUNNING) 去重——這是一個先查後存,且 (order_id,endpoint_id) 上沒有資料庫 UNIQUE 約束。
- **When** 一個自動 ISSUED-hook 的 enqueue 與一個使用者 POST /api/v1/orders/{orderId}/deploy 並行觸發(或兩次 ISSUED 重播),兩者都在彼此存檔之前通過了 existsBy 檢查。
- **Then** (O,E) 至多應存在一個有效的 DeployJobEntity。若建立了兩個 PENDING_APPROVAL 作業,即為缺陷(兩個核准項目、潛在的重複部署)。不變條件:冪等的 enqueue——儘管有並行/重播的觸發,每組 (order,endpoint) 仍只有一個有效作業。

### IT-RACE-14 INT-291 併發/競態
- **Given** 使用者 U 在兩個裝置上登入,持有刷新權杖 R1 與 R2。U 發起 POST /api/v1/auth/change-password(它會呼叫 revokeAllForUser('PASSWORD_CHANGED')),同時裝置 2 帶著 R2 發出 POST /api/v1/auth/refresh(isActive -> revokeOne -> register newR2)。
- **When** change-password 的 revokeAllForUser 與裝置 2 的刷新交錯:該刷新可能在 revokeAllForUser 掃過資料表「之後」才註冊 newR2,使 newR2 留在 active 且未被撤銷。
- **Then** 兩者完成後,U 不得有任何刷新權杖仍為 active——change-password 必須勝出,強制所有裝置重新登入。若一個剛輪替出的 newR2 存活下來,舊工作階段就在密碼變更後仍然持續。不變條件:變更密碼會撤銷每一個工作階段,包括那些在變更過程中輪替出的。

### IT-RACE-15 INT-293 併發/競態
- **Given** 授權 FQDN 配額恰好有 N 個可用網域。兩位 admin/supervisor 並行 POST /api/v1/certs/import:(a) 兩者都匯入「相同的」PEM(相同的 CN+serial),以及 (b) 兩個不同的多 SAN PEM,各自都對一個過時的 managedDomains() 快照通過 wouldExceed(),但合計超出 N。
- **When** 兩個匯入並行執行;各自從一個在任一方提交前取得的快照計算 FQDN 使用量,且各自執行相同 CN 的 OrderSupersessionService。
- **Then** 情況 (a):重複 PEM 至多建立一筆 IMPORTED 訂單(或第二筆被乾淨拒絕),且取代不得將一份存活的 ISSUED 憑證兩次翻轉為 SUPERSEDED/錯誤地取消其部署作業。情況 (b):合併匯入「不得」超出授權的 FQDN 數量。不變條件:並行匯入下不超出配額;重複 PEM 匯入冪等;取代只套用一次。

### IT-RACE-16 INT-294 併發/競態
- **Given** clm_license 是一筆 id=1 的單例資料列。兩位 admin 同時以兩個「不同的」簽署權杖(例如一個較高階、一個較低階)POST /api/v1/admin/license/activate;activate() 執行 verify -> repo.save(id=1) -> this.cached = saved,且沒有並行防護。另外,一批待核的 PENDING 簽發請求正被逐一核准。
- **When** (a) 兩個啟用競相覆寫 id=1 資料列與記憶體中的 cached 欄位。(b) 同時,多個針對「不同」CN 的 /cert-issuance-requests/{id}/approve 各自重新檢查 license.canIssue()+quotaFqdn()(每次核准重新計算、非原子性)。
- **Then** (a) 每個節點上持久化的 clm_license 資料列與 cached 權益必須一致(沒有節點留下兩個權杖宣告撕裂混合的狀態)。(b) 一波並行核准合計不得超出 quotaFqdn。不變條件:單一一致的有效授權;並行核准不超出配額;一個在核准潮中途降階的啟用必須阻擋後續超配額的核准。

---

## 效能 / 負載

### IT-PERF-01 INT-295 效能/負載
- **Given** GET /api/v1/certs/dashboard 針對一張 5 萬列的 acme_orders 資料表觸發 11 個獨立的 scoped() 計數 Specification(all/ISSUED/FAILED/REVOKED/IMPORTED/CANCELLED/SUPERSEDED/PENDING_DNS/VERIFYING/PROCESSING + 即將到期)
- **When** 一位 admin 載入儀表板 100 次(閉迴路、1 個並行使用者),接著 20 位並行 admin 各載入一次
- **Then** GET /certs/dashboard 的單一使用者端對端 p95 延遲 <= 800ms;在 20 位並行 admin 下 p95 <= 2.5s 且零 5xx;透過 SQL Profiler 驗證該端點發出 <= 11 個 COUNT 查詢,且每個都使用 status/group_id 索引(無資料表掃描、無資料列具體化)

### IT-PERF-02 INT-296 效能/負載
- **Given** GET /api/v1/certs/domains 呼叫 orderRepository.findAll(Sort) 載入「所有」資料列,接著在 JVM 內以 LinkedHashMap 依 CN 分組(page/size 參數僅在完整具體化「之後」才套用),在 DB 層忽略分頁
- **When** 5 位並行 admin 各自重複請求 /certs/domains?page=0&size=100 持續 2 分鐘,同時取樣堆積
- **Then** p95 延遲 <= 1.5s 且無 OutOfMemoryError;配置剖析器顯示每次呼叫都完整具體化全部 5 萬筆 OrderEntity 一次;若堆積壓力迫使 GC 暫停 > 500ms,或將資料加倍至 10 萬使延遲/堆積線性加倍(確認 O(n) 的非分頁載入),則判定情境 FAIL(並開立缺陷)。記錄每 1 萬筆訂單所需的堆積餘裕。

### IT-PERF-03 INT-298 效能/負載
- **Given** GET /api/v1/certs/export.xlsx 呼叫 orderRepository.findAll(spec, Sort),沒有資料列上限或分頁,並完全在記憶體中建構一個 XSSFWorkbook(不像稽核匯出有 50000 的上限)
- **When** 1 位 admin 在 5 萬列時觸發匯出;接著 3 位 admin 並行觸發
- **Then** 單次匯出在 30 秒內完成,且 HTTP 回應未被截斷(有效的 .xlsx、5 萬列全數存在);峰值堆積增量受限並記錄下來;3 個並行匯出「不」造成 OutOfMemoryError 或請求逾時。若並行匯出 OOM 或超過 60 秒,開立 P0 缺陷,建議加上硬性資料列上限 + SXSSF 串流式 workbook(如同稽核匯出已有的上限)。

### IT-PERF-04 INT-299 效能/負載
- **Given** GET /api/v1/audit/verify-integrity 呼叫 auditRepository.findAll(Sort) 將「所有」資料列載入一個 List,並在單一迴圈中為每一列重新計算 chainService.sign(e),沒有分頁或檢查點
- **When** 一位 admin 對 200 萬列的資料表呼叫一次 verify-integrity,同時測量堆積與牆鐘時間;接著 2 位 admin 並行呼叫
- **Then** 記錄實際的牆鐘時間與峰值堆積。斷言它要麼在 60 秒內完成且不 OOM,要麼團隊接受此限制並將其加入操作手冊。若單次呼叫 OOM 或超過 120 秒的閘道逾時,則 FAIL/標記為 P0,並確認 2 個並行呼叫不會使堆積加倍而 OOM。建議採用增量/檢查點式的驗證。

### IT-PERF-05 INT-301 效能/負載
- **Given** AutoRenewScheduler.findCurrentIssued/hasInFlight 每個 tick 都透過 orderRepo.findAll() 在記憶體中掃描,續期則排入 CIR;一旦核准,AcmeOrderService.createOrder 在共用的 acmeTaskExecutor(core=2/max=10/queue=50、AbortPolicy)上觸發非同步輪詢,而每個輪詢可佔用一個執行緒長達 180 秒
- **When** 觸發 autorenew 掃描(或以 admin 身分 POST /api/v1/certs/renew-now)使全部 500 張都符合資格,接著在一個緊湊迴圈中批次核准它們的 CIR 以模擬續期風暴
- **Then** 測量:在一張 5 萬筆訂單的表上、500 個符合資格 CN 的掃描 tick 牆鐘時間(斷言 < 30s,使其不超過 ShedLock lockAtMostFor=30m 且絕不衝破 30m 的鎖);批次核准後,斷言「沒有」任務被 AbortPolicy 拒絕(queue>50)以致訂單靜默卡在 VERIFYING/PROCESSING 且無 FAILED 狀態;全部 500 張在你記錄的 SLA 內達到終端狀態(經由模擬而 ISSUED)。若任何訂單被靜默丟棄(排入的任務被拒)則標記為 P0——那就是靜默的未簽發。

### IT-PERF-06 INT-305 效能/負載
- **Given** 每個 API 呼叫都解析 RBAC(即時群組查找),而數個熱端點(儀表板 11 個計數、domains 彙總 findAll、匯出)各自在其整個執行期間佔用一個 DB 連線,對上一個有限的 Hikari 連線池
- **When** 在 5 分鐘內從 10 個並行使用者爬升到 200 個並維持 200 達 15 分鐘,其中 1% 的請求為沉重的匯出/domains 呼叫
- **Then** 整體 API p95 <= 1500ms、p99 <= 3000ms、錯誤率 < 0.5%;Hikari 的活躍連線度量維持在上限以下(無 connectionTimeout 例外/連線池耗盡);無執行緒池飢餓。產出一份連線池尺寸建議(每個目標並行使用者所需的連線數)。若沉重端點佔用連線造成連線池耗盡並對輕量端點連鎖出 5xx,則 FAIL/標記為 P0。

### IT-PERF-07 INT-307 效能/負載
- **Given** DnsAutomationService(@Async acmeTaskExecutor)、DeploymentService.runJobAsync(@Async acmeTaskExecutor)、AsyncEmailSender(@Async acmeTaskExecutor)與 AcmeOrderPoller(@Async acmeTaskExecutor)全都共用「同一個」max=10 執行緒、queue=50、帶 AbortPolicy 的執行緒池
- **When** 在一個 10 秒的窗口內觸發全部四種工作負載派發,使合計任務數超過 10 個執行中 + 50 個排隊
- **Then** 斷言「沒有」任務被 AbortPolicy 以一種會讓訂單卡在 VERIFYING/PROCESSING、無 FAILED 狀態且無重試的方式靜默拒絕;測量一個緩慢的 SMTP 送出會使 ACME 輪詢與部署執行飢餓多久(記錄因電子郵件爭用而為簽發/部署增加的延遲)。若電子郵件或 DNS 作業明顯使憑證輪詢飢餓,或任何任務拒絕產生一個靜默丟棄的部署/輪詢,則 FAIL/標記為 P0。建議為每種工作負載分設執行緒池。

### IT-PERF-08 INT-297 效能/負載
- **Given** GET /api/v1/certs 使用帶 PageRequest 的 JPA Specification(size 上限 200)——分頁正確,但 commonNameLike 對應到一個無法使用索引的前置萬用字元 LIKE,而 expiringBefore 則對 cert_not_after 過濾
- **When** 25 位並行已登入使用者以合計 10 req/s 持續維持這組查詢混合 10 分鐘
- **Then** 整體 p95 <= 1200ms、p99 <= 2500ms、錯誤率 0%、吞吐量維持穩定(10 分鐘內無衰退);特別隔離 commonNameLike 子字串查詢並斷言其 p95 <= 1800ms——若前置萬用字元 LIKE 驅動的全表掃描主導了延遲,則標記為缺陷

### IT-PERF-09 INT-300 效能/負載
- **Given** GET /api/v1/audit 已分頁(size<=500、依 timestamp DESC),但 subject 過濾為 LIKE,且 export.xlsx 將至多 50,000 列未分頁地灌入 POI
- **When** 10 位並行 admin 以 5 req/s 執行這組帶過濾的稽核查詢混合 10 分鐘,接著一位 admin 執行觸及 5 萬上限的 export.xlsx
- **Then** 帶過濾的分頁查詢 p95 <= 1500ms(斷言每個過濾在有索引時都使用索引;subject-LIKE p95 <= 2500ms);5 萬列匯出在 45 秒內完成、不 OOM 且產出有效檔案。若一個沒有 action/subject 過濾的日期範圍掃描使 p95 衰退超出預算,則標記。

### IT-PERF-10 INT-302 效能/負載
- **Given** FqdnUsageService.wouldExceed() 在每次訂單提交/核准時都從一次 DB 掃描重新計算完整的網域 HashSet,且其先查後插並未以交易方式防護並行提交
- **When** 發出 50 個並行 POST /api/v1/{provider}/accounts/{accountId}/orders(各為一個新的不重複 FQDN),使多個對同一個過時快照通過 wouldExceed(),接著並行核准它們
- **Then** 兩項斷言:(1) 效能——測量在約 2000 個網域、50 個並行呼叫下,重複的 findCnAndSans()+HashSet 建構為每次提交增加的延遲(記錄 p95;若重算主導則標記);(2) 負載下的正確性——最終不重複 FQDN 數量「不得」超出 2000 的配額。若它超出了交錯並行請求的數量,開立缺陷:先查後做需要一個鎖/交易式防護。

### IT-PERF-11 INT-303 效能/負載
- **Given** POST /api/v1/certs/import/batch/preview 與 /commit 為了驗證而將完整的 order/account/provider 資料表載入記憶體,並在 commit 時重新讀取檔案(相對於 preview 形成 TOCTOU)
- **When** 在一張 5 萬筆訂單的背景下,將 500 列檔案上傳至 /preview 再 /commit,測量每個階段;上傳 1000 列檔案以確認上限會拒絕它
- **Then** 500 列的預覽在 20 秒內完成、commit 在 30 秒內完成且在 5 萬背景下不 OOM;記錄 findAll 驅動的驗證成本並在其主導時標記(建議改用索引化查找);1000 列檔案被 MAX_ROWS 乾淨拒絕且無部分作業;即使一個並行匯入在 preview 與 commit 之間改變了配額,commit 仍恰好寫入所選的有效列(無重複插入)。

### IT-PERF-12 INT-304 效能/負載
- **Given** 排程的探查掃描在所有端點上是同步且序列的,處於單一個 2 小時的全叢集 ShedLock 之下,而 listDiscoveries 是 findAll() + JVM 內過濾且無分頁
- **When** 對全部 200 個端點(包含卡住的那些)觸發排程掃描,測量總牆鐘時間與每個端點的時序,接著呼叫 GET /api/v1/admin/scan/discoveries,此時有數千筆結果資料列
- **Then** 序列掃描的總牆鐘時間「必須」遠低於 2 小時鎖的上限(斷言 200 個端點 < 60 分鐘,且沒有單一個卡住的端點會超過其驅動程式逾時而阻擋其餘端點);確認一個卡住的端點無法耗盡 2 小時鎖而讓第二個節點開始並行掃描;儘管有 JVM 內 findAll+過濾,GET /scan/discoveries 在超過 5000 筆探查列時 p95 <= 2s。若序列掃描時間逼近鎖窗口,或探查清單衰退,則標記。

### IT-PERF-13 INT-306 效能/負載
- **Given** POST /api/v1/admin/revoke-requests/break-glass/by-cn 掃描整張訂單表以找出該 CN 的憑證,接著序列地在 CA 上逐一撤銷,且無交易邊界
- **When** 一位 admin 為該 300 張憑證的 CN 呼叫 break-glass-by-CN;測量總時間並觀察 CA 在迴圈中途(第 #150 筆訂單)失敗時的行為
- **Then** 記錄 300 次序列 CA 撤銷(各約 1 秒)的總牆鐘時間(斷言它不超過 HTTP 閘道逾時——若超過則標記並建議非同步/批次化處理);定位單一 CN 憑證的全表 findAll 被標記為 O(n)(應使用一個索引化的依 CN 查詢);在第 #150 筆注入的 CA 失敗上,斷言回應/稽核準確報告一個「部分」撤銷(部分 REVOKED、部分仍 ISSUED),且不報告一個誤導性的全數成功計數。

### IT-PERF-14 INT-308 效能/負載
- **Given** 多個以 findAll() 為基礎的排程器掃描(RenewalService、AutoRenewScheduler、CertExpiryNotifier、OrderSupersessionService)以固定間隔對一張 5 萬筆訂單的表執行,同時並行的使用者瀏覽持續進行,全都在同一個 JVM/執行緒池/DB 上
- **When** 持續執行混合工作負載 60 分鐘
- **Then** API p95 從第 5 分鐘到第 60 分鐘的向上漂移不得超過 20%(無漸進式衰退);完整 GC 後的堆積回到一個穩定的基準(無單調成長以致暗示洩漏——例如無上限成長的 refresh_token_registry,或每個 tick 的配置未被回收);零連線池耗盡;排程器 tick 在其間隔內完成且絕不重疊其 ShedLock 窗口。對任何持續的向上延遲趨勢或無法回復的堆積予以標記。

---

## 韌性 / 故障轉移進階

### IT-FAIL-01 INT-309 韌性/故障轉移
- **Given** 一筆已核准的訂單正在進行中,狀態為 VERIFYING(或 PROCESSING),其 acme_orders.updated_at 距今未滿 6 小時,對應一個可控的 ACME 端點(Pebble/LE-staging),該端點最終會回傳 VALID 的授權。
- **When** 在 @Async 的 pollOrderAsync 任務執行中時對 JVM 發出 SIGKILL(使記憶體中的輪詢器執行緒遺失),然後重啟應用程式並讓 AcmeOrderRecoveryRunner.run() 執行。
- **Then** 啟動時恢復執行器找到該 VERIFYING/PROCESSING 列(updatedAt 在 staleAfterHours=6 之內),記錄 '[recovery] resuming poll for order ...',重新呼叫 poller.pollOrderAsync(orderId),訂單推進至 ISSUED(或誠實地進入 FAILED)。該訂單絕不可在沒有任何執行緒輪詢的情況下持續被固定在 VERIFYING/PROCESSING。

### IT-FAIL-02 INT-311 韌性/故障轉移
- **Given** 一筆已核准的部署作業狀態為 RUNNING(executeJob 對某個 local/ssh 端點安裝到一半),且另一筆訂單的第二個作業狀態為 QUEUED。
- **When** 在安裝中途對 JVM 發出 SIGKILL,然後重啟使 DeployJobRecoveryRunner.run() 執行。
- **Then** RUNNING 作業被無條件轉換為 FAILED,log_text 附加 '[recovery] JVM 重啟前卡在 RUNNING — 標記為 FAILED (stale)...',並設定 ended_at;QUEUED 作業透過 deploymentService.runJobAsync 重新派發。沒有任何安裝到一半的作業會永遠停在 RUNNING,也不會在未經驗證安裝的情況下翻轉為 SUCCESS。

### IT-FAIL-03 INT-312 併發/競態
- **Given** 一個共用單一 MSSQL 的雙節點叢集。A 節點正主動執行一筆 RUNNING 部署作業,並有一筆 QUEUED 作業待處理。
- **When** 在 A 節點的作業仍為 RUNNING 時重啟 B 節點(或啟動第二個節點);B 節點的 DeployJobRecoveryRunner.run() 在啟動時掃描叢集範圍內所有 RUNNING/QUEUED 作業。
- **Then** 記錄實際行為並斷言資料完整性邊界:B 節點將 A 節點仍存活的 RUNNING 作業標記為 FAILED 並重新派發 QUEUED 作業,對同一端點產生一次重複/競爭的安裝(已知的 HA 危害)。測試必須證明是否有一筆重複部署作業與 A 節點進行中的安裝產生競爭,以及該端點最終是否處於一致的憑證狀態。預期門檻:單一活躍部署節點是唯一安全的拓撲——斷言不會出現結果互相衝突的無聲雙重安裝。

### IT-FAIL-04 INT-313 韌性/故障轉移
- **Given** 一筆具備 AUTO_PLUGIN/手動 DNS 綁定的訂單,其 _acme-challenge TXT 被刻意永不發布(或發布到解析器看不到的區域),使 DnsPrechecker 永遠不會回傳 allMatch。
- **When** 驅動自動 DNS 流程;waitForPropagation 迴圈 30 次 x 5s(2.5 分鐘)而不收斂並回傳 false,之後程式碼仍會呼叫 triggerCaValidation。
- **Then** 訂單絕不可無限期掛起:約 2.5 分鐘後觸發 CA,驗證失敗(TXT 不存在),訂單被驅動至 FAILED 並帶有有意義的錯誤(而非永遠停在 VERIFYING/PUBLISHING)。dns_publish_status 反映該失敗;輪詢器最終在輪詢逾時/INVALID 時 markFailed。抵達終態的總實際耗時是有界的。

### IT-FAIL-05 INT-321 韌性/故障轉移
- **Given** 一張具備已核准撤銷請求的 ISSUED 憑證,以及一個會使 c.revoke() 拋出/逾時(CA 端撤銷失敗)的 ACME 代理。
- **When** 核准該撤銷請求使 AcmeOrderService.revokeCertificate 在 CA 呼叫失敗時執行;接著在第二次執行中讓 CA 成功。
- **Then** CA 失敗時:狀態必須維持 ISSUED(絕不翻轉為 REVOKED),寫入一筆 FAILURE 稽核 ORDER_REVOKED,且維運人員看到明確的撤銷失敗(沒有假成功)。CA 成功時:狀態轉換為 REVOKED 並寫入一筆 SUCCESS 稽核。這證明 FD-05 誠實契約:只有在 CA 真正確認時才設定本地 REVOKED。

### IT-FAIL-06 INT-310 韌性/故障轉移
- **Given** 一筆狀態為 VERIFYING 的訂單,其 acme_orders.updated_at 早於 acme.recovery.stale-after-hours(6h)——亦即其 CA 端的授權/訂單 URL 很可能已經過期。
- **When** 重啟應用程式並讓 AcmeOrderRecoveryRunner.run() 依 6h 截止標準評估該訂單。
- **Then** 執行器不會呼叫 pollOrderAsync(那樣會撞進一筆 INVALID 的 CA 訂單);反之它將訂單轉換為 FAILED,error_message 為 'stale in-flight order (no update for >6h), marked FAILED on startup recovery. Please retry.'。狀態是安全且可重試的,絕不無聲存活。

### IT-FAIL-07 INT-314 韌性/故障轉移
- **Given** 一個由故障注入代理(例如 Toxiproxy)在前的 ACME directory,在一筆已核准訂單的授權擷取/訂單最終化期間回傳 HTTP 500/503 或重設連線。
- **When** 核准一筆 CIR 使 AcmeOrderService.createOrder 執行;代理在 auth.fetch(fetchWithRetry 以固定 2s 睡眠剛好重試兩次)與輪詢時注入 5xx/重設。
- **Then** fetchWithRetry 嘗試兩次後重新拋出;輪詢器的 markFailed 將訂單轉換為 FAILED,並將 CA 錯誤捕捉到 error_message。acme_orders 列維持一致(加密的 domain_key_pem 完整、cert_pem 為 null、沒有部分憑證),訂單可重試,且一則 cert.failed 通知被排入佇列。沒有任何執行緒空轉,也沒有半寫入的憑證狀態殘留。

### IT-FAIL-08 INT-315 韌性/故障轉移
- **Given** 一個在訂單建立或最終化時回傳帶 Retry-After 標頭的 HTTP 429(模擬 LE 重複憑證/驗證失敗/每網域憑證數限制)的 ACME 代理。
- **When** 核准一筆訂單(並另外以 ADMIN 身分觸發 renew-now)使 CA 回傳 429;觀察 createOrder、輪詢器與續約重新排入佇列如何反應。
- **Then** 429 被轉譯為一筆 FAILED 訂單,並將速率限制原因記入 error_message——而不是無限的重試風暴。斷言系統不會立即在緊密迴圈中重新發送請求而加劇封禁(記錄實際行為:沒有遵守 Retry-After,因此 QA 必須驗證該失敗是終態,且排程器/續約重新排入佇列不會在 Retry-After 視窗內產生大量新的 CA 呼叫)。

### IT-FAIL-09 INT-316 韌性/故障轉移
- **Given** 一筆已核准、QUEUED 的部署作業,目標是一個 SSH 或 F5/k8s 端點,該端點在安裝進行到一半時變得無法連線或回傳驗證失敗,且已部署一張先前的憑證(SSH 情況下 before_cert_pem + before_key_pem 都存在)。
- **When** 核准該作業(PENDING_APPROVAL->QUEUED,透過 CAS)並讓 executeJob 執行,而端點被網路分割或憑證在安裝中途被拒。
- **Then** 作業轉換為 FAILED;若 before_cert_pem 與 before_key_pem 兩者皆存在且回滾成功,則轉換為 ROLLED_BACK(連同其先前金鑰一起還原先前那張憑證)。端點絕不會被留下提供半安裝/不受信任的憑證;即時 TLS 驗證以故障即關閉(fail-closed)使作業失敗,而非標記為 SUCCESS。last_deploy_status 反映失敗。對於 F5/webhook(不回傳先前金鑰),斷言它以 FAILED 結束且不回滾,而非錯誤的 SUCCESS。

### IT-FAIL-10 INT-317 效能/負載
- **Given** 超過 60 筆進行中的訂單(最多 10 條執行緒 + 50 個佇列)需要同時輪詢——例如在啟動時恢復一大批機群,或一波近乎同時的核准,每次輪詢最久持有一條執行緒達 60x3s=180s。
- **When** 在有 >60 筆 VERIFYING 訂單時重啟(或在很短的視窗內核准 >60 筆 CIR),使提交數超過 maxPool+queue,讓 ThreadPoolTaskExecutor 的 AbortPolicy 拒絕溢位任務。
- **Then** 刻畫並界定該故障:被拒絕的輪詢任務絕不可讓訂單在沒有執行緒、也沒有 FAILED 狀態的情況下無聲卡在 VERIFYING/PROCESSING,一直到下次重啟。斷言任務要嘛被排入佇列/重試,要嘛受影響的訂單被呈現為 FAILED/可觀察地降級——絕不是一個無聲黑洞。記錄被拒絕的任務數對比留在非終態的訂單數。

### IT-FAIL-11 INT-318 韌性/故障轉移
- **Given** 應用程式正在處理訂單狀態轉換與稽核寫入時,MSSQL 變得無法連線(代理斷開連線)然後又恢復。
- **When** 在訂單儲存/核准/稽核附加期間將 DB 連線分割(在 JDBC 埠上用 Toxiproxy)約 30-60 秒,然後恢復它並繼續發送請求。
- **Then** 中斷期間的操作以呈現的錯誤失敗(HTTP 5xx)而非提交部分狀態;重新連線後連線池恢復且新請求無需重啟即可成功。沒有訂單被留在不一致狀態(例如狀態已推進但 cert_pem 缺漏),且 HMAC 稽核鏈透過 GET /api/v1/audit/verify-integrity 仍可驗證(沒有因中斷而分叉/斷裂的鏈)。斷言被吞掉的稽核寫入失敗不會無聲地讓某個變更動作未被記錄而留下 verify-integrity 缺口。

### IT-FAIL-12 INT-319 通知
- **Given** 當一則到期或 cert.failed 通知被排入佇列時,SMTP 無法連線(沒有郵件伺服器/代理拒絕 25/587)。
- **When** 在 SMTP 中斷時觸發一則通知(例如一筆失敗訂單 -> cert.failed,或一張憑證跨越某個到期門檻),觀察 clm_email_log 狀態,然後啟動 SMTP 並讓重試清掃器執行。
- **Then** 該郵件列被持久化為 QUEUED,attempt_count 依文件記載的退避(1/5/15/60/240 分鐘)遞增,並在 SMTP 恢復後由 ShedLock 守護的清掃器重新派發,翻轉為 SENT——不遺失。關鍵資料完整性檢查:驗證 acme_orders.expiry_notified_thresholds 是否在送達確認之前就被標記(那個有文件記載的有損去重 bug)——若門檻在送達失敗時就被燒掉,且在 5 次嘗試的死信之後再也不重新告警,則標記出來。確認當清掃器與最初的非同步派發重疊時不會發生重複寄送。

### IT-FAIL-13 INT-320 授權強制
- **Given** 一個 VALID 授權以及一個已建立、持久化於 clm_settings 中 _license.clock.hwm 之下(HMAC 簽章)的時鐘高水位標記(HWM)。
- **When** (a)將系統時鐘往回撥到低於 HWM 且超過 acme.license.clock-skew-seconds(預設 120s),然後嘗試提交/核准一筆訂單;以及(b)另外將時鐘往回撥但在 120s 容差內,以確認沒有偽陽性。
- **Then** (a)isClockRolledBack() 回傳 true -> LicenseService.canIssue() 回傳 false -> GET /api/v1/license/status 回報 CLOCK_TAMPERED 且 blocksNewOrders=true;訂單提交/核准被拒(沒有 CA 呼叫),直到 NTP 校正超過 HWM。(b)一次低於偏移的往回撥不會觸發守衛,簽發仍可運作。同時驗證:DBA 將 clm_settings 中的 HWM epoch 往下編輯會被拒(HMAC 不符 -> 退回以當下重建),使竄改無法調低水位標記。

### IT-FAIL-14 INT-324 併發/競態
- **Given** 一個共用單一 MSSQL 的雙節點叢集,兩節點都執行受 @SchedulerLock 守護的作業(autorenew-scan、renewal-scheduler、expired-state-cleanup、cert-expiry-notifier)且兩者同時併發寫入稽核列。
- **When** 迫使兩節點的排程器 tick 在同一視窗觸發(對齊 cron/間隔),並同時在兩節點上驅動會附加到 acme_audit_log 的變更動作。
- **Then** 恰好一個節點取得每一個 ShedLock 並執行掃描/續約/清理(沒有重複的續約 CIR,沒有重複的到期郵件)——透過鎖列以及同一 CN 沒有重複 PENDING CIR 來驗證。另外,斷言有文件記載的稽核鏈分叉危害:兩節點的併發稽核寫入(chainLock 是 JVM 本地、非 ShedLock 協調)可能產生兩列具有相同 previous_hash,因此 GET /api/v1/audit/verify-integrity 可能在一條未被竄改的鏈上回報 previousHash 不符。測試必須區分真正的竄改與這個 HA 偽陽性,並確認鏈完整性需要單一寫入者。

### IT-FAIL-15 INT-322 韌性/故障轉移
- **Given** 一張憑證,其 CRL Distribution Point(或回應器主機)無法連線/逾時/回傳垃圾資料。
- **When** 在 CDP 被黑洞處理(Toxiproxy 逾時)時呼叫撤銷狀態檢查路徑,另外在回傳 5xx/垃圾內容時也呼叫一次。
- **Then** 只要 CDP 缺漏或所有 CRL 下載都失敗,RevocationStatusService 即回傳 Status.UNKNOWN——它絕不可發出假的 GOOD(遮蔽真正的撤銷)或假的 REVOKED。連線/讀取逾時(10s/15s)界定該呼叫,使一個掛住的回應器不會無限期阻塞呼叫端執行緒。

### IT-FAIL-16 INT-323 韌性/故障轉移
- **Given** 一個 hsm.enabled=true 的 Enterprise 授權,但 PKCS#11 函式庫路徑錯誤/HSM 裝置離線(SoftHSM token 被移除或 .so 路徑無效)。
- **When** 在 HSM 無法連線時以 ADMIN 身分呼叫 GET /api/v1/admin/hsm/status,並同時執行一項涉及金鑰的操作(例如建立訂單時產生/加密一把網域金鑰,或透過核准流程匯出帳號金鑰)。
- **Then** 狀態端點回傳 available=false 並附帶診斷訊息,而非拋出未處理的 500 / 使應用程式崩潰,且不會留下一個過時/重複的 SunPKCS11 provider 註冊。由於 CredentialCipher 維持 @Primary(軟體 AES),帳號/網域金鑰的加解密持續運作——HSM 中斷絕不可使既有加密金鑰無法解密或阻擋簽發。斷言沒有 NPE/啟動失敗,且金鑰素材可往返。

---

## DR / 資料遷移

### IT-DR-01 INT-325 資料遷移
- **Given** 一個沒有 flyway_schema_history、且啟用 prod(非 dev)Spring profile 的乾淨 MSSQL Server 實例
- **When** 應用程式啟動:FlywayConfig.repairAndMigrate 執行 repair() 然後 migrate() 套用 V1..V96(含 Java migration V3/V17/V61/V63/V64),接著 KeyEncryptionMigrationRunner、OrderFingerprintBackfillRunner 與 AuditChainBackfillRunner 執行
- **Then** 所有 migration 在 flyway_schema_history 中以 success=1 回報成功,每一個預期的表都存在(clm_users、acme_orders、acme_accounts、acme_audit_log、clm_license、clm_cert_issuance_request、clm_revoke_request、clm_download_request、clm_deploy_binding_request、shedlock、clm_refresh_token_registry、clm_api_keys),Hibernate ddl-auto=validate 通過(沒有 schema 漂移),tenant_id 欄位已消失(V3),acme_orders 沒有 CHECK 約束且具備 cert_fingerprint_sha256(V77),稽核欄位為 before_data/after_data/actor_user_id(V61),且應用程式抵達健康的 /actuator/health 狀態

### IT-DR-02 INT-326 資料遷移
- **Given** 一個被採用為 Flyway baseline 的舊版已填入資料的 RXCLM 資料庫,其稽核列缺少 HMAC 簽章且訂單缺少 cert_fingerprint_sha256
- **When** 新的建置啟動並向前滾動 V61->V96 加上啟動回填執行器
- **Then** orders/accounts/audit 的列數不變(無刪除),V61 將 before_json->before_data 改名並保留 JSON 內容,AuditChainBackfillRunner 為每一筆舊有稽核列簽章(signed 計數 > 0,未竄改資料的 repaired 計數 = 0)且 GET /api/v1/audit/verify-integrity 回傳 intact=true,OrderFingerprintBackfillRunner 為每一筆具有 cert_pem 的列填入 cert_fingerprint_sha256,且所有先前 ISSUED 的憑證仍可下載

### IT-DR-03 INT-327 資料遷移
- **Given** acme_accounts 與 acme_orders 的列,其金鑰欄位是明文 PEM、enc_v1: 舊版密文與 enc_v2: 現行密文的混合
- **When** 應用程式啟動且 KeyEncryptionMigrationRunner.migrate() 掃描兩個欄位
- **Then** 啟動後每一筆 account_key_pem 與 domain_key_pem 都以 enc_v2: 前綴開頭(cipher.isStrongFormat()==true),原始明文可透過 decrypt 還原(往返相等),已是 v2 的列維持不動,且第二次啟動是一個有記錄的 no-op(冪等自我修復);接著應用程式提供帳號 key.pem 匯出與憑證下載,證明升級後的金鑰仍可解密

### IT-DR-04 INT-328 韌性/故障轉移
- **Given** 在加密密鑰 A 下封存的既有加密金鑰欄位,以及一個現在被設定為密鑰 B 的節點
- **When** 應用程式啟動且 KeyEncryptionMigrationRunner 嘗試解密 + 重新封裝每一列
- **Then** 執行器以 WARN 記錄並跳過每一筆無法解密的列(它不會覆寫或清空密文),這些列維持其原始 enc_v1/enc_v2 形式(沒有破壞性的就地損毀),對受影響帳號嘗試匯出帳號 key.pem 時會以一個解密錯誤大聲失敗而非回傳垃圾,且操作人員擁有一個有文件記載、可逆的狀態(還原密鑰 A)——以此確認金鑰輪替是破壞性但可偵測的,而非無聲的資料遺失

### IT-DR-05 INT-329 冪等性
- **Given** 一個已對持久化 MSSQL DB 完成所有 migration 與回填的應用程式
- **When** 應用程式被停止並重啟三次,每次啟動都執行 repair() 然後 migrate() 然後三個啟動執行器
- **Then** 沒有任何 migration 被重新執行(flyway_schema_history 列數與 installed_rank 維持不變),repair() 不會改動已成功列的 checksum,KeyEncryptionMigrationRunner/AuditChainBackfillRunner/OrderFingerprintBackfillRunner 全都記錄 no-op(0 變更),verify-integrity 維持 intact=true,且任何一次啟動都不會發生 DDL 錯誤(例如重複欄位/重複索引)

### IT-DR-06 INT-331 災難復原/備份
- **Given** 一套運作中的 RXCLM,其中的有效 ACME 帳號,其私鑰是唯一無法重建的資產
- **When** 管理員 GET /api/v1/admin/dr/account-keys?passphrase=P(加密封套),接著清空 accounts 資料表,然後管理員以 dryRun=false + 該封套 + passphrase P 對 /api/v1/admin/dr/account-keys/restore 發出 POST
- **Then** 還原會回報正確的 wouldCreate 集合,帳號會重新出現且 account_key_pem 完全相同(由 JPA 轉換器以目標節點金鑰重新加密),擁有者/指派者欄位被保留(createdBy/assignee 的 containsKey 防護),DR_RESTORE_ACCOUNT_KEYS 有被稽核,且後續一筆呼叫 AcmeOrderService.createOrder 的 POST 訂單核准,能成功在 CA 端綁定到還原後的帳號(證明該金鑰仍能控制該 ACME 帳號)

### IT-DR-07 INT-330 負向/邊界
- **Given** 一個 DB,其中一個非冪等的 migration(沒有 IF NOT EXISTS 的裸 ADD COLUMN,或一個 V90/V92 對 clm_settings 的裸 INSERT)被留在半套用狀態
- **When** 應用程式啟動;FlywayConfig.repair() 清除失敗的列且 migrate() 從頭重新執行該 migration
- **Then** 重新執行以一個重複欄位/重複鍵錯誤失敗,且應用程式無法啟動(fail-fast),且測試精確記錄哪些 migration 是非冪等的(V90/V92 裸 INSERT 造成 clm_settings PK 違規、V77 裸 ADD COLUMN),以便能夠強化它們使用 NOT EXISTS / IF NOT EXISTS 守衛

### IT-DR-08 INT-332 災難復原/備份
- **Given** 一位管理員正從加密的 DR 封套進行還原
- **When** 在以下情況呼叫還原:(a) 帶封套但沒有 passphrase,(b) 帶錯誤的 passphrase,(c) 帶有效封套但沒有 dryRun 欄位,以及 (d) 帶 dryRun=false 但封套密文遭竄改
- **Then** (a) 回傳 400 'envelope encrypted but passphrase missing' 且不寫入任何東西;(b) 回傳 400 'envelope decrypt failed'(GCM 驗證失敗)且不寫入任何東西;(c) 預設為 dryRun=true,只回傳 wouldCreate/wouldUpdate/conflicts 差異,沒有任何資料庫變動也沒有 DR_RESTORE 稽核;(d) GCM 驗證失敗,所有線上金鑰維持不變 — 證明線上金鑰無法被損毀或未經驗證的酬載清除

### IT-DR-09 INT-333 災難復原/備份
- **Given** 一份透過 API 從 node A 匯出的 DR 備份(JSON/封套內含明文 PEM),以及另外在資料庫層級複製的原始 enc_v2: 密文
- **When** 將 API 備份以 dryRun=false 還原到 node B,相對於直接把 node A 的 enc_v2: account_key_pem 資料列複製到 node B 的資料庫
- **Then** API 路徑的還原成功 — node B 以金鑰 B 重新加密明文 PEM 並能解密/匯出它 — 而原始密文複製在 node B 上無法解密(decrypt failed),展示了文件所記載的風險:這兩條還原路徑不可混用;測試斷言 API 路徑是唯一安全的跨節點 DR 途徑

### IT-DR-10 INT-334 資料遷移
- **Given** 完整的 Flyway i18n 種子資料,內含繁體中文(zh-TW)NVARCHAR 值,以及在 V53/V54 中整理過的中日韓標點符號
- **When** 應用程式啟動、套用所有 i18n 遷移,且 SPA 抓取 /api/v1/i18n/messages?locale=zh-TW 與 ?locale=en
- **Then** 每一個 zh-TW 值回傳時與其遷移字面值逐位元相同(正確的中日韓字元,沒有來自非 NVARCHAR 或 Latin1 定序路徑的 '?'/亂碼),en 與 zh-TW 回傳成對的鍵集合且無缺漏鍵,clm_i18n_messages 主鍵 (locale, msg_key) 在大小寫/腔調不敏感的定序下沒有重複碰撞,且 zh-TW 依 ui.default.locale 維持為預設值

### IT-DR-11 INT-335 資料遷移
- **Given** 一個已填入資料的資料庫,含有 V14 設計要彌補的缺口(根據遷移標頭,V2 無法彌補)
- **When** V14 執行其 4 步回填(accounts->預設群組、orders->帳號/預設群組、assignee=created_by 然後輪詢然後第一個 admin 後備、assignee_email 回填)
- **Then** 每一個 acme_accounts.group_id 皆非 null,每一個 acme_orders.group_id 都符合其帳號的 group_id(或預設值),每一筆訂單都有非 null 的 assignee_user_id 與相符的 assignee_email,輪詢分配在重新執行時具決定性(相同輸入 -> 相同輸出,ORDER BY 以 order_id 作 tie-breaker),群組中沒有任何已啟用使用者的訂單會指派給第一個 admin(3c 後備),且後續一筆 RBAC 查詢(user 角色)只會回傳該使用者在其群組內被指派的訂單 — 確認分配沒有造成跨群組外洩

### IT-DR-12 INT-336 災難復原/備份
- **Given** 一條已填入資料、可驗證為完整的稽核鏈,以及一份資料庫的原生 MSSQL 備份
- **When** 清除資料庫並從備份 RESTORE、應用程式重新啟動、呼叫 verify-integrity;另外,直接在 SQL 中變更某一筆稽核資料列的 after_data 並重新啟動應用程式(觸發 AuditChainBackfillRunner)
- **Then** 還原後 verify-integrity 回傳 intact=true、totalRows == verifiedRows、firstBreakAt=null(因為密鑰與 DATETIME2 精度被保留,HMAC 在 BACKUP/RESTORE 中存活);對竄改案例,測試斷言文件所記載的風險 — AuditChainBackfillRunner case-3 會自我修復損壞的資料列並重新簽名,記錄一筆 WARN,因此 verify-integrity 錯誤地回報 intact — 並標記出竄改證據被重新啟動摧毀(一個在販售完整性之前必須修復、會阻擋發行的合規缺口)

### IT-DR-13 INT-337 韌性/故障轉移
- **Given** 一條在 HMAC 密鑰 S1 下驗證為完整的稽核鏈備份
- **When** 將資料庫還原到一個設定了「不同」CLM_AUDIT_HMAC_SECRET S2 的節點(金鑰輪替/設定錯誤),並在任何重新啟動回填重新簽名之前執行 verify-integrity
- **Then** verify-integrity 回報 broken,firstBreakAt 在第一筆資料列,且 firstBreakReason 為 'signature mismatch',證明稽核 HMAC 金鑰不可輪替,必須與資料庫一同備份;測試也斷言若鏈被還原到一個以比 DATETIME2-MICROS 更粗精度儲存時間戳的引擎,簽名會大規模失敗 — 使密鑰/精度與可驗證性之間的操作耦合明確化

### IT-DR-14 INT-338 資料遷移
- **Given** 重複的相同 CN 憑證,以及一個會拒絕 SUPERSEDED 值的殘留 status CHECK 約束
- **When** V17__supersede_old_orders 執行:它查詢 INFORMATION_SCHEMA 取得 acme_orders 上所有 CHECK 約束、卸除它們,接著將較舊的相同 CN 資料列 UPDATE 為 SUPERSEDED
- **Then** acme_orders 上所有 CHECK 約束都消失(因此 OrderStatus 列舉是唯一的驗證者),每個 CN 恰好有一筆資料列(created_at 最大者)維持 ISSUED/IMPORTED,所有嚴格較舊的相同 CN 資料列變成 SUPERSEDED,任何具有唯一 CN 的資料列都不被變更,且重新執行遷移是無作用的(對已填入資料的集合具冪等性)

### IT-DR-15 INT-339 災難復原/備份
- **Given** DR 的兩半(T1 的原生資料庫備份、T2 的帳號金鑰匯出)在不一致的時間點擷取,符合文件所記載的風險:它們是各自獨立的手動步驟
- **When** 從 T1 備份還原資料庫,並以 dryRun=false 將 T2 帳號金鑰匯出疊加還原在其上
- **Then** 測試偵測並回報所產生的不一致:參照帳號 A1 的訂單現在指向一把資料庫不再預期的金鑰、帳號 A3 被建立但沒有訂單、且任何帳號遺失的訂單都被標記出來;此情境斷言沒有自動化的時間點調和,並量測有效 RTO(達到一致、可簽發狀態的時間)與 RPO(T1 與 T2 之間遺失的資料),讓產品能記載具體的 BC/DR 數字,而非任其未經量測

---

## 合規 / 授權 / 可觀測

### IT-GOV-01 INT-340 合規強制
- **Given** 一個 'full' 合規授權處於啟用狀態;管理員透過 POST /api/v1/admin/policies 建立一個已啟用的政策 scope=global enforcement=BLOCK rulesJson={"ca.whitelist":["letsencrypt"]};呼叫者群組中存在一個 TWCA provider+帳號
- **When** 一位 user 對 POST /api/v1/{twca}/accounts/{accountId}/orders 為 CN test.example.com POST 一筆訂單(回傳 202 pendingApproval),接著一位 supervisor 呼叫 POST /api/v1/admin/cert-issuance-requests/{requestId}/approve
- **Then** 由於 provider 'twca' 不在白名單中,該核准必須被拒絕;沒有建立任何 ACME 訂單資料列(acme_orders 中沒有該 CN 的新資料列),CIR 不會默默地停在 APPROVED 而 createdOrderId=null,一筆 PolicyViolationEntity 資料列(severity=ERROR、ruleKey=ca.whitelist)被持久化並可在 GET /api/v1/admin/policies/violations 看到,且此失敗可稽核。斷言呼叫者收到一個確定的 policy_violation 訊號(而非一個不透明的 500)

### IT-GOV-02 INT-345 授權強制
- **Given** 一個有效(未到期)的分階授權處於啟用狀態;一位 user 提交一筆新訂單(202 pendingApproval)建立一個 PENDING CIR;接著將授權移至 EXPIRED(啟用一個已到期但真實的權杖,或將時鐘推進至到期之後)
- **When** 一位 supervisor 對現已過時的 PENDING CIR 呼叫 approve
- **Then** approve 被拒絕,訊息含有授權狀態(EXPIRED),沒有建立任何 ACME 訂單,且該 CIR 維持可操作(不會默默地變成 APPROVED)。另外斷言一筆全新的 POST 訂單提交仍被允許(提交不受授權閘門限制),但 approve 才是硬性閘門

### IT-GOV-03 INT-352 可觀測性
- **Given** 一套乾淨的 V67 後系統,已設定稽核 HMAC 密鑰
- **When** 執行一個具代表性的變更序列:登入、註冊帳號、提交簽發請求、核准、拒絕另一個、建立政策、啟用授權、建立端點、提交+核准一個下載請求(SECRET_DOWNLOADED)、撤銷核准;接著 GET /api/v1/audit(admin)與 GET /api/v1/audit/verify-integrity
- **Then** 每一個變更動作都有一筆對應的稽核資料列,具有正確的 actor/action/result/subject 與 before/after 快照(斷言存在 CERT_REQUEST_SUBMITTED/APPROVED/REJECTED、LICENSE_ACTIVATED、DOWNLOAD/SECRET_DOWNLOADED、REVOKE_*),且 verify-integrity 回傳 intact=true,verifiedRows==totalRows 且 firstBreakAt=null

### IT-GOV-04 INT-341 合規強制
- **Given** full-tier 授權;四個已啟用的 BLOCK 政策:san.no.wildcard=true、san.max.count=3、subject.cn.pattern=^[a-z0-9.-]+\.corp\.example\.com$、san.allowed.suffixes=[".corp.example.com"]
- **When** 針對以下每一個:(a) 訂單 CN *.corp.example.com、(b) 含 5 個 SAN 的訂單、(c) CN evil.attacker.net、(d) SAN foo.outside.org — 提交該訂單,接著由一位 supervisor 核准它
- **Then** 每次核准都被擋下,持久化一筆具有正確 ruleKey 的相異 PolicyViolationEntity,且提出請求者能確定拒絕原因(斷言存在一個請求者可見的訊號 — 一個 CIR 終態 rejected/blocked 狀態「或」一個通知,而非默默地停在 PENDING)。SAN 數量檢查必須計算 CN 與 SAN 的聯集(FD-03),因此一個只藏在 SAN 中的受管名稱仍會觸發

### IT-GOV-05 INT-342 合規強制
- **Given** full-tier 授權;已啟用的 BLOCK 政策 key.size.min=2048 與 signature.algorithm.blacklist=[sha1,md5];該 CN 存在一個 auto_deploy=true 的部署綁定;一筆訂單透過受控 CA/staging 或注入的 cert_pem 被推進至 ISSUED,且攜帶一張具有 1024 位元金鑰(或 SHA1 簽名)的葉憑證
- **When** AcmeOrderPoller 到達 ISSUED 並執行 evaluatePostIssuance
- **Then** 持久化一筆 PolicyViolationEntity(ERROR),寫入一筆 result=FAILURE 的 POST_ISSUANCE_POLICY_VIOLATION 稽核資料列,不會排入任何自動部署 DeployJob(blockDeploy 在 enqueueForOrder 之前短路),一封 cert.failed 電子郵件被排入給指派者的佇列,「但」訂單狀態維持 ISSUED 且該憑證仍可透過核准流程下載。斷言該憑證明確「不」被自動撤銷

### IT-GOV-06 INT-343 合規強制
- **Given** full-tier 授權;一個已啟用的 BLOCK 政策 key.size.min=2048
- **When** 管理員透過 POST /api/v1/certs/import 及透過批次提交 POST /api/v1/certs/import/batch/commit 匯入一張 1024 位元(或 SHA1 簽名)的外部 PEM
- **Then** 匯入成功並建立一筆 IMPORTED 訂單,「但」產生零筆 PolicyViolationEntity 資料列,且該弱憑證以受管資產的形式出現 — 記載此繞過行為。測試必須明確斷言此缺口以便追蹤,並驗證匯入的憑證即使略過政策仍「會」被計入 FQDN 配額

### IT-GOV-07 INT-344 授權強制
- **Given** 啟用一個 Standard(core7)階授權權杖
- **When** 管理員嘗試 POST /api/v1/admin/policies(建立自訂政策)、PUT /{id},以及 GET /api/v1/admin/policies/violations/export.xlsx;接著另外啟動一台全新的機器(core7)並檢視政策
- **Then** 建立/更新/匯出被拒絕(requireFullCompliance 拋出 -> 400 型升級訊息),DELETE「未」被設限(basic/core7 admin 可刪除已建種子的基準 — 斷言此誤觸風險),且 CompliancePolicySeedRunner 已自動以 WARN 模式建立一個 core7 基準(7 條規則)的種子,因此引擎不論階別都會評估全部 10 個 rule key

### IT-GOV-08 INT-346 授權強制
- **Given** 一個 EXPIRED 授權;一張既存的 ISSUED 憑證,具有一個部署綁定與一個在門檻內的自動續約政策
- **When** (a) 為該 ISSUED 憑證消費一個已核准的下載請求;(b) 排入並核准一次手動部署;(c) 呼叫 POST /api/v1/certs/renew-now 且自動續約掃描觸發
- **Then** 既有憑證的下載與部署「成功」(正式流量永不中斷),但續約在核准時被阻擋(canIssue=false)— 續約 CIR 無法進行。斷言授權狀態中 blocksNewOrders=true,但既有憑證的操作仍可使用

### IT-GOV-09 INT-347 授權強制
- **Given** 一個 quotaFqdn=5 的分階授權;既有的 ISSUED 訂單已消耗 4 個相異 FQDN
- **When** (a) 提交一筆其 CN+SAN 新增 2 個相異網域的新訂單(總計會達到 6);(b) 提交一筆單網域訂單(總計 5)接著第二筆單網域訂單(總計 6)並核准兩者;(c) 撤銷/取消一筆訂單後重新提交
- **Then** (a) 在提交時被拒絕,並附帶達到網域上限的訊息;(b) 第 6 個網域的核准被拒絕(在核准時重新檢查);(c) 在終態釋放之後,重新提交成功。斷言一張具有 5 個 SAN 的單一憑證會消耗 5 個額度(而非 1 個)

### IT-GOV-10 INT-348 併發/競態
- **Given** 一個 quotaFqdn=N 的分階授權;已管理 (N-1) 個網域;兩個 PENDING 的新 CIR,各自針對一個「不同」的新單網域 CN(兩者各自皆能容納,合在一起則超出)
- **When** 兩位核准者(或兩個執行緒)同時對這兩個相異的 requestId 呼叫 approve
- **Then** 記載實際行為:因為 wouldExceed() 讀取一個過時的 managedDomains() 快照,且對並行插入而言非交易性,兩者都可能通過並建立 2 筆訂單 -> usedCount 變成 N+1,超出配額。測試斷言最終的受管網域計數,並將任何超量標記為一個待修復的缺陷(資料庫層級防護或序列化檢查)

### IT-GOV-11 INT-350 授權強制
- **Given** 一個 Standard(非 Enterprise)授權;clm_settings 中同時設定 hsm.enabled=true 與 siem.enabled=true 並帶有效的 siem.host(模擬一個切換了開關的客戶)
- **When** 管理員呼叫 GET /api/v1/admin/hsm/status;且一個變更動作產生一個本應被 SIEM 轉送的稽核事件;接著在一個 Enterprise 授權下重新執行兩者
- **Then** 在 Standard 上:hsm/status 回傳 licensed=false 且「不」嘗試 PKCS#11 連線,且 SiemForwarder.forward() 無作用(featSiemExport=false),因此儘管 siem.enabled=true,「沒有任何東西」到達 syslog 收集器。在 Enterprise 上:hsm/status 嘗試連線,且 SIEM 發出一個 CEF 訊框到收集器。斷言光憑設定開關在沒有授權的情況下不足夠

### IT-GOV-12 INT-351 授權強制
- **Given** 一個有效的 Enterprise 授權;時鐘防護的高水位線已推進至一個已知時間;稽核/時鐘 HMAC 密鑰已設定
- **When** (a) 將系統時鐘往後移超過偏差容忍度(>120s)然後嘗試一筆訂單核准;(b) 一位 DBA 直接在 clm_settings 中調低 _license.clock.hwm 紀元而不重新簽署 HMAC,且系統重新讀取它
- **Then** (a) 授權狀態回報 CLOCK_TAMPERED 且 canIssue()=false -> 核准被阻擋,並附帶 NTP 校正訊息;(b) 被竄改的 HWM 通不過固定時間的 HMAC 驗證、被拒絕(回退至 0 然後從現在重建),因此此回撥無法藉由編輯資料庫來隱藏

### IT-GOV-13 INT-353 可觀測性
- **Given** 一套已有數列已提交稽核紀錄、且稽核鏈完整無損的系統
- **When** (a) 直接在資料庫中 UPDATE 某一列稽核紀錄的 after_data 欄位,並呼叫 verify-integrity;(b) DELETE 一列位於鏈中的紀錄並呼叫 verify-integrity;(c) 竄改某一列後 RESTART 應用程式(觸發 AuditChainBackfillRunner)並再次呼叫 verify-integrity
- **Then** (a) verify-integrity 回報 intact=false,firstBreakAt 為被竄改的那一列,reason 為 'signature mismatch';(b) 在孤立的串接點回報 previousHash mismatch;(c) 記錄該遮蔽缺陷:backfill 在開機時自我修復遭竄改的那一列,使得 verify-integrity 之後錯誤地回報 intact=true——本測試必須斷言此情形並將其標示為已知限制(僅能對外部編輯具備防竄改證據能力,對享有特權的重啟則無效)

### IT-GOV-14 INT-355 可觀測性
- **Given** 應用程式以出貨的 application.yml 執行(management.endpoints.web.exposure.include=health,info,prometheus,metrics;management.endpoint.health.show-details=always)
- **When** 一個未經驗證的用戶端對 /actuator/health、/actuator/info、/actuator/prometheus、/actuator/metrics 發出 GET;接著在資料庫停機的情況下探測就緒狀態(readiness)
- **Then** /actuator/health 在健康時回傳 200 UP,在資料庫無法連線時回傳 DOWN(503)(就緒狀態必須反映元件的真實狀態)。記錄此資安缺陷:show-details:always 在無正式環境覆寫設定的情況下,會將各元件的資料庫/磁碟細節揭露給匿名呼叫者——斷言匿名回應中存在詳細的資料庫元件區塊,並標示其需要一個正式環境覆寫設定(when-authorized)。確認 /prometheus 發出可抓取(scrapeable)的計量(metric)行

### IT-GOV-15 INT-358 韌性/故障轉移
- **Given** 應用程式設定為以正式環境(預設,非 dev/test)profile 啟動
- **When** (a) 以未設定或長度 <32 位元組的 CLM_JWT_SECRET 啟動;(b) 以未設定的 CLM_ENCRYPTION_KEY 啟動;(c) 在正式環境 profile 中以 acme.license.allow-unlicensed=true 啟動;(d) 以 SPRING_PROFILES_ACTIVE=dev 啟動
- **Then** (a) 與 (b) 在開機時快速失敗(應用程式 context 拒絕啟動,並有明確錯誤)——絕無不安全的隨機後援值;(c) allow-unlicensed 旗標被忽略,並產生一筆 ERROR 記錄(LicenseService.init),強制執行維持開啟(無授權時 canIssue 回傳 false,而非被繞過);(d) dev profile 可以啟動,但測試斷言/標示其危險性(每個密碼都被重設為 'testtest'、/api/v1/auth/dev-users 被公開),以便在正式環境的維運手冊中將其排除

### IT-GOV-16 INT-349 授權強制
- **Given** 一個 Standard 授權(依下調的訂閱對應,quotaCaTypes=2、quotaDnsTypes=3)
- **When** 建立 CA provider 第 2 個(POST /api/v1/providers)接著第 3 個;另外建立 DNS provider 帳號:在 3 種類型之後的第 4 個「相異」類型,以及一個「既有」類型的第 2 個帳號
- **Then** CA 第 2 個成功,CA 第 3 個被拒絕(達到 CA 連接器上限);DNS:第 4 個相異類型被拒絕,但既有類型的第 2 個帳號被允許(相異類型計數)。斷言並標記此不一致:ProviderController 使用 listAll().size()(原始「資料列」計數,第 69 行)且標示為 'CA types',而 DNS 使用相異類型 — 一個為同一個 CA 新增兩個帳號的客戶可能意外撞到 CA 上限

### IT-GOV-17 INT-354 併發/競態
- **Given** 兩個 RXCLM 應用程式執行個體共用同一個 MSSQL 資料庫(受支援的 ShedLock 叢集拓撲),兩者的稽核 HMAC 密鑰相同
- **When** 兩個節點在同一瞬間執行會寫入稽核紀錄的變更動作(在各節點同時核准),接著呼叫 GET /api/v1/audit/verify-integrity
- **Then** 記錄此行為:由於 chainLock 為 JVM 本機鎖(AuditChainWriter 在每個節點以 REQUIRES_NEW 提交,但無跨節點序列化),兩列紀錄可能共用相同的 previous_hash -> verify-integrity 在一條誠實的鏈上回報 previousHash-mismatch 斷裂,而這是假性竄改警報。斷言此分岔確實發生,並將其標示為足以阻擋發行的 HA 限制(建議採用單一稽核寫入節點或資料庫層級的序列化)

### IT-GOV-18 INT-356 可觀測性
- **Given** Enterprise 授權;已設定 siem.enabled=true、siem.host=127.0.0.1、siem.port=<擷取埠>、siem.protocol=udp
- **When** (a) 產生一個 FAILURE 稽核事件(登入失敗)與一個 SUCCESS 事件,並擷取 syslog 訊框;(b) 將 siem.host 指向一個黑洞/已關閉的埠,並在持續進行正常變更的同時灌入超過 1000 筆事件
- **Then** (a) 擷取到的訊框為有效的 CEF:0,FAILURE 的 severity=7、其餘為 3,且包含 suser/src/act/outcome/cs1Label=provider/cs3Label=subject;(b) 在停機期間稽核紀錄仍會寫入資料庫(轉發絕不阻斷),佇列丟棄最舊紀錄(DiscardOldest,上限 1000)且不向外傳播例外,變更作業不受影響。斷言即使 SIEM 事件被丟棄,也沒有任何稽核寫入遺失

### IT-GOV-19 INT-357 相容性
- **Given** 由 Flyway 種入的 clm_i18n_messages 資料表(V10..V95 逐步插入平行的 zh-TW/en 列)
- **When** 擷取 GET /api/v1/i18n/messages?locale=zh-TW 與 ?locale=en,計算兩者鍵值集合的對稱差;接著以 en 載入具代表性的 SPA 頁面(儀表板、憑證、admin/policies、admin/license、approvals)並掃描渲染出的文字
- **Then** 兩種語系具有完全相同的鍵值集合(zh-TW 有而 en 缺的鍵值為零,反之亦然)。沒有任何渲染出的字串等於其原始鍵值(例如 'page.dashboard' 或 'policy.violations.title' 不得逐字出現)。任何 en 缺漏的鍵值都應回報為缺陷,因為 I18nController 只回傳既有的列,而 SPA 會靜默退回到原始鍵值

---

## 審查補充（買方盡調）

### IT-REVIEW-01 INT-359 安全
- **Given** 一位銀行資安架構師要求書面證明:在「HSM」階層下產生的網域/帳號私鑰,無法被任何角色或路徑以明文方式外洩
- **When** 稽核員逐一觸發每一個承載金鑰的介面(GET .../key.pem、無通行碼的 backup.json、/admin/dr/account-keys 明文、download-request pfx/domain-key 取用),並在一次簽發 + 撤銷 + 部署週期中擷取 JVM 堆積傾印 + grep 所有應用程式記錄
- **Then** 在政策開啟的情況下,每一條明文匯出路徑皆回傳 403 並以失敗安全(fail-closed)方式關閉;同時必須揭露立基於事實的真相:CredentialCipher 仍為 @Primary,因此金鑰實際上是由源自 CLM_ENCRYPTION_KEY 的軟體 AES 金鑰所封存,PKCS#11 後端僅證明連通性——所以「金鑰絕不離開 HSM」的宣稱為假,必須在販售前予以修正或以合約方式界定範圍

### IT-REVIEW-02 INT-360 安全
- **Given** 採購要求將「任何 secret 絕不以明文形式靜置儲存或出現在記錄中」列為硬性控制項
- **When** 稽核員呼叫 GET /api/v1/admin/settings,觸發 SMTP/HSM/DNS 錯誤以強制產生堆疊追蹤,傾印 clm_settings 資料表,並 grep 記錄以搜尋 PIN/密碼/權杖值
- **Then** 測試必須讓 build 失敗,以證實已記錄的真相:hsm.pkcs11.pin 與 mail.smtp.password 以明文儲存於 clm_settings,且被 GET /admin/settings 逐字回傳;這是一個直接違規,會阻擋對銀行/政府的販售,必須予以修補(改為唯寫欄位 + 遮罩 + 對設定 secrets 的靜置加密)

### IT-REVIEW-03 INT-361 安全
- **Given** 一位政府買家要求對信任根元件(它把關所有權益(entitlement)以及(所宣稱的)HSM/SIEM 功能)提供 SBOM + 已簽署/已證明的相依項與可重現的 build
- **When** 稽核員檢查 local-repo/com/ruenxin/rxlicense-verify/2.0.0/ 是否有分離式簽章(detached signature)、pom 中的檢核碼釘選(checksum-pinning)以及 SBOM 項目;重新建置兩次以檢查可重現性;將該 jar 替換為一個遭竄改的 build
- **Then** build 必須針對一個已釘選的 SHA-256/PGP 簽章驗證該 jar,並在被替換時失敗;如今沒有任何東西釘選它(只有 _remote.repositories),因此一個惡意 jar 的替換能靜默地偽造授權,並可能中和該驗證公鑰——必須在販售前加入檢核碼強制執行、SBOM(CycloneDX)以及相依項漏洞關卡

### IT-REVIEW-04 INT-362 安全
- **Given** 銀行的供應商風險團隊要求對出貨的確切版本下的每一個遞移相依項(transitive dependency)提供 CVE 證明
- **When** 對已解析的相依樹(Spring Boot 3.3.5 平台管理版本、acme4j 5.1.0、poi-ooxml 5.2.5、bcpkix 1.78、mssql-jdbc、jsch 0.2.18、jjwt 0.12.5)執行一次相依項漏洞掃描
- **Then** 發行以「零個未修補的 Critical/High CVE」為關卡,並搭配一套已記錄的例外處理流程;尤其要特別標示承載 XML 的 POI 以及 SQL Server JDBC 驅動程式(因其已知的剖析器/反序列化類別);缺少掃描 = 自動阻擋採購

### IT-REVIEW-05 INT-363 合規強制
- **Given** 一位具備 WebTrust/CA-Browser-Forum 認知的買家詢問 RXCLM 如何防止對一個其 CAA 禁止所選 CA 的網域請求憑證,以及它如何在耗用一次 CA 驗證之前拒絕弱/已遭破解的金鑰
- **When** 提交以下訂單:(a) 一個帶有禁止該 CA 的 CAA 紀錄的網域、(b) 一把在已知弱金鑰黑名單上的匯入/CSR 提供金鑰、(c) 透過訂單 DTO 請求低於 2048 的 RSA keySize
- **Then** 揭露此缺口:並不存在任何內部 CAA 預檢(grep 未找到任何相關程式),且金鑰強度僅在 AcmeOrderService.generateDomainKey 內部驗證(硬性 400,而非經政策路由),且無 Debian 弱金鑰/ROCA 黑名單;誤簽發防範完全仰賴 CA——記錄此情形,並為受監管的買家加入簽發前的 CAA 檢查 + 弱金鑰篩檢

### IT-REVIEW-06 INT-365 可觀測性
- **Given** 一位滲透測試人員在盡職調查期間探測未驗證與低權限的攻擊面
- **When** GET /actuator/health(匿名)、/actuator/prometheus(匿名),接著以 role=user 身分 GET /actuator/metrics 與 /actuator/info
- **Then** 匿名的 /actuator/health 回傳完整的資料庫/元件/磁碟空間細節(show-details:always,無正式環境覆寫設定),且 /actuator/prometheus 將 JVM/HTTP/資料庫連線池內部資訊暴露給任何人;/actuator/metrics 與 /actuator/info 退回到 anyRequest().authenticated(),因此任何租戶使用者都能讀取全域、非依群組範圍限定的維運計量——兩者皆為資訊揭露阻擋項,需要對正式環境 actuator 進行鎖定

### IT-REVIEW-07 INT-366 安全
- **Given** 銀行的記錄衛生控制:任何憑證、私鑰、JWT 或完整 PII 皆不得被寫入記錄或在錯誤主體中回傳
- **When** 強制觸發解密失敗、SMTP 驗證失敗、DNS 供應商 401、F5 交握失敗、一個未處理的匯入例外(CertController 500)以及一個 acme4j AcmeException;擷取所有記錄 + HTTP 錯誤主體
- **Then** 沒有任何 account_key_pem/domain_key_pem、沒有 DNS/端點憑證 JSON、沒有 SMTP 密碼、沒有 Bearer/refresh 權杖,也沒有原始 exceptionClass/堆疊追蹤洩漏給用戶端;如今 CertController 的泛用處理常式會暴露 exceptionClass(資訊洩漏),且非同步輪詢器(poller)的例外可能記錄承載憑證的 context——必須予以修補

### IT-REVIEW-08 INT-368 災難復原/備份
- **Given** 銀行的營運持續/災難復原(BC/DR)政策要求一份有文件記載、有時間量測的 RTO/RPO 演練——而不僅僅是還原端點存在
- **When** 模擬節點 + 資料庫全毀,還原原生備份,透過 /admin/dr/account-keys/restore(dryRun=false)還原帳號金鑰,以「相同的」CLM_ENCRYPTION_KEY/CLM_AUDIT_HMAC_SECRET 啟動一個全新節點,接著針對同一個 CA 帳號簽發一次續約
- **Then** 擷取以實際時間計算的 RTO 與資料損失 RPO;證明稽核鏈能重新驗證、secrets 能解密,且一次真實的續約能對 CA 成功;若無法產出量測的 RTO/RPO 數字(本產品出貨時並無任何自動化的耦合),這是一個採購缺口,必須以一份維運手冊 + 演練成品來補足

### IT-REVIEW-09 INT-372 韌性/故障轉移
- **Given** 一個銀行營運團隊需要確保一筆已核准的憑證請求不會靜默地憑空消失
- **When** 核准一筆 PENDING 簽發請求,在 markApprovedIfPending 提交之後、AcmeOrderService.createOrder 之前的精確時刻 kill JVM,接著重啟
- **Then** 重啟後必須有一個復原清掃器,能偵測出 APPROVED-with-null-createdOrderId 的 CIR,並重新驅動它們或將其標為失敗並發出操作員警示;如今並不存在這樣的清掃器(回滾只在被攔截的例外時執行,而非當機時),因此憑證會靜默地永不簽發,且該憑證最終會過期——這是一個達到正式環境事故等級的缺口

### IT-REVIEW-10 INT-374 安全
- **Given** 一家銀行在購買前要求一份獨立的滲透測試報告
- **When** 對稽核/憑證/探索的 Specification 篩選器(commonNameLike、subject LIKE)進行注入模糊測試、嘗試 JWT alg:none/RS256 混淆,並以錯誤租戶以及 user-vs-assignee 的身分對每一個路徑 id 端點進行 IDOR 列舉
- **Then** 沒有 SQLi(JPA criteria 理應參數化,但 LIKE 模式與任何原生查詢必須被證明安全)、演算法混淆被拒絕,且每一次跨租戶/承辦人(assignee)存取皆回傳 404 而非資料——產出採購所要求的滲透測試成品;任何發現皆為發行阻擋項

### IT-REVIEW-11 INT-383 功能整合
- **Given** 某次續訂屆期並獲核准,新憑證抵達 ISSUED 但觸發了一項簽發後 BLOCK 違規
- **When** AcmeOrderPoller 執行簽發後評估:blockDeploy=true 在 enqueueForOrder 之前「以及」在 OrderSupersessionService 之前/前後即回傳
- **Then** 斷言新憑證維持 ISSUED 且可下載、自動部署被抑制(未建立部署作業)、舊憑證「未」被標記為 SUPERSEDED(因此該 CN 有兩張 ISSUED 憑證並存)、POST_ISSUANCE_POLICY_VIOLATION 稽核 + cert.failed 電子郵件皆觸發,且儀表板的「目前憑證」明確無歧義或被明確標示

### IT-REVIEW-12 INT-384 併發/競態
- **Given** 訂單 A 的部署作業正在執行(狀態 RUNNING)的當下,一張較新的同 CN 憑證取代了訂單 A
- **When** OrderSupersessionService 僅將 SKIPPED 連鎖套用至 PENDING_APPROVAL/QUEUED 作業(第 74 行略過 RUNNING),而 executeJob 重新擷取訂單 A,它現已為 SUPERSEDED,但因為安裝前沒有 SUPERSEDED/REVOKED 防護而繼續執行
- **Then** 記載/斷言該 RUNNING 作業是否安裝被取代的憑證;定義預期行為(中止並標記為 SKIPPED,或完成後再標示),並要求端點不得在無稽核訊號下仍提供一張過時、剛被取代的憑證

### IT-REVIEW-13 INT-386 可觀測性
- **Given** 在核准中途崩潰後,一筆 CIR 卡在 APPROVED 且 createdOrderId=null
- **When** 應用程式重新啟動且所有開機 runner(AcmeOrderRecoveryRunner 等)完成
- **Then** 斷言「沒有」訂單、「沒有」復原清掃器修復或重新驅動該 CIR,且關鍵在於「沒有」任何指標/告警/稽核浮現此孤兒——接著要求一項發布關卡決策:要不新增一個清掃器,要不新增一個可觀測的計數器/健康訊號,使無聲的永不簽發可被偵測;憑證絕不可在零維運訊號下無聲地永不簽發

### IT-REVIEW-14 INT-389 災難復原/備份
- **Given** 某 ADMIN 提交一個 dryRun=false 的還原內文,其純文字(無通行詞組)accounts 陣列的 sha256 與其內容不符(或 schemaVersion 不符/缺漏)
- **When** POST /api/v1/admin/dr/account-keys/restore(DrController writeAccount 依 containsKey upsert,第 156-169 行)且無完整性驗證
- **Then** 斷言目前行為(將被竄改/不符的金鑰無聲地 upsert 覆寫線上帳號),並要求還原在任何寫入之前驗證 sha256 與 schemaVersion、在不符時故障關閉 (fail-closed),且絕不從未經驗證的內文覆寫線上金鑰

### IT-REVIEW-15 INT-391 資料遷移
- **Given** 一個 V61(Java,MSSQL sp_rename)部分套用後應用程式崩潰的資料庫
- **When** 應用程式重新開機:FlywayConfig.repair() 清除失敗的歷史列,然後 migrate() 對一個已更名/半更名的欄位重新執行 V61
- **Then** 斷言精確的失效(重新執行時欄位重複/缺失,或因 before_data 處於不一致狀態而導致 V62/AuditEntity Hibernate validate 失敗),並以稽核更名鏈 V60->V61->V62 必須具冪等性或受防護為關卡;記載一個非冪等的 Java 遷移 + repair() = 開機迴圈

### IT-REVIEW-16 INT-395 相容性
- **Given** 一個叢集化叢集在有線上進行中工作的情況下進行滾動重啟升級
- **When** 重啟中的節點執行 AcmeOrderRecoveryRunner(無 ShedLock)與 DeployJobRecoveryRunner(無 ShedLock),而另一個節點仍在服務,且兩個節點同時寫入稽核列(JVM 本地的鏈鎖)
- **Then** 斷言可觀察的後果:跨節點的重複 CSR 提交 / 重複或失敗的部署,以及一個被 verify-integrity 誤判為遭竄改的分叉稽核鏈;以升級作業手冊須要求單一作用中節點或升級前清空為關卡

### IT-REVIEW-17 INT-403 可觀測性
- **Given** 一個成功的業務變更,其 AuditLogger.persist() 拋出例外(DB 當機、鎖飢餓、JSON 序列化錯誤)
- **When** 該例外被吞掉,而業務動作仍然 commit
- **Then** 斷言存在一個稽核缺口,且「沒有」任何浮現的錯誤、「沒有」任何失敗度量指標,接著管控稽核寫入失敗應發出一個可告警的訊號(計數器/ERROR 等級的 log/健康度降級),使一個默默的合規缺口可被偵測——特別是 SECRET_DOWNLOADED / REVOKE 事件絕不可在未稽核且未被察覺的情況下 commit

### IT-REVIEW-18 INT-364 合規強制
- **Given** 一家銀行詢問「若攻擊者從另一個 CA 取得我們網域的偽造憑證,RXCLM 會通知我們嗎?」(CT 監控是一項標準預期)
- **When** 稽核員審查探索(discovery)/撤銷子系統,看是否對受管的 CN 有任何 CT-log 輪詢
- **Then** 確認並記錄:CT-log/crt.sh 監控已被刻意移除(V66),且 RevocationStatusService 為無用程式碼(dead code)——RXCLM 對自有網域並未提供任何獨立的誤簽發/惡意憑證偵測;此項必須在資安宣傳中排除範圍,或予以加入

### IT-REVIEW-19 INT-367 效能/負載
- **Given** 一位買家的韌性審查會檢查已驗證端點上是否存在無界限的請求 DoS
- **When** 對 /api/v1/certs/import/batch/preview POST 一個數 GB 的檔案,對 /api/v1/cert-inspect POST 一個 500MB 的 PEM 區塊(無 @PreAuthorize、無大小上限),外加一個超大的 DR 還原 JSON
- **Then** 伺服器必須在緩衝進入堆積/POI 之前以 413 拒絕超大上傳;如今並未設定任何 multipart 限制,且 cert-inspect 無上限且對任何已驗證角色開放,因此單一使用者就能讓節點發生 OOM——需要 max-file-size、各端點上限,以及對 cert-inspect 的角色把關

### IT-REVIEW-20 INT-369 合規強制
- **Given** 一位買家的 GRC 團隊在購買前要求一份控制對證據的矩陣(職務分離 SoD = A.5.3/CC、存取控制 = A.5.15、密碼學 = A.8.24、變更管理、記錄 = CC7)
- **When** 針對每一項所宣傳的合規宣稱(四眼職務分離、防竄改證據稽核、最小權限多租戶、靜置加密、繫結的變更控制)執行其證明測試,並產出面向稽核員的成品(稽核 XLSX、verify-integrity 報告、核准前後的 JSON)
- **Then** 每一項宣稱都解析到一個綠燈測試 + 一份可攜帶的證據檔案;缺口被明確列出(例如稽核 XLSX 省略了 signature/previousHash 因此無法被獨立驗證;缺少依群組的 supervisor 稽核檢視),讓買家看到誠實的控制涵蓋情況

### IT-REVIEW-21 INT-370 合規強制
- **Given** 一位政府/GDPR/PDPA 買家要求一份資料保存排程,以及一套對個人資料站得住腳的被遺忘權(RTBF)說法
- **When** 稽核員要求 (a) 從稽核軌跡刪除一位離職員工的電子郵件、(b) 清除已過期的 refresh 權杖(清理作業明確跳過它們)、(c) 陳述含有憑證資料的 email_log 主體的保存期限
- **Then** 揭露此不可避免的衝突:從稽核鏈抹除 PII 會破壞 verify-integrity(防竄改證據 vs 抹除)、refresh 權杖登錄表帶著 PII 無界限地成長(無清除),且不存在任何書面的保存排程——在受監管的販售之前,需要一套資料分類 + 保存政策以及一個法律依據立場

### IT-REVIEW-22 INT-371 安全
- **Given** 對發放 admin 工作階段的 SSO 路徑進行縱深防禦審查
- **When** IdP 回傳含有腳本/破壞 HTML 之 payload 的 display_name;OidcController 建構 redirectTo = returnUrl + '#oidc=' + URLEncode(僅做引號跳脫的 json),而 SPA 讀取該 fragment
- **Then** Payload 必須被中和(嚴格的 JSON+HTML/JS 跳脫、SPA 中的輸出編碼、CSP script-src 'self' 必須封鎖 inline)——驗證該腳本不會執行;鑑於 CSP 允許 style-src 'unsafe-inline',也須確認不存在以樣式為基礎的外洩(exfiltration);若它確實執行,則一次 SSO-admin 接管即有可能

### IT-REVIEW-23 INT-373 韌性/故障轉移
- **Given** HA 部署審查:本產品透過 ShedLock 進行叢集化,卻不共用任何 secret 一致性檢查
- **When** 節點 B 以一個不同的 JWT secret(權杖在跨節點時 401)、一個不同的加密金鑰(既有密文在 B 上無法解密)以及一個不同的稽核 HMAC(B 使鏈分岔/假性竄改)啟動;以輪詢(round-robin)方式導流流量
- **Then** 要嘛必須有一個開機時的跨節點一致性斷言快速失敗,要嘛那些已記錄的失敗模式(隨機 401、解密失敗、假性「偵測到竄改」)必須被攔截並警示;橫跨一個叢集的靜默部分失敗,是銀行不會接受的營運事故

### IT-REVIEW-24 INT-375 安全
- **Given** 某銀行 DBA 安全審查詢問此應用程式的資料庫帳號持有哪些權限,以及磁碟區/欄位是否在儲存層加密(在應用程式 AES 之外的縱深防禦)
- **When** 檢視資料來源的授權層級(Flyway repair/migrate 是否需要 db_owner?),以及 TDE / 欄位層級加密是否被記載為一項部署需求
- **Then** 記載每次開機都執行 Flyway repair()+migrate() 會要求高權限的 DDL 權限(一項常駐權限風險),建議將遷移帳號與執行期帳號分離,並要求啟用 TDE,因為 cert_pem 與(若應用程式金鑰遭洩)所有密文都存放於同一個資料庫中——最小權限與儲存加密必須納入強化作業手冊

### IT-REVIEW-25 INT-377 效能/負載
- **Given** 針對某客戶在電子郵件/部署同時執行下續訂數百張憑證的情境進行容量審查
- **When** 在 SMTP 寄送耗時約 40 秒且數個 F5 部署卡住、全部爭用同一個執行緒池的情況下,觸發一波核准 (60+)
- **Then** 量測 ACME 輪詢是否被餓死/被佇列拒絕 (AbortPolicy),導致訂單卡在 VERIFYING/PROCESSING 而無 FAILED 狀態;要求執行緒池隔離(獨立的執行器)或有界的背壓——共用池飽和是一起無聲的訂單卡死事件

### IT-REVIEW-26 INT-381 安全
- **Given** 懂安全的買方要求精確的完整性保證,而非行銷主張
- **When** 以持有 HMAC 金鑰的特權內部人員身分,編輯一筆稽核紀錄、往後重新計算簽章,並執行完整性驗證;另外刪除尾端的紀錄
- **Then** 證明在金鑰持有者改寫後完整性驗證仍回報「完好 (intact)」,且尾端截斷無法被偵測——此控制僅對外部人員具備竄改證明性;若要做出真正的不可變性主張,需要外部公證/僅可附加的 WORM 或非對稱簽署,並確保銷售素材如實陳述此點(虛假主張是一項採購誠信風險)

### IT-REVIEW-27 INT-385 韌性/故障轉移
- **Given** 某 accountId 的已快取 Login,其 CA 端帳號已不再有效
- **When** 嘗試一次新簽發、續訂輪詢或撤銷時重用已快取的 Login(AcmeLoginManager.cache 從不過期;僅在 delete/register 時清除)
- **Then** 斷言該操作以清楚的診斷訊息失敗,且已快取的 Login 被失效/重新擷取(或訂單以可採取行動的錯誤落入 FAILED),而非不透明的卡死,或對該帳號上每筆訂單重複出現過時憑證失敗

### IT-REVIEW-28 INT-387 安全
- **Given** 一個未經驗證的網路用戶端
- **When** GET /actuator/prometheus(並確認 /actuator/metrics 的暴露範圍)
- **Then** 斷言所暴露的內容:訂單/憑證計數、佇列深度、JVM/資料庫連線池內部資訊,以及任何攜帶 CN/客戶/供應商名稱的指標標籤;要求對抓取端點加上驗證、清洗標籤,或明確記載接受此屬於偵察等級的揭露

### IT-REVIEW-29 INT-388 資料遷移
- **Given** 管理員透過 PUT /api/v1/admin/settings/siem.enabled 與 siem.host 設定 SIEM
- **When** 讀取這些鍵(V90 種入 siem.enabled/host/port/protocol/facility/vendor/product/version)並觸發一個受稽核的事件
- **Then** 斷言 PUT 回傳 200(而「非」404 setting_not_found)、這些鍵持久化,且在 Enterprise 上有一個 CEF 框被發送至收集器;在 Standard 上斷言無聲丟棄。明確淘汰/更正任何以「無遷移種入 siem.*」為前提的測試

### IT-REVIEW-30 INT-390 災難復原/備份
- **Given** 資料庫還原至時間 T,而帳號金鑰從 T-1 的匯出還原(時間點不一致)
- **When** 系統開機並對某筆訂單嘗試續訂/簽發,而該訂單的帳號在還原的金鑰集合中缺失,或某帳號存在卻無參照它的訂單
- **Then** 斷言該懸空參照被偵測並浮現(受影響 CN 的續訂以清楚的「帳號金鑰缺失」診斷失敗,而非無聲卡死),並量測/斷言該維運落差,使作業手冊強制要求以同一時間點擷取

### IT-REVIEW-31 INT-392 安全
- **Given** 某張群組 A 憑證的到期門檻被跨越,而其 assignee/creator 電子郵件是一個群組外的地址
- **When** CertExpiryNotifier.scan 在無群組/租戶驗證下解析 recipient = assignee_email,否則 created_by_email,並將含有 CN 與訂單資料的電子郵件加入佇列
- **Then** 斷言該電子郵件被寄送至該地址並攜帶群組 A 的 CN/SAN,證明此跨租戶資料洩漏,並要求依群組成員資格驗證收件者,或明確接受此風險

### IT-REVIEW-32 INT-393 安全
- **Given** U1 提交綁定申請(由 U2 核准),然後 U1 手動觸發一個部署作業 (triggeredBy=U1)
- **When** U2 核准該部署作業(triggeredBy=U1 != 核准者 U2 通過),因此跨「兩」道閘只有 U2 是一個明確的第二方,而 U1 影響了兩個階段
- **Then** 斷言該雙閘鏈是否真正在每個不可逆步驟強制獨立的第二人審查,或是否因 SoD 主體不對稱(requestedBy 對 triggeredBy)而在整條鏈上塌縮為單一明確核准者;定義所意圖的控制

### IT-REVIEW-33 INT-394 效能/負載
- **Given** 在 SMTP 緩慢時,一波核准 + 到期電子郵件 + 部署全部被派發至單一的 acmeTaskExecutor
- **When** 50 個槽位的佇列 + 10 個最大執行緒同時被全部四種非同步工作負載爭用
- **Then** 量測執行緒池佔用率,並斷言緩慢的電子郵件寄送「不會」將 ACME 輪詢/部署執行緒餓死至 AbortPolicy 拒絕或訂單卡在 VERIFYING/PROCESSING 的程度;以「每工作負載的執行緒池隔離」或「一份已記載的容量範圍」為關卡

### IT-REVIEW-34 INT-397 授權強制
- **Given** 一個以 Enterprise 階執行中的 JVM,且 HSM/SIEM/F5 正在使用中
- **When** 啟用一組 Standard 授權 token(activate() 會刷新快取),並針對 HSM 狀態、SIEM 送出、以及部署到既有 F5 endpoint 重新評估功能管控閘門
- **Then** 驗證哪些閘門會立即重新生效(canIssue/配額重新讀取),哪些則需要重啟(endpointTypeAllowed 僅在 CREATE 時強制執行,因此既有的 F5 endpoint 仍可部署;SIEM/HSM 的閘門行為),並管控降階不會默默地讓 Enterprise 專屬的資料路徑持續處於啟用狀態

### IT-REVIEW-35 INT-399 韌性/故障轉移
- **Given** Auto-DNS 發布了 TXT,但傳播在 2.5 分鐘上限內始終未收斂
- **When** 管線仍然呼叫 triggerCaValidation(寬鬆的自動化路徑),訂單落入 FAILED,而自動續約的退避機制反覆重新排入另一個完整的 publish+trigger 週期
- **Then** 驗證每個週期都消耗一次 LE 失敗驗證嘗試,衡量退避排程多快複合逼近 LE 失敗驗證封鎖,並管控在重新觸發之前應有傳播收斂的閘門「或」速率限制的察覺機制(目前兩者皆無)

### IT-REVIEW-36 INT-400 功能整合
- **Given** 在某個 binding 與自動續約政策仍參照著 DNS provider 帳號 P 時,該帳號 P 被刪除
- **When** AutoRenewScheduler 處理到該 CN,續約訂單的發布命中 getOrThrow『dns account not found』,訂單 FAILED,recordFailure 遞增 consecutive_failures,並在 max.retries(4) 之後設定 escalated 並送出 SLA 違反警示
- **Then** 驗證該 binding 被留下成懸空狀態、每個週期續約都失敗、升級被觸發,且該憑證最終默默逼近到期;管控 DNS provider 刪除要嘛在有使用中的 binding 時阻擋,要嘛主動警示該 binding/政策的擁有者

### IT-REVIEW-37 INT-405 韌性/故障轉移
- **Given** 一個針對數張 ISSUED 憑證、因金鑰外洩而依 CN 觸發的 break-glass 請求
- **When** 逐訂單的 breakGlass 迴圈對部分訂單成功,接著 CA 在迴圈中途失敗,且無交易邊界
- **Then** 斷言該 CN 被留下成部分撤銷(有些 REVOKED、有些仍 ISSUED 且存活)、REVOKE_BREAK_GLASS_BATCH 計數具誤導性,並管控回應/稽核應清楚列舉哪些憑證「未」被撤銷,使事件回應人員知道該外洩尚未被完全圍堵

### IT-REVIEW-38 INT-376 授權強制
- **Given** 買方探查以時間為基礎的權益強制執行是否可被擊破,以及一把金鑰的洩漏是否會連鎖擴散
- **When** 停用/餓死排程器執行緒,在 120 秒偏差範圍內跨重新啟動將時鐘往前再往後撥;另外建模 CLM_AUDIT_HMAC_SECRET 的洩漏
- **Then** 證明 HWM 停止推進,使得「先往前再往後」的時間窗可規避 CLOCK_TAMPERED,且洩漏這唯一一把共用的 HMAC 金鑰會同時擊破時鐘回退保護「與」稽核竄改證明——建議金鑰分離與排程器存活檢查

### IT-REVIEW-39 INT-378 安全
- **Given** 買方評估驗證控制是否可被轉化為針對特權使用者的自我 DoS
- **When** 跨兩個節點對某個已知的 admin 電子郵件反覆提交錯誤密碼以使該帳號永久處於鎖定狀態,同時將登入嘗試分散到各節點以倍增有效的速率限制
- **Then** 證明 admin 可被無限期鎖定(逐帳號鎖定與 IP 無關),且 N 個節點提供 N 倍的暴力破解容量(記憶體內限流器,無共用儲存)——需要分散式的速率限制/鎖定儲存,以及一項 admin 鎖定的防護措施(例如緊急破窗用的 IP 允許清單)

### IT-REVIEW-40 INT-379 效能/負載
- **Given** 針對預期在無 DBA 介入下運行數年的系統進行維運審查
- **When** 模擬持續的登入/刷新流量,量測 clm_refresh_token_registry 的成長(無清除)以及每次 /refresh 的 findByTokenHash + lastUsedAt 寫入延遲隨時間的變化;另外停用每日的 ShedLock 排程,觀察 mfa_pending/oidc_state 成長
- **Then** 量化資料表成長與延遲劣化,證明註冊表從不清除,並要求一項保留/清除作業——無界的驗證資料表成長是一起緩慢延燒的生產事件,也是一項鑑識/保留考量

### IT-REVIEW-41 INT-380 安全
- **Given** 買方的應用安全團隊就 XSS 韌性審查 CSP 以及 cookie/路由防護模型
- **When** 嘗試 'unsafe-inline' 樣式所允許的 CSS 注入/外洩向量,驗證 HttpOnly access cookie 是否確實阻擋 token 竊取,並確認 OIDC 重新導向鏈下的 SameSite + Secure 旗標
- **Then** 確認 script-src 'self' 阻擋 JS 注入,但記載 style-src 'unsafe-inline' 允許以 CSS 為基礎的資料外洩模式;建議採用以 nonce/hash 為基礎的樣式,並驗證 cookie 旗標成立,使 XSS 無法升級為 token 竊取

### IT-REVIEW-42 INT-396 相容性
- **Given** 完整的已遷移 i18n 訊息資料表
- **When** 跨所有已種入的鍵,比較 zh-TW 鍵集合與 en 鍵集合
- **Then** 斷言不存在任何鍵在 zh-TW 中有但在 en 中缺少(反之亦然);任何缺少的 en 鍵都是一項發布封鎖項,因為 SPA 會無聲地回退至原始鍵(例如 page.dashboard)而無可見錯誤

### IT-REVIEW-43 INT-398 併發/競態
- **Given** 一項即時設定變更透過節點 A 套用
- **When** 後續的請求(登入鎖定、token TTL、郵件送出)由節點 B 處理,而其 SettingsService.invalidate() 從未被觸發
- **Then** 驗證節點 B 會無限期繼續使用變更前的數值(無基於 TTL 的快取到期),並管控與安全相關的設定(鎖定門檻、TTL)應在叢集範圍內傳播,或被書面化為需要全節點重啟 / 黏著式路由(sticky routing)

### IT-REVIEW-44 INT-401 負向/邊界
- **Given** 為了刷新體驗而設定了一個很大的 access-token TTL
- **When** 使用者在共用工作站上閒置超過一般 30 分鐘的預期
- **Then** 驗證閒置自動登出在(現在很大的)TTL 流逝前「不會」觸發,證明單一設定同時控制兩項安全屬性;管控閒置逾時與 access TTL 應解耦,或將此耦合書面化為共用工作站部署的合規風險

### IT-REVIEW-45 INT-402 效能/負載
- **Given** 一個長期運行的部署,擁有眾多使用者與頻繁的靜默 refresh,持續累積 ISSUED+ROTATED+EXPIRED 列
- **When** 資料表成長到數百萬列,且每次 /auth/refresh 都針對這張臃腫的資料表執行 findByTokenHash(已建索引)+ lastUsedAt 寫入
- **Then** 衡量 refresh 的 p95 隨資料表成長的變化,並斷言要嘛有一個可接受的上界、要嘛要求一個清除作業;管控一張已知無上限的資料表不應在沒有書面化的保留/清理程序下發版

### IT-REVIEW-46 INT-404 韌性/故障轉移
- **Given** 一個針對大型資料集開機的應用,其遷移/回填 runner 在啟動時同步執行
- **When** LB 在 migrate/回填處於交易中途時探測 /actuator/health
- **Then** 斷言健康狀態究竟是過早回報 UP(把流量送往一個未就緒的節點),還是正確地管控就緒直到開機 runner 完成;管控應有一個與存活有所區別的就緒訊號,使 LB 不會把流量路由到一個仍在重新加密金鑰 / 重新簽署鏈的節點

### IT-REVIEW-47 INT-382 相容性
- **Given** 台灣政府買方要求正確的 zh-TW 標示,且在稽核相關畫面上不得有無聲的未翻譯 UI
- **When** 驗證 zh-TW NVARCHAR 來回往返不會出現亂碼、不分大小寫/重音的定序不會使兩個相異的訊息鍵衝突,以及缺漏的鍵顯示一個可見的指示器而非無聲地渲染原始鍵
- **Then** 所有字串正確渲染;缺漏的翻譯可在冒煙關卡中偵測到;定序不會造成鍵衝突——在合規/同意畫面上無聲的原始鍵回退,對於受規範的在地化而言是一項缺陷
